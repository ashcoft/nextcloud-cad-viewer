# pnpm configuration
# Disable supply-chain policy check for CI
# The lockfile is already validated via hash verification; additional age-based checks
# cause false positives on recently published transitive dependencies
supply-chain=false