var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { AcEdPromptOptions } from './AcEdPromptOptions';
/**
 * Options for prompting the user to enter a keyword,
 * similar to AutoCAD .NET `PromptKeywordOptions`.
 */
var AcEdPromptKeywordOptions = /** @class */ (function (_super) {
    __extends(AcEdPromptKeywordOptions, _super);
    /**
     * Constructs a new `AcEdPromptKeywordOptions` with a given prompt message.
     * @param message - The message shown to the user when prompting for a number.
     */
    function AcEdPromptKeywordOptions(message, globalKeywords) {
        var _this = _super.call(this, message, globalKeywords) || this;
        _this._allowNone = false;
        _this._allowArbitraryInput = false;
        return _this;
    }
    Object.defineProperty(AcEdPromptKeywordOptions.prototype, "allowNone", {
        /**
         * Gets or sets whether pressing ENTER alone (no input) is accepted.
         * Corresponds to `PromptKeywordOptions.AllowNone` in AutoCAD .NET API.
         */
        get: function () {
            return this._allowNone;
        },
        set: function (flag) {
            if (!this.isReadOnly) {
                this._allowNone = flag;
            }
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcEdPromptKeywordOptions.prototype, "allowArbitraryInput", {
        /**
         * Gets or sets whether arbitrary input is accepted.
         * If true, the prompt may allow strings or other input, depending on implementation.
         * Corresponds to `PromptKeywordOptions.AllowArbitraryInput` in AutoCAD .NET API.
         */
        get: function () {
            return this._allowArbitraryInput;
        },
        set: function (flag) {
            if (!this.isReadOnly) {
                this._allowArbitraryInput = flag;
            }
        },
        enumerable: false,
        configurable: true
    });
    return AcEdPromptKeywordOptions;
}(AcEdPromptOptions));
export { AcEdPromptKeywordOptions };
//# sourceMappingURL=AcEdPromptKeywordOptions.js.map