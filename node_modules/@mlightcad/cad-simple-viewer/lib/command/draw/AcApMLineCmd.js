var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcDbMLine, AcDbMLineJustification, AcGePoint3d } from '@mlightcad/data-model';
import { AcApDocManager } from '../../app';
import { AcEdCommand, AcEdOpenMode, AcEdPreviewJig, AcEdPromptDoubleOptions, AcEdPromptKeywordOptions, AcEdPromptPointOptions, AcEdPromptStateMachine, AcEdPromptStatus, AcEdPromptStringOptions } from '../../editor';
import { AcApI18n } from '../../i18n';
var ACAP_MLINE_EPSILON = 1e-9;
/**
 * Preview jig that renders an MLINE from confirmed points and the current cursor point.
 */
var AcApMLineJig = /** @class */ (function (_super) {
    __extends(AcApMLineJig, _super);
    function AcApMLineJig(view, points, resolveState) {
        var _this = _super.call(this, view) || this;
        _this._mline = new AcDbMLine();
        _this._points = points;
        _this._resolveState = resolveState;
        if (points.length > 0) {
            var state = _this._resolveState();
            applyMLineGeometry(_this._mline, points, state.styleName, state.justification, state.scale, false, state.elementOffsets);
        }
        return _this;
    }
    Object.defineProperty(AcApMLineJig.prototype, "entity", {
        get: function () {
            return this._mline;
        },
        enumerable: false,
        configurable: true
    });
    AcApMLineJig.prototype.update = function (point) {
        if (this._points.length <= 0)
            return;
        var previewPoints = __spreadArray(__spreadArray([], __read(this._points), false), [new AcGePoint3d(point)], false);
        var state = this._resolveState();
        applyMLineGeometry(this._mline, previewPoints, state.styleName, state.justification, state.scale, false, state.elementOffsets);
    };
    return AcApMLineJig;
}(AcEdPreviewJig));
export { AcApMLineJig };
/**
 * Command to create one AcDbMLine entity.
 *
 * Behavior is aligned with AutoCAD MLINE:
 * - Supports `Justification`, `Scale`, `Style` options during point input.
 * - Supports `Undo` and `Close` after first segment.
 */
var AcApMLineCmd = /** @class */ (function (_super) {
    __extends(AcApMLineCmd, _super);
    function AcApMLineCmd() {
        var _this = _super.call(this) || this;
        _this.mode = AcEdOpenMode.Write;
        return _this;
    }
    AcApMLineCmd.prototype.execute = function (context) {
        return __awaiter(this, void 0, void 0, function () {
            var db, justification, scale, styleName, points, shouldClose, resolveStyleElementOffsets, appendPoint, undoLastPoint, closePath, promptJustification, promptScale, showStyleList, promptStyle, handleCommonKeyword, prompt_1, result, keepRunning, NextPointState, machine, mline;
            var _this = this;
            var _a, _b;
            return __generator(this, function (_c) {
                switch (_c.label) {
                    case 0:
                        db = context.doc.database;
                        justification = AcDbMLineJustification.Zero;
                        scale = (_a = db.cmlscale) !== null && _a !== void 0 ? _a : 1;
                        styleName = db.cmlstyle || 'STANDARD';
                        points = [];
                        shouldClose = false;
                        resolveStyleElementOffsets = function () {
                            var _a;
                            var style = db.objects.mlineStyle.getAt(styleName);
                            return resolveMLineStyleElementOffsets((_a = style === null || style === void 0 ? void 0 : style.elements) !== null && _a !== void 0 ? _a : [], justification, scale);
                        };
                        appendPoint = function (point) {
                            return points.push(new AcGePoint3d(point));
                        };
                        undoLastPoint = function () {
                            if (points.length > 1) {
                                points.pop();
                            }
                        };
                        closePath = function () {
                            if (points.length <= 1)
                                return;
                            var first = points[0];
                            var current = points[points.length - 1];
                            if (!isSamePoint3d(first, current)) {
                                appendPoint(first);
                            }
                            shouldClose = true;
                        };
                        promptJustification = function () { return __awaiter(_this, void 0, void 0, function () {
                            var prompt, result, keyword;
                            var _a;
                            return __generator(this, function (_b) {
                                switch (_b.label) {
                                    case 0:
                                        prompt = new AcEdPromptKeywordOptions(AcApI18n.t('jig.mline.justificationPrompt'));
                                        prompt.allowNone = true;
                                        prompt.keywords.add(AcApI18n.t('jig.mline.keywords.top.display'), AcApI18n.t('jig.mline.keywords.top.global'), AcApI18n.t('jig.mline.keywords.top.local'));
                                        prompt.keywords.add(AcApI18n.t('jig.mline.keywords.zero.display'), AcApI18n.t('jig.mline.keywords.zero.global'), AcApI18n.t('jig.mline.keywords.zero.local'));
                                        prompt.keywords.add(AcApI18n.t('jig.mline.keywords.bottom.display'), AcApI18n.t('jig.mline.keywords.bottom.global'), AcApI18n.t('jig.mline.keywords.bottom.local'));
                                        return [4 /*yield*/, AcApDocManager.instance.editor.getKeywords(prompt)];
                                    case 1:
                                        result = _b.sent();
                                        if (result.status === AcEdPromptStatus.None)
                                            return [2 /*return*/, true];
                                        if (result.status !== AcEdPromptStatus.Keyword)
                                            return [2 /*return*/, false];
                                        keyword = (_a = result.stringResult) !== null && _a !== void 0 ? _a : '';
                                        if (keyword === 'Top') {
                                            justification = AcDbMLineJustification.Top;
                                        }
                                        else if (keyword === 'Bottom') {
                                            justification = AcDbMLineJustification.Bottom;
                                        }
                                        else {
                                            justification = AcDbMLineJustification.Zero;
                                        }
                                        return [2 /*return*/, true];
                                }
                            });
                        }); };
                        promptScale = function () { return __awaiter(_this, void 0, void 0, function () {
                            var prompt, result;
                            var _a;
                            return __generator(this, function (_b) {
                                switch (_b.label) {
                                    case 0:
                                        prompt = new AcEdPromptDoubleOptions(AcApI18n.t('jig.mline.scalePrompt'));
                                        prompt.allowNone = true;
                                        prompt.allowZero = true;
                                        prompt.allowNegative = true;
                                        prompt.useDefaultValue = true;
                                        prompt.defaultValue = scale;
                                        return [4 /*yield*/, AcApDocManager.instance.editor.getDouble(prompt)];
                                    case 1:
                                        result = _b.sent();
                                        if (result.status === AcEdPromptStatus.None)
                                            return [2 /*return*/, true];
                                        if (result.status !== AcEdPromptStatus.OK)
                                            return [2 /*return*/, false];
                                        scale = (_a = result.value) !== null && _a !== void 0 ? _a : scale;
                                        return [2 /*return*/, true];
                                }
                            });
                        }); };
                        showStyleList = function () {
                            var styleNames = __spreadArray([], __read(db.objects.mlineStyle.entries()), false).map(function (_a) {
                                var _b = __read(_a, 1), name = _b[0];
                                return name;
                            });
                            if (styleNames.length <= 0) {
                                AcApDocManager.instance.editor.showMessage(AcApI18n.t('jig.mline.styleListEmpty'), 'warning');
                                return;
                            }
                            AcApDocManager.instance.editor.showMessage("".concat(AcApI18n.t('jig.mline.styleListHeader'), " ").concat(styleNames.join(', ')), 'info');
                        };
                        promptStyle = function () { return __awaiter(_this, void 0, void 0, function () {
                            var prompt_2, result, raw;
                            var _a;
                            return __generator(this, function (_b) {
                                switch (_b.label) {
                                    case 0:
                                        if (!true) return [3 /*break*/, 2];
                                        prompt_2 = new AcEdPromptStringOptions(AcApI18n.t('jig.mline.stylePrompt'));
                                        prompt_2.allowEmpty = false;
                                        prompt_2.allowSpaces = false;
                                        return [4 /*yield*/, AcApDocManager.instance.editor.getString(prompt_2)];
                                    case 1:
                                        result = _b.sent();
                                        if (result.status !== AcEdPromptStatus.OK)
                                            return [2 /*return*/, false];
                                        raw = ((_a = result.stringResult) !== null && _a !== void 0 ? _a : '').trim();
                                        if (!raw)
                                            return [3 /*break*/, 0];
                                        if (raw === '?') {
                                            showStyleList();
                                            return [3 /*break*/, 0];
                                        }
                                        styleName = raw;
                                        return [2 /*return*/, true];
                                    case 2: return [2 /*return*/];
                                }
                            });
                        }); };
                        handleCommonKeyword = function (keyword) { return __awaiter(_this, void 0, void 0, function () {
                            return __generator(this, function (_a) {
                                switch (_a.label) {
                                    case 0:
                                        if (!(keyword === 'Justification')) return [3 /*break*/, 2];
                                        return [4 /*yield*/, promptJustification()];
                                    case 1: return [2 /*return*/, _a.sent()];
                                    case 2:
                                        if (!(keyword === 'Scale')) return [3 /*break*/, 4];
                                        return [4 /*yield*/, promptScale()];
                                    case 3: return [2 /*return*/, _a.sent()];
                                    case 4:
                                        if (!(keyword === 'Style')) return [3 /*break*/, 6];
                                        return [4 /*yield*/, promptStyle()];
                                    case 5: return [2 /*return*/, _a.sent()];
                                    case 6: return [2 /*return*/, true];
                                }
                            });
                        }); };
                        _c.label = 1;
                    case 1:
                        if (!true) return [3 /*break*/, 4];
                        prompt_1 = new AcEdPromptPointOptions(AcApI18n.t('jig.mline.startPointWithOptions'));
                        prompt_1.keywords.add(AcApI18n.t('jig.mline.keywords.justification.display'), AcApI18n.t('jig.mline.keywords.justification.global'), AcApI18n.t('jig.mline.keywords.justification.local'));
                        prompt_1.keywords.add(AcApI18n.t('jig.mline.keywords.scale.display'), AcApI18n.t('jig.mline.keywords.scale.global'), AcApI18n.t('jig.mline.keywords.scale.local'));
                        prompt_1.keywords.add(AcApI18n.t('jig.mline.keywords.style.display'), AcApI18n.t('jig.mline.keywords.style.global'), AcApI18n.t('jig.mline.keywords.style.local'));
                        return [4 /*yield*/, AcApDocManager.instance.editor.getPoint(prompt_1)];
                    case 2:
                        result = _c.sent();
                        if (result.status === AcEdPromptStatus.OK) {
                            appendPoint(result.value);
                            return [3 /*break*/, 4];
                        }
                        if (result.status !== AcEdPromptStatus.Keyword) {
                            return [2 /*return*/];
                        }
                        return [4 /*yield*/, handleCommonKeyword((_b = result.stringResult) !== null && _b !== void 0 ? _b : '')];
                    case 3:
                        keepRunning = _c.sent();
                        if (!keepRunning)
                            return [2 /*return*/];
                        return [3 /*break*/, 1];
                    case 4:
                        NextPointState = /** @class */ (function () {
                            function NextPointState() {
                            }
                            NextPointState.prototype.buildPrompt = function () {
                                var prompt = new AcEdPromptPointOptions(AcApI18n.t('jig.mline.nextPointWithOptions'));
                                prompt.keywords.add(AcApI18n.t('jig.mline.keywords.justification.display'), AcApI18n.t('jig.mline.keywords.justification.global'), AcApI18n.t('jig.mline.keywords.justification.local'));
                                prompt.keywords.add(AcApI18n.t('jig.mline.keywords.scale.display'), AcApI18n.t('jig.mline.keywords.scale.global'), AcApI18n.t('jig.mline.keywords.scale.local'));
                                prompt.keywords.add(AcApI18n.t('jig.mline.keywords.style.display'), AcApI18n.t('jig.mline.keywords.style.global'), AcApI18n.t('jig.mline.keywords.style.local'));
                                prompt.keywords.add(AcApI18n.t('jig.mline.keywords.undo.display'), AcApI18n.t('jig.mline.keywords.undo.global'), AcApI18n.t('jig.mline.keywords.undo.local'));
                                if (points.length > 1) {
                                    prompt.keywords.add(AcApI18n.t('jig.mline.keywords.close.display'), AcApI18n.t('jig.mline.keywords.close.global'), AcApI18n.t('jig.mline.keywords.close.local'));
                                }
                                prompt.useDashedLine = true;
                                prompt.useBasePoint = true;
                                prompt.basePoint = new AcGePoint3d(points[points.length - 1]);
                                prompt.jig = new AcApMLineJig(context.view, points, function () { return ({
                                    justification: justification,
                                    scale: scale,
                                    styleName: styleName,
                                    elementOffsets: resolveStyleElementOffsets()
                                }); });
                                return prompt;
                            };
                            NextPointState.prototype.handleResult = function (result) {
                                return __awaiter(this, void 0, void 0, function () {
                                    var keyword, keepRunning;
                                    var _a;
                                    return __generator(this, function (_b) {
                                        switch (_b.label) {
                                            case 0:
                                                if (result.status === AcEdPromptStatus.OK) {
                                                    appendPoint(result.value);
                                                    return [2 /*return*/, 'continue'];
                                                }
                                                if (result.status !== AcEdPromptStatus.Keyword) {
                                                    return [2 /*return*/, 'finish'];
                                                }
                                                keyword = (_a = result.stringResult) !== null && _a !== void 0 ? _a : '';
                                                if (keyword === 'Undo') {
                                                    undoLastPoint();
                                                    return [2 /*return*/, 'continue'];
                                                }
                                                if (keyword === 'Close' && points.length > 1) {
                                                    closePath();
                                                    return [2 /*return*/, 'finish'];
                                                }
                                                return [4 /*yield*/, handleCommonKeyword(keyword)];
                                            case 1:
                                                keepRunning = _b.sent();
                                                return [2 /*return*/, keepRunning ? 'continue' : 'finish'];
                                        }
                                    });
                                });
                            };
                            return NextPointState;
                        }());
                        machine = new AcEdPromptStateMachine();
                        machine.setState(new NextPointState());
                        return [4 /*yield*/, machine.run(function (prompt) { return AcApDocManager.instance.editor.getPoint(prompt); })];
                    case 5:
                        _c.sent();
                        if (points.length < 2)
                            return [2 /*return*/];
                        mline = new AcDbMLine();
                        applyMLineGeometry(mline, points, styleName, justification, scale, shouldClose, resolveStyleElementOffsets());
                        db.tables.blockTable.modelSpace.appendEntity(mline);
                        db.cmlstyle = styleName;
                        db.cmlscale = scale;
                        return [2 /*return*/];
                }
            });
        });
    };
    return AcApMLineCmd;
}(AcEdCommand));
export { AcApMLineCmd };
function applyMLineGeometry(mline, points, styleName, justification, scale, closed, elementOffsets) {
    if (points.length <= 0) {
        mline.clearSegments();
        return;
    }
    mline.styleName = styleName;
    // Let AcDbMLine resolve style from current database style dictionaries.
    mline.styleObjectHandle = '';
    mline.justification = justification;
    mline.scale = scale;
    mline.styleCount = elementOffsets.length;
    mline.closed = closed;
    mline.startPosition = points[0];
    if (points.length < 2) {
        mline.clearSegments();
        return;
    }
    mline.segments = createMLineSegments(points, elementOffsets);
}
function createMLineSegments(points, elementOffsets) {
    var edgeDirections = createEdgeDirections(points);
    return points.slice(1).map(function (position, index) {
        var vertexIndex = index + 1;
        var miter = createMiterInfo(edgeDirections, vertexIndex);
        return {
            position: position,
            direction: edgeDirections[Math.min(vertexIndex, edgeDirections.length - 1)],
            miterDirection: miter.direction,
            elements: elementOffsets.map(function (offset) { return ({
                parameterCount: 1,
                parameters: [offset * miter.offsetScale],
                fillCount: 0,
                fillParameters: []
            }); })
        };
    });
}
function createEdgeDirections(points) {
    var directions = [];
    for (var i = 0; i < points.length - 1; i++) {
        var from = points[i];
        var to = points[i + 1];
        var direction = normalizeVector({
            x: to.x - from.x,
            y: to.y - from.y,
            z: to.z - from.z
        });
        directions.push(direction !== null && direction !== void 0 ? direction : { x: 1, y: 0, z: 0 });
    }
    return directions;
}
function createMiterInfo(edgeDirections, vertexIndex) {
    if (edgeDirections.length <= 0) {
        return { direction: { x: 0, y: 1, z: 0 }, offsetScale: 1 };
    }
    if (vertexIndex <= 0) {
        return { direction: leftNormal(edgeDirections[0]), offsetScale: 1 };
    }
    if (vertexIndex >= edgeDirections.length) {
        return {
            direction: leftNormal(edgeDirections[edgeDirections.length - 1]),
            offsetScale: 1
        };
    }
    var prevNormal = leftNormal(edgeDirections[vertexIndex - 1]);
    var nextNormal = leftNormal(edgeDirections[vertexIndex]);
    var blended = normalizeVector({
        x: prevNormal.x + nextNormal.x,
        y: prevNormal.y + nextNormal.y,
        z: prevNormal.z + nextNormal.z
    });
    if (!blended) {
        return { direction: nextNormal, offsetScale: 1 };
    }
    var projection = Math.abs(dotVector(blended, nextNormal));
    if (projection <= ACAP_MLINE_EPSILON) {
        return { direction: blended, offsetScale: 1 };
    }
    return {
        direction: blended,
        offsetScale: 1 / projection
    };
}
function resolveMLineStyleElementOffsets(elements, justification, scale) {
    var e_1, _a;
    if (elements.length <= 0)
        return [];
    var minOffset = Number.POSITIVE_INFINITY;
    var maxOffset = Number.NEGATIVE_INFINITY;
    try {
        for (var elements_1 = __values(elements), elements_1_1 = elements_1.next(); !elements_1_1.done; elements_1_1 = elements_1.next()) {
            var element = elements_1_1.value;
            if (element.offset < minOffset)
                minOffset = element.offset;
            if (element.offset > maxOffset)
                maxOffset = element.offset;
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (elements_1_1 && !elements_1_1.done && (_a = elements_1.return)) _a.call(elements_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    var shift = 0;
    if (justification === AcDbMLineJustification.Top) {
        shift = -maxOffset;
    }
    else if (justification === AcDbMLineJustification.Bottom) {
        shift = -minOffset;
    }
    return elements.map(function (element) { return (element.offset + shift) * scale; });
}
function leftNormal(vector) {
    var normal = normalizeVector({
        x: -vector.y,
        y: vector.x,
        z: 0
    });
    return normal !== null && normal !== void 0 ? normal : { x: 0, y: 1, z: 0 };
}
function normalizeVector(vector) {
    var length = Math.sqrt(vector.x * vector.x + vector.y * vector.y + vector.z * vector.z);
    if (length <= ACAP_MLINE_EPSILON)
        return undefined;
    return {
        x: vector.x / length,
        y: vector.y / length,
        z: vector.z / length
    };
}
function dotVector(a, b) {
    return a.x * b.x + a.y * b.y + a.z * b.z;
}
function isSamePoint3d(a, b) {
    return (Math.abs(a.x - b.x) <= ACAP_MLINE_EPSILON &&
        Math.abs(a.y - b.y) <= ACAP_MLINE_EPSILON &&
        Math.abs(a.z - b.z) <= ACAP_MLINE_EPSILON);
}
//# sourceMappingURL=AcApMLineCmd.js.map