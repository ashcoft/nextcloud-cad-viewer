var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
import { AcTrLinePatternShaders, createGradientHatchShaderMaterialFromUniforms, createHatchPatternShaderMaterial } from '@mlightcad/three-renderer';
import * as THREE from 'three';
import { LineMaterial } from 'three/examples/jsm/lines/LineMaterial.js';
import { copyFloat32Range } from './AcExBatchBuffers';
/** Shared camera zoom uniform updated by the offline HTML viewer. */
export var acexCameraZoomUniform = { value: 1.0 };
function asShaderMaterial(material) {
    var candidate = material;
    if (candidate.isShaderMaterial === true ||
        material.type === 'ShaderMaterial') {
        return candidate;
    }
    return undefined;
}
/**
 * Reads a serialized linetype from a live export-time shader material.
 */
export function extractLinePattern(material) {
    var _a, _b;
    var shaderMaterial = asShaderMaterial(material);
    if (!shaderMaterial) {
        return undefined;
    }
    var patternUniform = shaderMaterial.uniforms.pattern;
    var patternLengthUniform = shaderMaterial.uniforms.patternLength;
    if (!patternUniform || !patternLengthUniform) {
        return undefined;
    }
    var pattern = patternUniform.value;
    if (!Array.isArray(pattern) || pattern.length === 0) {
        return undefined;
    }
    return {
        pattern: __spreadArray([], __read(pattern), false),
        patternLength: Number(patternLengthUniform.value),
        viewportScale: Number((_b = (_a = shaderMaterial.uniforms.u_viewportScale) === null || _a === void 0 ? void 0 : _a.value) !== null && _b !== void 0 ? _b : 1)
    };
}
/**
 * Reads a serialized hatch pattern from a live export-time shader material.
 */
export function extractHatchPattern(material) {
    var _a, _b;
    var shaderMaterial = asShaderMaterial(material);
    if (!shaderMaterial) {
        return undefined;
    }
    var patternLinesUniform = shaderMaterial.uniforms.u_patternLines;
    if (!patternLinesUniform) {
        return undefined;
    }
    var patternLines = patternLinesUniform.value;
    if (!Array.isArray(patternLines) || patternLines.length === 0) {
        return undefined;
    }
    return {
        patternAngle: Number((_b = (_a = shaderMaterial.uniforms.u_patternAngle) === null || _a === void 0 ? void 0 : _a.value) !== null && _b !== void 0 ? _b : 0),
        patternLines: patternLines.map(serializeHatchPatternLine)
    };
}
/**
 * Reads a serialized gradient fill from a live export-time shader material.
 */
export function extractGradientFill(material) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    var shaderMaterial = asShaderMaterial(material);
    if (!shaderMaterial) {
        return undefined;
    }
    if (shaderMaterial.uniforms.u_patternLines) {
        return undefined;
    }
    var startColor = (_a = shaderMaterial.uniforms.u_startColor) === null || _a === void 0 ? void 0 : _a.value;
    var endColor = (_b = shaderMaterial.uniforms.u_endColor) === null || _b === void 0 ? void 0 : _b.value;
    var gradientTypeUniform = shaderMaterial.uniforms.u_gradientType;
    if (!(startColor === null || startColor === void 0 ? void 0 : startColor.getHex) || gradientTypeUniform == null) {
        return undefined;
    }
    return {
        startColor: startColor.getHex(),
        endColor: (_d = (_c = endColor === null || endColor === void 0 ? void 0 : endColor.getHex) === null || _c === void 0 ? void 0 : _c.call(endColor)) !== null && _d !== void 0 ? _d : startColor.getHex(),
        angle: Number((_f = (_e = shaderMaterial.uniforms.u_angle) === null || _e === void 0 ? void 0 : _e.value) !== null && _f !== void 0 ? _f : 0),
        shift: Number((_h = (_g = shaderMaterial.uniforms.u_shift) === null || _g === void 0 ? void 0 : _g.value) !== null && _h !== void 0 ? _h : 0),
        gradientType: Number(gradientTypeUniform.value)
    };
}
function serializeHatchPatternLine(line) {
    return {
        angle: line.angle,
        base: [line.base.x, line.base.y],
        offset: [line.offset.x, line.offset.y],
        dashLengths: __spreadArray([], __read(line.dashLengths), false),
        patternLength: line.patternLength
    };
}
function deserializeHatchPatternLine(line) {
    return {
        angle: line.angle,
        base: new THREE.Vector2(line.base[0], line.base[1]),
        offset: new THREE.Vector2(line.offset[0], line.offset[1]),
        dashLengths: __spreadArray([], __read(line.dashLengths), false),
        patternLength: line.patternLength
    };
}
/**
 * Computes cumulative `lineDistance` values for line-segment geometry.
 */
export function computeLineDistancesForSegments(positions) {
    var _a, _b;
    var vertexCount = positions.length / 3;
    if (vertexCount < 2) {
        return new Float32Array(0);
    }
    var lineDistances = new Float32Array(vertexCount);
    for (var i = 0; i < vertexCount; i += 2) {
        if (i === 0) {
            lineDistances[i] = 0;
        }
        else {
            lineDistances[i] = lineDistances[i - 1];
        }
        var x1 = positions[i * 3];
        var y1 = positions[i * 3 + 1];
        var z1 = (_a = positions[i * 3 + 2]) !== null && _a !== void 0 ? _a : 0;
        var x2 = positions[(i + 1) * 3];
        var y2 = positions[(i + 1) * 3 + 1];
        var z2 = (_b = positions[(i + 1) * 3 + 2]) !== null && _b !== void 0 ? _b : 0;
        var dx = x2 - x1;
        var dy = y2 - y1;
        var dz = z2 - z1;
        lineDistances[i + 1] =
            lineDistances[i] + Math.sqrt(dx * dx + dy * dy + dz * dz);
    }
    return lineDistances;
}
/**
 * Copies the `lineDistance` attribute honoring the geometry draw range.
 */
export function exportLineDistanceSlice(geometry) {
    var lineDistanceAttr = geometry.getAttribute('lineDistance');
    if (!lineDistanceAttr || lineDistanceAttr.count === 0) {
        return undefined;
    }
    var indexAttr = geometry.getIndex();
    if (indexAttr) {
        var array_1 = lineDistanceAttr.array;
        return copyFloat32Range(array_1, 0, lineDistanceAttr.count);
    }
    var drawRange = geometry.drawRange;
    var total = lineDistanceAttr.count;
    var vertexStart = Math.max(0, Math.min(Math.floor(drawRange.start), total));
    var available = Math.max(0, total - vertexStart);
    var drawCount = !Number.isFinite(drawRange.count) || drawRange.count <= 0
        ? available
        : Math.min(Math.floor(drawRange.count), available);
    if (drawCount <= 0) {
        return undefined;
    }
    var array = lineDistanceAttr.array;
    return copyFloat32Range(array, vertexStart, drawCount);
}
/**
 * Copies a per-vertex buffer attribute honoring the geometry draw range.
 */
export function exportVertexAttributeSlice(geometry, attributeName) {
    var attribute = geometry.getAttribute(attributeName);
    if (!attribute || attribute.count === 0) {
        return undefined;
    }
    var itemSize = attribute.itemSize;
    var indexAttr = geometry.getIndex();
    if (indexAttr) {
        var array_2 = attribute.array;
        return copyFloat32Range(array_2, 0, attribute.count * itemSize);
    }
    var drawRange = geometry.drawRange;
    var total = attribute.count;
    var vertexStart = Math.max(0, Math.min(Math.floor(drawRange.start), total));
    var available = Math.max(0, total - vertexStart);
    var drawCount = !Number.isFinite(drawRange.count) || drawRange.count <= 0
        ? available
        : Math.min(Math.floor(drawRange.count), available);
    if (drawCount <= 0) {
        return undefined;
    }
    var array = attribute.array;
    return copyFloat32Range(array, vertexStart * itemSize, drawCount * itemSize);
}
/**
 * Creates a viewer material for one exported line batch.
 */
export function createViewerLineMaterial(batch, resolution) {
    if (batch.lineWidth != null && batch.lineWidth > 0) {
        var material = new LineMaterial({
            color: batch.color,
            linewidth: batch.lineWidth
        });
        if (resolution) {
            material.resolution.copy(resolution);
        }
        return material;
    }
    if (batch.linePattern) {
        return AcTrLinePatternShaders.createLineShaderMaterialFromScaledPattern(batch.linePattern.pattern, batch.linePattern.patternLength, batch.color, batch.linePattern.viewportScale, acexCameraZoomUniform);
    }
    return new THREE.LineBasicMaterial({ color: batch.color });
}
/**
 * Creates a viewer material for one exported point batch.
 */
export function createViewerPointsMaterial(batch) {
    return new THREE.PointsMaterial({
        color: batch.color,
        size: 1,
        sizeAttenuation: false
    });
}
/**
 * Re-aligns hatch pattern coordinates after hatch meshes were world-baked
 * during batch-group cloning (pattern fills stay on the unbatched draw path).
 */
export function transformHatchPatternToWorldSpace(pattern, matrixElements) {
    var rotation = Math.atan2(matrixElements[1], matrixElements[0]);
    var transformPoint = function (x, y) { return [
        matrixElements[0] * x + matrixElements[4] * y + matrixElements[12],
        matrixElements[1] * x + matrixElements[5] * y + matrixElements[13]
    ]; };
    var transformVector = function (x, y) { return [
        matrixElements[0] * x + matrixElements[4] * y,
        matrixElements[1] * x + matrixElements[5] * y
    ]; };
    return {
        patternAngle: pattern.patternAngle + rotation,
        patternLines: pattern.patternLines.map(function (line) { return ({
            angle: line.angle + rotation,
            base: transformPoint(line.base[0], line.base[1]),
            offset: transformVector(line.offset[0], line.offset[1]),
            dashLengths: __spreadArray([], __read(line.dashLengths), false),
            patternLength: line.patternLength
        }); })
    };
}
/**
 * Creates a viewer material for one exported mesh batch.
 */
export function createViewerMeshMaterial(batch) {
    var _a, _b;
    if (batch.hatchPattern && batch.hatchPattern.patternLines.length > 0) {
        try {
            var patternLines = batch.hatchPattern.patternLines.map(deserializeHatchPatternLine);
            return createHatchPatternShaderMaterial(patternLines, batch.hatchPattern.patternAngle, acexCameraZoomUniform, new THREE.Color(batch.color), 0, ((_a = batch.side) !== null && _a !== void 0 ? _a : THREE.FrontSide));
        }
        catch (_c) {
            // Fall back to a solid fill if pattern shader creation fails.
        }
    }
    if (batch.gradientFill &&
        batch.gradientPositions &&
        batch.gradientPositions.length >= 2) {
        try {
            return createGradientHatchShaderMaterialFromUniforms(batch.gradientFill, ((_b = batch.side) !== null && _b !== void 0 ? _b : THREE.FrontSide));
        }
        catch (_d) {
            // Fall back to a solid fill if gradient shader creation fails.
        }
    }
    return new THREE.MeshBasicMaterial({
        color: batch.color,
        side: THREE.DoubleSide
    });
}
//# sourceMappingURL=AcExPatternSnapshot.js.map