/**
 * Checks if the given value is `null` or `undefined`.
 *
 * @example
 * ```ts
 * isNil(1); // false
 * isNil('1'); // false
 * isNil(undefined); // true
 * isNil(null); // true
 * ```
 */
declare const isNil: <T>(input: T) => input is T & (null | undefined);
export default isNil;
//# sourceMappingURL=isNil.d.ts.map