import type IterableInfer from "./types/IterableInfer";
import type ReturnValueType from "./types/ReturnValueType";
/**
 * Returns a new array, sorted according to the comparator `f`, which should accept two values.
 * Unlike `sort`, this function does not mutate the original array.
 *
 * @example
 * ```ts
 * const arr = [3, 4, 1, 2, 5, 2];
 * toSorted((a, b) => a > b, arr); // [1, 2, 2, 3, 4, 5]
 * arr; // [3, 4, 1, 2, 5, 2] - original array is unchanged
 *
 * toSorted((a, b) => a.localeCompare(b), "bcdaef"); // ["a", "b", "c", "d", "e", "f"]
 *
 * // Can be used in a pipeline
 * pipe(
 *   [3, 4, 1, 2, 5, 2],
 *   filter((a) => a % 2 !== 0),
 *   toSorted((a, b) => a > b),
 * ); // [1, 3, 5]
 * ```
 */
declare function toSorted(f: (a: any, b: any) => unknown, iterable: readonly []): any[];
declare function toSorted<T>(f: (a: T, b: T) => unknown, iterable: Iterable<T>): T[];
declare function toSorted<T>(f: (a: T, b: T) => unknown, iterable: AsyncIterable<T>): Promise<T[]>;
declare function toSorted<T extends Iterable<unknown> | AsyncIterable<unknown>>(f: (a: IterableInfer<T>, b: IterableInfer<T>) => unknown): (iterable: T) => ReturnValueType<T, IterableInfer<T>[]>;
export default toSorted;
//# sourceMappingURL=toSorted.d.ts.map