/**
 * Returns true if `n` is a Number.
 *
 * @example
 * ```ts
 * isNumber(2); // true
 * isNumber("a"); // false
 * ```
 */
declare const isNumber: <T>(input: T) => input is T & number;
export default isNumber;
//# sourceMappingURL=isNumber.d.ts.map