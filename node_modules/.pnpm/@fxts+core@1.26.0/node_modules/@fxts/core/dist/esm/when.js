import isUndefined from "./isUndefined.js";
function when(predicate, callback, a) {
    if (isUndefined(a)) {
        return (b) => when(predicate, callback, b);
    }
    if (predicate(a)) {
        return callback(a);
    }
    return a;
}
export default when;
//# sourceMappingURL=when.js.map