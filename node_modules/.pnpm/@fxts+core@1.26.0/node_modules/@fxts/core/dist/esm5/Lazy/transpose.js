import { __awaiter, __generator, __read, __spreadArray } from "tslib";
import { isAsyncIterable, isIterable, toIterator } from "../_internal/utils.js";
import isUndefined from "../isUndefined.js";
import max from "../max.js";
import nth from "../nth.js";
import pipe from "../pipe.js";
import size from "../size.js";
import toArray from "../toArray.js";
import filter from "./filter.js";
import map from "./map.js";
import toAsync from "./toAsync.js";
function sync(iterable) {
    var maxLen, index;
    return __generator(this, function (_a) {
        switch (_a.label) {
            case 0:
                maxLen = pipe(iterable, map(size), max);
                index = 0;
                _a.label = 1;
            case 1:
                if (!(index < maxLen)) return [3 /*break*/, 4];
                return [4 /*yield*/, pipe(iterable, map(nth(index)), filter(function (value) { return !isUndefined(value); }), toArray)];
            case 2:
                _a.sent();
                _a.label = 3;
            case 3:
                index++;
                return [3 /*break*/, 1];
            case 4: return [2 /*return*/];
        }
    });
}
function async(iterable) {
    var _a;
    var iterators = toArray(map(toIterator, iterable));
    return _a = {},
        _a[Symbol.asyncIterator] = function () {
            return this;
        },
        _a.next = function (_concurrent) {
            return __awaiter(this, void 0, void 0, function () {
                var headIterators, done;
                return __generator(this, function (_a) {
                    switch (_a.label) {
                        case 0: return [4 /*yield*/, pipe(toAsync(iterators), map(function (iterator) { return iterator.next(_concurrent); }), toArray)];
                        case 1:
                            headIterators = _a.sent();
                            done = headIterators.every(function (_a) {
                                var done = _a.done;
                                return done;
                            });
                            if (done)
                                return [2 /*return*/, { done: true, value: undefined }];
                            return [2 /*return*/, {
                                    done: false,
                                    value: pipe(headIterators, map(function (_a) {
                                        var value = _a.value;
                                        return value;
                                    }), filter(function (value) { return !isUndefined(value); }), toArray),
                                }];
                    }
                });
            });
        },
        _a;
}
function transpose() {
    var iterables = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        iterables[_i] = arguments[_i];
    }
    if (size(iterables) < 2)
        return function () {
            var args = [];
            for (var _i = 0; _i < arguments.length; _i++) {
                args[_i] = arguments[_i];
            }
            return transpose.apply(void 0, __spreadArray(__spreadArray([], __read(iterables), false), __read(args), false));
        };
    if (iterables.some(function (value) { return !isIterable(value) && !isAsyncIterable(value); })) {
        throw new TypeError("'iterable' must be type of Iterable or AsyncIterable");
    }
    var hasAsyncIterable = iterables.some(function (iterable) {
        return isAsyncIterable(iterable);
    });
    if (hasAsyncIterable) {
        return async(iterables);
    }
    return sync(iterables);
}
export default transpose;
//# sourceMappingURL=transpose.js.map