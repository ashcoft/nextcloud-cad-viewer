import { throwIfPromiseError } from "../_internal/error.js";
import { isAsyncIterable, isIterable } from "../_internal/utils.js";
import flat from "./flat.js";
import map from "./map.js";
function flatMap(f, iterable) {
    if (iterable === undefined) {
        return function (iterable) {
            return flatMap(f, iterable);
        };
    }
    if (isIterable(iterable)) {
        return flat(map(function (a) { return throwIfPromiseError(f(a)); }, iterable));
    }
    if (isAsyncIterable(iterable)) {
        return flat(map(f, iterable));
    }
    throw new TypeError("'iterable' must be type of Iterable or AsyncIterable");
}
export default flatMap;
//# sourceMappingURL=flatMap.js.map