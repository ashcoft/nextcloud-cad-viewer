"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var tslib_1 = require("tslib");
var utils_1 = require("../_internal/utils");
var linkedList_1 = require("../dataStructure/linkedList/linkedList");
var isNil_1 = tslib_1.__importDefault(require("../isNil"));
var forkMap = new WeakMap();
var DONE_RESULT = Object.freeze({ done: true, value: undefined });
function sync(iterable) {
    var _a;
    var iterator = iterable[Symbol.iterator]();
    var getNext = function (forkItem) {
        var current = forkItem.queue.getLastNode();
        // Register this fork
        forkItem.forks.add(current);
        var done = function () {
            iterator.next = forkItem.next;
            // Unregister this fork when done
            forkItem.forks.delete(current);
            return DONE_RESULT;
        };
        var isDone = false;
        var next = function () {
            var _a;
            if (forkItem.error) {
                throw forkItem.error;
            }
            if (isDone) {
                return done();
            }
            var item = current === null || current === void 0 ? void 0 : current.getNext();
            if ((0, isNil_1.default)(item) || item === forkItem.queue.getTail()) {
                try {
                    var node = forkItem.next();
                    current = forkItem.queue.insertLast(node);
                    isDone = (_a = node.done) !== null && _a !== void 0 ? _a : true;
                    if (isDone) {
                        return done();
                    }
                    return node;
                }
                catch (error) {
                    forkItem.error = error;
                    throw error;
                }
            }
            current = item;
            // Update this fork's position
            var prevNode = current.getPrev();
            if (prevNode) {
                forkItem.forks.delete(prevNode);
            }
            forkItem.forks.add(current);
            return current.getValue();
        };
        return next;
    };
    var forkItem = forkMap.get(iterator);
    if (!forkItem) {
        var originNext = iterator.next.bind(iterator);
        forkItem = {
            queue: new linkedList_1.LinkedList(),
            next: originNext,
            error: undefined,
            forks: new Set(),
        };
        iterator.next = getNext(forkItem);
        forkMap.set(iterator, forkItem);
    }
    var next = getNext(forkItem);
    return _a = {},
        _a[Symbol.iterator] = function () {
            return this;
        },
        _a.next = next,
        _a;
}
var forkAsyncMap = new WeakMap();
function async(iterable) {
    var _a;
    var _this = this;
    var iterator = iterable[Symbol.asyncIterator]();
    var getNext = function (forkItem) {
        var _a;
        var settlementQueue = [];
        var currentNode = (_a = forkItem.queue.getLastNode()) !== null && _a !== void 0 ? _a : null;
        var nextCallCount = 0;
        var resolvedCount = 0;
        var prevItem = Promise.resolve();
        // Register this fork
        forkItem.forks.add(currentNode);
        var fillBuffer = function (concurrent) {
            var nextItem = forkItem.next(concurrent);
            prevItem = prevItem
                .then(function () { return nextItem; })
                .then(function (_a) {
                var done = _a.done, value = _a.value;
                if (done) {
                    while (settlementQueue.length > 0) {
                        // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
                        var _b = tslib_1.__read(settlementQueue.shift(), 1), resolve = _b[0];
                        resolve(DONE_RESULT);
                    }
                    // Unregister this fork when done
                    forkItem.forks.delete(currentNode);
                    forkItem.done = true;
                    return;
                }
                forkItem.queue.insertLast({ done: false, value: value });
                recur(concurrent);
            })
                .catch(function (reason) {
                forkItem.done = true;
                forkItem.error = reason;
                // Unregister this fork on error
                forkItem.forks.delete(currentNode);
                while (settlementQueue.length > 0) {
                    // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
                    var _a = tslib_1.__read(settlementQueue.shift(), 2), reject = _a[1];
                    reject(reason);
                }
            });
        };
        function consumeBuffer() {
            while (forkItem.queue.hasNext(currentNode) &&
                nextCallCount > resolvedCount) {
                // Update this fork's position
                var prevNode = currentNode;
                // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
                var result = currentNode.getNext().getValue();
                currentNode = currentNode.getNext();
                if (prevNode !== null) {
                    forkItem.forks.delete(prevNode);
                }
                forkItem.forks.add(currentNode);
                // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
                var _a = tslib_1.__read(settlementQueue.shift(), 1), resolve = _a[0];
                resolve(result);
                resolvedCount++;
            }
        }
        function recur(concurrent) {
            if (nextCallCount === resolvedCount) {
                return;
            }
            else if (forkItem.queue.hasNext(currentNode)) {
                consumeBuffer();
            }
            else {
                fillBuffer(concurrent);
            }
        }
        var next = function (concurrent) { return tslib_1.__awaiter(_this, void 0, void 0, function () {
            return tslib_1.__generator(this, function (_a) {
                if (forkItem.error) {
                    throw forkItem.error;
                }
                if (forkItem.done && !forkItem.queue.hasNext(currentNode)) {
                    // Unregister this fork when done
                    forkItem.forks.delete(currentNode);
                    return [2 /*return*/, DONE_RESULT];
                }
                nextCallCount++;
                return [2 /*return*/, new Promise(function (resolve, reject) {
                        settlementQueue.push([resolve, reject]);
                        recur(concurrent);
                    })];
            });
        }); };
        return next;
    };
    var forkItem = forkAsyncMap.get(iterator);
    if (!forkItem) {
        var originNext = iterator.next.bind(iterator);
        forkItem = {
            queue: new linkedList_1.LinkedList(),
            next: originNext,
            done: false,
            error: undefined,
            forks: new Set(),
        };
        iterator.next = getNext(forkItem);
        forkAsyncMap.set(iterator, forkItem);
    }
    var next = getNext(forkItem);
    return _a = {},
        _a[Symbol.asyncIterator] = function () {
            return this;
        },
        _a.next = next,
        _a;
}
/**
 * Returns an iterable of forks of original source. Each fork contains the same values as source, and can be consumed independently.
 *
 * @experimental
 * @example
 * ```ts
 * const arr = [1, 2, 3];
 * const iter1 = fork(arr);
 * const iter2 = fork(arr);
 *
 * iter1.next() // {done:false, value: 1}
 * iter1.next() // {done:false, value: 2}
 * iter2.next() // {done:false, value: 1}
 * iter2.next() // {done:false, value: 2}
 *
 * const str = 'abc'
 * const strIter1 = fork(str);
 * const strIter2 = fork(str);
 *
 * strIter1.next() // {done:false, value: 'a'}
 * strIter1.next() // {done:false, value: 'b'}
 * strIter2.next() // {done:false, value: 'a'}
 * strIter2.next() // {done:false, value: 'b'}
 *
 * // with pipe
 * const arrAdd10 = pipe(
 *   [1, 2, 3],
 *   map((a) => a + 10),
 * );
 *
 * const arrAdd10Iter1 = fork(arrAdd10);
 * const arrAdd10Iter2 = fork(arrAdd10);
 * arrAdd10Iter1.next() // { value: 11, done: false }
 * arrAdd10Iter2.next() // { value: 11, done: false }
 *
 * const arrAdd10Iter3 = fork(arrAdd10Iter1);
 * arrAdd10Iter1.next() // { value: 12, done: false }
 * arrAdd10Iter1.next() // { value: 13, done: false }
 * arrAdd10Iter2.next() // { value: 12, done: false }
 * arrAdd10Iter3.next() // { value: 12, done: false }
 * ```
 */
// prettier-ignore
function fork(iterable) {
    if ((0, utils_1.isIterable)(iterable)) {
        return sync(iterable);
    }
    if ((0, utils_1.isAsyncIterable)(iterable)) {
        return async(iterable);
    }
    throw new TypeError("'iterable' must be type of Iterable or AsyncIterable");
}
exports.default = fork;
//# sourceMappingURL=fork.js.map