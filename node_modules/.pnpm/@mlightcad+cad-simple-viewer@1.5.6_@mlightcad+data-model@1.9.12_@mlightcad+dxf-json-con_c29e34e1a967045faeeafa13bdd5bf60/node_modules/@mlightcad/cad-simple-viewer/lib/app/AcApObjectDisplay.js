var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
/**
 * Returns true when the view supports session-only object visibility changes.
 */
function isSceneVisibilityView(view) {
    return (typeof view.hasEntity === 'function' &&
        typeof view.setEntitySceneVisible === 'function');
}
/**
 * Applies the session hidden state for one object when it enters the scene.
 */
export function applySessionHiddenObjectState(doc, view, objectId) {
    if (!doc.isObjectHidden(objectId) || !isSceneVisibilityView(view)) {
        return;
    }
    view.setEntitySceneVisible(objectId, false);
}
/**
 * Temporarily suppresses display of the specified objects in the current view.
 *
 * Hidden objects remain in the drawing database and reappear after
 * {@link unisolateObjects} or when the document session ends.
 */
export function hideObjects(context, objectIds) {
    var e_1, _a;
    var view = context.view;
    if (!isSceneVisibilityView(view)) {
        return 0;
    }
    var doc = context.doc;
    var db = doc.database;
    var count = 0;
    try {
        for (var objectIds_1 = __values(objectIds), objectIds_1_1 = objectIds_1.next(); !objectIds_1_1.done; objectIds_1_1 = objectIds_1.next()) {
            var objectId = objectIds_1_1.value;
            if (doc.isObjectHidden(objectId)) {
                continue;
            }
            var entity = db.tables.blockTable.getEntityById(objectId);
            if (!(entity === null || entity === void 0 ? void 0 : entity.visibility) || !view.hasEntity(objectId)) {
                continue;
            }
            if (view.setEntitySceneVisible(objectId, false)) {
                doc.addHiddenObject(objectId);
                count++;
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (objectIds_1_1 && !objectIds_1_1.done && (_a = objectIds_1.return)) _a.call(objectIds_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    if (count > 0) {
        view.selectionSet.clear();
    }
    return count;
}
/**
 * Redisplay all objects temporarily hidden by {@link hideObjects}.
 */
export function unisolateObjects(context) {
    var e_2, _a;
    var view = context.view;
    if (!isSceneVisibilityView(view)) {
        return 0;
    }
    var doc = context.doc;
    var db = doc.database;
    var hiddenIds = doc.takeHiddenObjects();
    var count = 0;
    try {
        for (var hiddenIds_1 = __values(hiddenIds), hiddenIds_1_1 = hiddenIds_1.next(); !hiddenIds_1_1.done; hiddenIds_1_1 = hiddenIds_1.next()) {
            var objectId = hiddenIds_1_1.value;
            var entity = db.tables.blockTable.getEntityById(objectId);
            if (!entity || !view.hasEntity(objectId)) {
                continue;
            }
            if (view.setEntitySceneVisible(objectId, entity.visibility)) {
                count++;
            }
        }
    }
    catch (e_2_1) { e_2 = { error: e_2_1 }; }
    finally {
        try {
            if (hiddenIds_1_1 && !hiddenIds_1_1.done && (_a = hiddenIds_1.return)) _a.call(hiddenIds_1);
        }
        finally { if (e_2) throw e_2.error; }
    }
    view.selectionSet.clear();
    return count;
}
//# sourceMappingURL=AcApObjectDisplay.js.map