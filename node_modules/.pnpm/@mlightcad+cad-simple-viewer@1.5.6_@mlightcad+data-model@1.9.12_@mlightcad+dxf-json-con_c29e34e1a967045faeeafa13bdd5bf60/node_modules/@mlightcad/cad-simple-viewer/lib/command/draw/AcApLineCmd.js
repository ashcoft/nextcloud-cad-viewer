var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import { AcDbLine, AcGePoint3d } from '@mlightcad/data-model';
import { AcApDocManager } from '../../app';
import { AcEdCommand, AcEdOpenMode, AcEdPreviewJig, AcEdPromptPointOptions, AcEdPromptStateMachine, AcEdPromptStatus } from '../../editor';
import { AcApI18n } from '../../i18n';
/**
 * Dynamic preview jig for LINE command.
 */
var AcApLineJig = /** @class */ (function (_super) {
    __extends(AcApLineJig, _super);
    /**
     * Creates a line jig.
     *
     * @param view - The associated view
     */
    function AcApLineJig(view, start) {
        var _this = _super.call(this, view) || this;
        _this._line = new AcDbLine(start, start);
        return _this;
    }
    Object.defineProperty(AcApLineJig.prototype, "entity", {
        /**
         * Gets transient line entity used by this jig.
         */
        get: function () {
            return this._line;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Updates preview line endpoint from current cursor point.
     *
     * @param point - Current cursor/input point.
     */
    AcApLineJig.prototype.update = function (point) {
        this._line.endPoint = point;
    };
    return AcApLineJig;
}(AcEdPreviewJig));
export { AcApLineJig };
/**
 * Command to create chained line segments.
 *
 * Behavior is aligned with AutoCAD LINE:
 * - After the first point, each confirmed next point immediately creates one segment.
 * - `Undo` removes the last created segment.
 * - `Close` creates the closing segment to the first point and ends command.
 */
var AcApLineCmd = /** @class */ (function (_super) {
    __extends(AcApLineCmd, _super);
    /**
     * Creates LINE command instance.
     */
    function AcApLineCmd() {
        var _this = _super.call(this) || this;
        _this.mode = AcEdOpenMode.Write;
        return _this;
    }
    /**
     * Command entry point.
     *
     * Supports chained point input with `Undo` and `Close` options.
     *
     * @param context - Current application/document context.
     */
    AcApLineCmd.prototype.execute = function (context) {
        return __awaiter(this, void 0, void 0, function () {
            var db, points, createdSegments, hasContinuePoint, getContinuePoint, syncLastEndpoint, appendPoint, getCurrentPoint, appendSegment, undoLastSegment, closeSegment, startPointPrompt, startPointResult, NextPointState, machine;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        db = context.doc.database;
                        points = [];
                        createdSegments = [];
                        hasContinuePoint = function () { return !!AcApLineCmd._lastEndpoint; };
                        getContinuePoint = function () {
                            return AcApLineCmd._lastEndpoint
                                ? new AcGePoint3d(AcApLineCmd._lastEndpoint)
                                : undefined;
                        };
                        syncLastEndpoint = function () {
                            var current = getCurrentPoint();
                            if (current) {
                                AcApLineCmd._lastEndpoint = new AcGePoint3d(current);
                            }
                        };
                        appendPoint = function (point) {
                            points.push(new AcGePoint3d(point));
                        };
                        getCurrentPoint = function () { return points[points.length - 1]; };
                        appendSegment = function (endPoint) {
                            var startPoint = getCurrentPoint();
                            if (!startPoint)
                                return;
                            var segment = new AcDbLine(new AcGePoint3d(startPoint), endPoint);
                            db.tables.blockTable.modelSpace.appendEntity(segment);
                            createdSegments.push(segment);
                            appendPoint(endPoint);
                        };
                        undoLastSegment = function () {
                            var _a;
                            if (createdSegments.length <= 0 || points.length <= 1)
                                return;
                            (_a = createdSegments.pop()) === null || _a === void 0 ? void 0 : _a.erase();
                            points.pop();
                        };
                        closeSegment = function () {
                            if (points.length <= 1)
                                return;
                            var first = points[0];
                            var current = getCurrentPoint();
                            if (!current)
                                return;
                            var isSamePoint = first.x === current.x && first.y === current.y && first.z === current.z;
                            if (!isSamePoint) {
                                appendSegment(first);
                            }
                        };
                        startPointPrompt = new AcEdPromptPointOptions(hasContinuePoint()
                            ? AcApI18n.t('jig.line.firstPointOrContinue')
                            : AcApI18n.t('jig.line.firstPoint'));
                        if (hasContinuePoint()) {
                            startPointPrompt.keywords.add(AcApI18n.t('jig.line.keywords.continue.display'), AcApI18n.t('jig.line.keywords.continue.global'), AcApI18n.t('jig.line.keywords.continue.local'));
                        }
                        return [4 /*yield*/, AcApDocManager.instance.editor.getPoint(startPointPrompt)];
                    case 1:
                        startPointResult = _a.sent();
                        if (startPointResult.status === AcEdPromptStatus.OK) {
                            appendPoint(startPointResult.value);
                        }
                        else if (startPointResult.status === AcEdPromptStatus.Keyword &&
                            startPointResult.stringResult === 'Continue' &&
                            hasContinuePoint()) {
                            appendPoint(getContinuePoint());
                        }
                        else {
                            return [2 /*return*/];
                        }
                        NextPointState = /** @class */ (function () {
                            /**
                             * State-machine node that prompts for subsequent line points.
                             */
                            function NextPointState() {
                            }
                            /**
                             * Builds point prompt for the next vertex with dynamic line preview.
                             *
                             * @returns Configured point prompt with runtime keywords.
                             */
                            NextPointState.prototype.buildPrompt = function () {
                                var prompt = new AcEdPromptPointOptions(AcApI18n.t('jig.line.nextPointWithOptions'));
                                prompt.keywords.add(AcApI18n.t('jig.line.keywords.undo.display'), AcApI18n.t('jig.line.keywords.undo.global'), AcApI18n.t('jig.line.keywords.undo.local'));
                                if (points.length > 1) {
                                    prompt.keywords.add(AcApI18n.t('jig.line.keywords.close.display'), AcApI18n.t('jig.line.keywords.close.global'), AcApI18n.t('jig.line.keywords.close.local'));
                                }
                                var basePoint = getCurrentPoint();
                                if (basePoint) {
                                    prompt.useDashedLine = true;
                                    prompt.useBasePoint = true;
                                    prompt.basePoint = new AcGePoint3d(basePoint);
                                    prompt.jig = new AcApLineJig(context.view, basePoint);
                                }
                                return prompt;
                            };
                            /**
                             * Handles point/keyword input and controls loop continuation.
                             *
                             * @param result - Point prompt result for this step.
                             * @returns State-machine step result.
                             */
                            NextPointState.prototype.handleResult = function (result) {
                                return __awaiter(this, void 0, void 0, function () {
                                    var keyword;
                                    var _a;
                                    return __generator(this, function (_b) {
                                        if (result.status === AcEdPromptStatus.OK) {
                                            appendSegment(result.value);
                                            return [2 /*return*/, 'continue'];
                                        }
                                        if (result.status === AcEdPromptStatus.Keyword) {
                                            keyword = (_a = result.stringResult) !== null && _a !== void 0 ? _a : '';
                                            if (keyword === 'Undo') {
                                                undoLastSegment();
                                                return [2 /*return*/, 'continue'];
                                            }
                                            if (keyword === 'Close' && points.length > 1) {
                                                closeSegment();
                                                return [2 /*return*/, 'finish'];
                                            }
                                            return [2 /*return*/, 'continue'];
                                        }
                                        return [2 /*return*/, 'finish'];
                                    });
                                });
                            };
                            return NextPointState;
                        }());
                        machine = new AcEdPromptStateMachine();
                        machine.setState(new NextPointState());
                        return [4 /*yield*/, machine.run(function (prompt) { return AcApDocManager.instance.editor.getPoint(prompt); })];
                    case 2:
                        _a.sent();
                        syncLastEndpoint();
                        return [2 /*return*/];
                }
            });
        });
    };
    return AcApLineCmd;
}(AcEdCommand));
export { AcApLineCmd };
//# sourceMappingURL=AcApLineCmd.js.map