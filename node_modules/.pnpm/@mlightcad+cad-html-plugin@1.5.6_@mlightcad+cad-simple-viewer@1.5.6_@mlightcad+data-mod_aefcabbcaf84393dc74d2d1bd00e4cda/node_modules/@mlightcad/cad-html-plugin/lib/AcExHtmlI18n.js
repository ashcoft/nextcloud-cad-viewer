var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
/** All locales accepted by the offline viewer, in display order. */
export var ACEX_HTML_LOCALES = ['en', 'zh'];
/**
 * `localStorage` key used to persist the user's language choice
 * across reloads of exported HTML files.
 */
export var ACEX_HTML_LOCALE_STORAGE_KEY = 'mlcad-html-locale';
var MESSAGES = {
    en: {
        toolbar: {
            viewerTools: 'Viewer tools',
            zoomExtents: 'Zoom extents',
            measureDistance: 'Measure distance',
            measureAngle: 'Measure angle',
            measureArc: 'Measure arc length',
            measureArea: 'Measure area',
            measureCoordinate: 'Measure coordinates',
            clearMeasurements: 'Clear measurements',
            settings: 'Measure settings',
            layers: 'Layers',
            language: 'Language',
            languageSwitch: 'Switch to Chinese',
            collapse: 'Collapse toolbar',
            expand: 'Expand toolbar'
        },
        settings: {
            toolbar: 'Measure settings',
            measureColor: 'Measure color',
            ortho: 'Toggle orthogonal mode',
            polar: 'Polar tracking angles',
            polarAngles: 'Polar tracking angles'
        },
        layers: {
            title: 'Layers',
            close: 'Close layers',
            showAll: 'Show all',
            hideAll: 'Hide all',
            zoomTo: 'Zoom to {name}'
        },
        status: {
            ready: 'Ready',
            measureDistanceHint: 'Click two points to measure distance (object snap enabled).',
            measureAngleHint: 'Click vertex, then two points on each arm (object snap enabled).',
            measureArcHint: 'Click arc start, a point on the arc, then arc end (object snap enabled).',
            measureAreaHint: 'Click polygon vertices; click near the first point or press Enter to finish.',
            measureCoordinateHint: 'Click a point to read its X/Y coordinates (object snap enabled).',
            distance: 'Distance: {value}',
            coordinates: 'X: {x}  Y: {y}',
            angle: 'Angle: {value}',
            arcLength: 'Arc length: {value}',
            area: 'Area: {value}',
            lengthTotal: 'Length total: {value}',
            areaTotal: 'Area total: {value}',
            zoomLayer: 'Zoom: {name}',
            loadFailed: 'Failed to load drawing: {error}',
            noLayout: 'No layout data in snapshot.'
        }
    },
    zh: {
        toolbar: {
            viewerTools: '查看器工具',
            zoomExtents: '范围缩放',
            measureDistance: '测量距离',
            measureAngle: '测量角度',
            measureArc: '测量弧长',
            measureArea: '测量面积',
            measureCoordinate: '测量坐标',
            clearMeasurements: '清除测量',
            settings: '测量设置',
            layers: '图层',
            language: '语言',
            languageSwitch: '切换到 English',
            collapse: '收起工具栏',
            expand: '展开工具栏'
        },
        settings: {
            toolbar: '测量设置',
            measureColor: '测量颜色',
            ortho: '切换正交模式',
            polar: '极轴追踪角度',
            polarAngles: '极轴追踪角度'
        },
        layers: {
            title: '图层',
            close: '关闭图层',
            showAll: '全部显示',
            hideAll: '全部隐藏',
            zoomTo: '缩放到 {name}'
        },
        status: {
            ready: '就绪',
            measureDistanceHint: '点击两点以测量距离（已启用对象捕捉）。',
            measureAngleHint: '依次点击顶点与两条边上的点（已启用对象捕捉）。',
            measureArcHint: '依次点击弧起点、弧上一点与弧端点（已启用对象捕捉）。',
            measureAreaHint: '依次点击多边形顶点；靠近首点或按 Enter 完成。',
            measureCoordinateHint: '点击一点以读取其 X/Y 坐标（已启用对象捕捉）。',
            distance: '距离：{value}',
            coordinates: 'X：{x}  Y：{y}',
            angle: '角度：{value}',
            arcLength: '弧长：{value}',
            area: '面积：{value}',
            lengthTotal: '长度合计：{value}',
            areaTotal: '面积合计：{value}',
            zoomLayer: '缩放：{name}',
            loadFailed: '无法加载图纸：{error}',
            noLayout: '快照中没有布局数据。'
        }
    }
};
/**
 * Type guard for {@link AcExHtmlLocale}.
 *
 * @param value - Arbitrary string to test.
 * @returns `true` when `value` is `'en'` or `'zh'`.
 */
export function isAcExHtmlLocale(value) {
    return value === 'en' || value === 'zh';
}
/**
 * Normalizes a BCP 47 or short locale tag to a supported {@link AcExHtmlLocale}.
 *
 * @param value - Locale string from snapshot meta, `<html lang>`, or `navigator.language`.
 * @returns `'en'`, `'zh'`, or `null` when unrecognized.
 */
export function resolveAcExHtmlLocale(value) {
    if (value == null || value === '')
        return null;
    var normalized = value.toLowerCase().replace('_', '-');
    if (normalized === 'en' || normalized.startsWith('en-'))
        return 'en';
    if (normalized === 'zh' || normalized.startsWith('zh'))
        return 'zh';
    return null;
}
/**
 * Detects locale from the browser's language preferences.
 * Chinese (`zh*`) maps to `'zh'`; all other languages default to `'en'`.
 *
 * @returns `'zh'` when a preferred browser language is Chinese, otherwise `'en'`.
 */
export function detectBrowserAcExHtmlLocale() {
    var e_1, _a;
    var _b;
    if (typeof navigator === 'undefined')
        return 'en';
    var candidates = __spreadArray(__spreadArray([], __read(((_b = navigator.languages) !== null && _b !== void 0 ? _b : [])), false), [
        navigator.language
    ], false).filter(Boolean);
    try {
        for (var candidates_1 = __values(candidates), candidates_1_1 = candidates_1.next(); !candidates_1_1.done; candidates_1_1 = candidates_1.next()) {
            var candidate = candidates_1_1.value;
            var resolved = resolveAcExHtmlLocale(candidate);
            if (resolved === 'zh')
                return 'zh';
            if (resolved === 'en')
                return 'en';
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (candidates_1_1 && !candidates_1_1.done && (_a = candidates_1.return)) _a.call(candidates_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return 'en';
}
/**
 * Chooses the initial locale for the offline viewer using, in order:
 * persisted user choice in `localStorage`, then {@link detectBrowserAcExHtmlLocale}.
 *
 * @returns Resolved locale, defaulting to `'en'`.
 */
export function detectAcExHtmlLocale() {
    if (typeof localStorage !== 'undefined') {
        try {
            var stored = localStorage.getItem(ACEX_HTML_LOCALE_STORAGE_KEY);
            var fromStorage = resolveAcExHtmlLocale(stored);
            if (fromStorage)
                return fromStorage;
        }
        catch (_a) {
            /* private mode */
        }
    }
    return detectBrowserAcExHtmlLocale();
}
function lookupMessage(tree, key) {
    var e_2, _a;
    var parts = key.split('.');
    var node = tree;
    try {
        for (var parts_1 = __values(parts), parts_1_1 = parts_1.next(); !parts_1_1.done; parts_1_1 = parts_1.next()) {
            var part = parts_1_1.value;
            if (node == null || typeof node === 'string')
                return undefined;
            node = node[part];
        }
    }
    catch (e_2_1) { e_2 = { error: e_2_1 }; }
    finally {
        try {
            if (parts_1_1 && !parts_1_1.done && (_a = parts_1.return)) _a.call(parts_1);
        }
        finally { if (e_2) throw e_2.error; }
    }
    return typeof node === 'string' ? node : undefined;
}
/**
 * Replaces `{name}` placeholders in a message template.
 *
 * @param template - Localized string possibly containing `{param}` tokens.
 * @param params - Values substituted by token name; missing keys are left unchanged.
 * @returns Interpolated user-visible string.
 */
export function formatAcExHtmlMessage(template, params) {
    if (!params)
        return template;
    return template.replace(/\{(\w+)\}/g, function (_, name) {
        var value = params[name];
        return value != null ? String(value) : "{".concat(name, "}");
    });
}
/**
 * Lightweight i18n helper for the exported HTML viewer shell.
 * Updates elements marked with `data-i18n-text` / `data-i18n-attr` and persists locale choice.
 */
var AcExHtmlI18n = /** @class */ (function () {
    /**
     * @param initialLocale - Starting locale; defaults to {@link detectAcExHtmlLocale} when omitted.
     */
    function AcExHtmlI18n(initialLocale) {
        this._onChange = null;
        this._locale = initialLocale !== null && initialLocale !== void 0 ? initialLocale : detectAcExHtmlLocale();
    }
    Object.defineProperty(AcExHtmlI18n.prototype, "locale", {
        /** Active locale used for {@link AcExHtmlI18n.t}. */
        get: function () {
            return this._locale;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcExHtmlI18n.prototype, "localeBadge", {
        /** Short badge text shown on the language toolbar button (`EN` or `中`). */
        get: function () {
            return this._locale === 'zh' ? '中' : 'EN';
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Registers a callback invoked after {@link AcExHtmlI18n.setLocale} or
     * {@link AcExHtmlI18n.toggleLocale} updates the UI.
     *
     * @param handler - Listener, or `null` to clear.
     */
    AcExHtmlI18n.prototype.setOnChange = function (handler) {
        this._onChange = handler;
    };
    /**
     * Resolves and formats a message for the active locale, falling back to English.
     *
     * @param key - Dot-separated message key.
     * @param params - Optional placeholder values for `{name}` tokens.
     */
    AcExHtmlI18n.prototype.t = function (key, params) {
        var _a, _b;
        var template = (_b = (_a = lookupMessage(MESSAGES[this._locale], key)) !== null && _a !== void 0 ? _a : lookupMessage(MESSAGES.en, key)) !== null && _b !== void 0 ? _b : key;
        return formatAcExHtmlMessage(template, params);
    };
    /**
     * Switches between English and Chinese, persists the choice, and refreshes the DOM.
     *
     * @returns The locale after toggling.
     */
    AcExHtmlI18n.prototype.toggleLocale = function () {
        var next = this._locale === 'en' ? 'zh' : 'en';
        this.setLocale(next);
        return next;
    };
    /**
     * Sets the active locale, updates `<html lang>`, storage, and bound DOM nodes.
     *
     * @param locale - Target locale.
     */
    AcExHtmlI18n.prototype.setLocale = function (locale) {
        var _a;
        if (this._locale === locale)
            return;
        this._locale = locale;
        if (typeof document !== 'undefined') {
            document.documentElement.lang = locale;
        }
        try {
            if (typeof localStorage !== 'undefined') {
                localStorage.setItem(ACEX_HTML_LOCALE_STORAGE_KEY, locale);
            }
        }
        catch (_b) {
            /* private mode */
        }
        this.applyToDocument();
        (_a = this._onChange) === null || _a === void 0 ? void 0 : _a.call(this);
    };
    /**
     * Applies translated text to elements under `root` (or the full document).
     * Only updates leaf nodes with `data-i18n-text` to avoid clobbering icon markup.
     *
     * @param root - Subtree to scan; defaults to `document` when omitted.
     */
    AcExHtmlI18n.prototype.applyToDocument = function (root) {
        var _this = this;
        if (typeof document === 'undefined')
            return;
        var scope = root !== null && root !== void 0 ? root : document;
        document.documentElement.lang = this._locale;
        // Only leaf text targets — never set textContent on containers or icon buttons.
        scope.querySelectorAll('[data-i18n-text]').forEach(function (el) {
            var key = el.dataset.i18nKey;
            if (!key)
                return;
            el.textContent = _this.t(key);
        });
        scope.querySelectorAll('[data-i18n-attr]').forEach(function (el) {
            var e_3, _a;
            var _b, _c;
            var key = el.dataset.i18nKey;
            var attrs = (_c = (_b = el.dataset.i18nAttr) === null || _b === void 0 ? void 0 : _b.split(/\s+/)) !== null && _c !== void 0 ? _c : [];
            if (!key || attrs.length === 0)
                return;
            var text = _this.t(key);
            try {
                for (var attrs_1 = __values(attrs), attrs_1_1 = attrs_1.next(); !attrs_1_1.done; attrs_1_1 = attrs_1.next()) {
                    var attr = attrs_1_1.value;
                    el.setAttribute(attr, text);
                }
            }
            catch (e_3_1) { e_3 = { error: e_3_1 }; }
            finally {
                try {
                    if (attrs_1_1 && !attrs_1_1.done && (_a = attrs_1.return)) _a.call(attrs_1);
                }
                finally { if (e_3) throw e_3.error; }
            }
        });
        var badge = document.getElementById('mlcad-lang-badge');
        if (badge)
            badge.textContent = this.localeBadge;
        var langBtn = document.getElementById('mlcad-lang-btn');
        if (langBtn) {
            langBtn.setAttribute('title', this.t('toolbar.languageSwitch'));
            langBtn.setAttribute('aria-label', this.t('toolbar.languageSwitch'));
        }
    };
    return AcExHtmlI18n;
}());
export { AcExHtmlI18n };
//# sourceMappingURL=AcExHtmlI18n.js.map