var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
import { toWcsCoord } from './AcExBatchBuffers';
/**
 * Creates an empty extents bucket with {@link AcExMutableExtents.valid} set to `false`.
 */
export function createEmptyExtents() {
    return {
        minX: 0,
        minY: 0,
        maxX: 0,
        maxY: 0,
        valid: false
    };
}
/**
 * Expands mutable extents from a flat XYZ position buffer, applying a world offset per vertex.
 *
 * @param extents - Extents bucket to update in place.
 * @param positions - Flat `[x, y, z, …]` buffer in local space.
 * @param offset - World translation added to each vertex before comparing XY.
 */
export function expandExtentsFromPositions(extents, positions, offset) {
    if (positions.length < 3)
        return;
    var ox = offset[0];
    var oy = offset[1];
    for (var i = 0; i + 2 < positions.length; i += 3) {
        var x = toWcsCoord(positions[i], ox);
        var y = toWcsCoord(positions[i + 1], oy);
        if (!extents.valid) {
            extents.minX = extents.maxX = x;
            extents.minY = extents.maxY = y;
            extents.valid = true;
        }
        else {
            if (x < extents.minX)
                extents.minX = x;
            if (x > extents.maxX)
                extents.maxX = x;
            if (y < extents.minY)
                extents.minY = y;
            if (y > extents.maxY)
                extents.maxY = y;
        }
    }
}
/**
 * Incorporates all vertices from a line or mesh batch into mutable extents.
 *
 * @param extents - Extents bucket to update in place.
 * @param batch - Exported batch whose {@link AcExLineBatch.positions} and
 *   {@link AcExLineBatch.offset} are scanned.
 */
export function expandExtentsFromBatch(extents, batch) {
    expandExtentsFromPositions(extents, batch.positions, batch.offset);
}
/**
 * Converts a mutable extents bucket to an immutable {@link AcExExtents}, or `null` if empty.
 *
 * @param extents - Accumulator that may still be invalid.
 * @returns Frozen extents, or `null` when no geometry was added.
 */
export function toExtents(extents) {
    if (!extents.valid)
        return null;
    return {
        minX: extents.minX,
        minY: extents.minY,
        maxX: extents.maxX,
        maxY: extents.maxY
    };
}
/**
 * Unions XY extents from every line and mesh batch in one layout.
 *
 * Used for initial zoom-to-fit in the offline viewer so hatch pattern shaders
 * receive a realistic {@link acexCameraZoomUniform} instead of zooming to the
 * often much larger database `EXTMIN`/`EXTMAX` header.
 */
export function computeLayoutExtents(lineBatches, meshBatches) {
    var e_1, _a, e_2, _b;
    var bucket = createEmptyExtents();
    try {
        for (var lineBatches_1 = __values(lineBatches), lineBatches_1_1 = lineBatches_1.next(); !lineBatches_1_1.done; lineBatches_1_1 = lineBatches_1.next()) {
            var batch = lineBatches_1_1.value;
            expandExtentsFromBatch(bucket, batch);
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (lineBatches_1_1 && !lineBatches_1_1.done && (_a = lineBatches_1.return)) _a.call(lineBatches_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    try {
        for (var meshBatches_1 = __values(meshBatches), meshBatches_1_1 = meshBatches_1.next(); !meshBatches_1_1.done; meshBatches_1_1 = meshBatches_1.next()) {
            var batch = meshBatches_1_1.value;
            expandExtentsFromBatch(bucket, batch);
        }
    }
    catch (e_2_1) { e_2 = { error: e_2_1 }; }
    finally {
        try {
            if (meshBatches_1_1 && !meshBatches_1_1.done && (_b = meshBatches_1.return)) _b.call(meshBatches_1);
        }
        finally { if (e_2) throw e_2.error; }
    }
    return toExtents(bucket);
}
/**
 * Resolves zoom-to-fit extents for one layout snapshot.
 *
 * Prefers live batch geometry, then persisted {@link AcExSnapshot.meta.viewExtents},
 * and only then the database header extents as a last resort.
 */
export function resolveLayoutViewExtents(layout, fallbackExtents) {
    var _a, _b;
    return ((_b = (_a = computeLayoutExtents(layout.lineBatches, layout.meshBatches)) !== null && _a !== void 0 ? _a : fallbackExtents) !== null && _b !== void 0 ? _b : {
        minX: 0,
        minY: 0,
        maxX: 1,
        maxY: 1
    });
}
/**
 * Builds per-layer XY extents from all line and mesh batches in a layout.
 * Used by the layer panel “zoom to layer” action.
 *
 * @param lineBatches - Line batches for the active layout.
 * @param meshBatches - Mesh batches for the active layout.
 * @returns Map from layer name to extents, or `null` when the layer has no geometry.
 */
export function computeLayerExtentsMap(lineBatches, meshBatches) {
    var e_3, _a, e_4, _b, e_5, _c;
    var buckets = new Map();
    var ensure = function (layer) {
        var bucket = buckets.get(layer);
        if (!bucket) {
            bucket = createEmptyExtents();
            buckets.set(layer, bucket);
        }
        return bucket;
    };
    try {
        for (var lineBatches_2 = __values(lineBatches), lineBatches_2_1 = lineBatches_2.next(); !lineBatches_2_1.done; lineBatches_2_1 = lineBatches_2.next()) {
            var batch = lineBatches_2_1.value;
            expandExtentsFromBatch(ensure(batch.layer), batch);
        }
    }
    catch (e_3_1) { e_3 = { error: e_3_1 }; }
    finally {
        try {
            if (lineBatches_2_1 && !lineBatches_2_1.done && (_a = lineBatches_2.return)) _a.call(lineBatches_2);
        }
        finally { if (e_3) throw e_3.error; }
    }
    try {
        for (var meshBatches_2 = __values(meshBatches), meshBatches_2_1 = meshBatches_2.next(); !meshBatches_2_1.done; meshBatches_2_1 = meshBatches_2.next()) {
            var batch = meshBatches_2_1.value;
            expandExtentsFromBatch(ensure(batch.layer), batch);
        }
    }
    catch (e_4_1) { e_4 = { error: e_4_1 }; }
    finally {
        try {
            if (meshBatches_2_1 && !meshBatches_2_1.done && (_b = meshBatches_2.return)) _b.call(meshBatches_2);
        }
        finally { if (e_4) throw e_4.error; }
    }
    var result = new Map();
    try {
        for (var buckets_1 = __values(buckets), buckets_1_1 = buckets_1.next(); !buckets_1_1.done; buckets_1_1 = buckets_1.next()) {
            var _d = __read(buckets_1_1.value, 2), layer = _d[0], bucket = _d[1];
            result.set(layer, toExtents(bucket));
        }
    }
    catch (e_5_1) { e_5 = { error: e_5_1 }; }
    finally {
        try {
            if (buckets_1_1 && !buckets_1_1.done && (_c = buckets_1.return)) _c.call(buckets_1);
        }
        finally { if (e_5) throw e_5.error; }
    }
    return result;
}
//# sourceMappingURL=AcExLayerExtents.js.map