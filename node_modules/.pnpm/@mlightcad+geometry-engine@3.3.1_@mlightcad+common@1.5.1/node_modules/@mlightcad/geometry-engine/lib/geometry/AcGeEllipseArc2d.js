var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { AcCmErrors } from '@mlightcad/common';
import { AcGeBox2d, AcGePoint2d, AcGePoint3d } from '../math';
import { AcGeMathUtil, TAU } from '../util';
import { AcGeCurve2d } from './AcGeCurve2d';
/**
 * Class representing a 3d ellipse arc defined by center, normal, majorAxis, majorAxisRadius,
 * minorAxisRadius, startAngle, and endAngle.
 * - The majorAxis vector represents half the major axis of the ellipse (that is, from the center
 * point to the start point of the ellipse) and is the zero angle for startAngle and endAngle.
 * - Positive angles are counter-clockwise when looking down the normal vector (that is, right-hand
 * rule). A startAngle of 0 and endAngle of 2pi will produce a closed Ellipse.
 * - If startAngle is equal to 0 and endAngle is equal to 2 * Math.PI, it represents a full 3d ellipse.
 */
var AcGeEllipseArc2d = /** @class */ (function (_super) {
    __extends(AcGeEllipseArc2d, _super);
    /**
     * Construct an instance of the ellipse arc.
     * @param center Center point of the ellipse.
     * @param majorAxisRadius Major axis radius of the ellipse.
     * @param minorAxisRadius Minor axis radius of the ellipse.
     * @param startAngle Start angle of the ellipse arc in radians.
     * @param endAngle End angle of the ellipse arc in radians.
     * @param clockwise The flag Whether the ellipse arc is drawn clockwise. Default is false.
     * @param rotation The rotation angle of the ellipse in radians, counterclockwise from the positive X
     * axis (optional). Default is 0.
     */
    function AcGeEllipseArc2d(center, majorAxisRadius, minorAxisRadius, startAngle, endAngle, clockwise, rotation) {
        if (startAngle === void 0) { startAngle = 0; }
        if (endAngle === void 0) { endAngle = TAU; }
        if (clockwise === void 0) { clockwise = false; }
        if (rotation === void 0) { rotation = 0; }
        var _this = _super.call(this) || this;
        _this.center = center;
        _this.majorAxisRadius = majorAxisRadius;
        _this.minorAxisRadius = minorAxisRadius;
        // Check whether it is a full ellipse
        if ((endAngle - startAngle) % TAU == 0) {
            _this.startAngle = 0;
            _this.endAngle = TAU;
        }
        else {
            _this.startAngle = startAngle;
            _this.endAngle = endAngle;
        }
        _this.clockwise = clockwise;
        _this.rotation = rotation;
        return _this;
    }
    Object.defineProperty(AcGeEllipseArc2d.prototype, "center", {
        /**
         * Center of the ellipse in 3d space
         */
        get: function () {
            return this._center;
        },
        set: function (value) {
            this._center = new AcGePoint3d(value.x, value.y, value.z || 0);
            this._boundingBoxNeedsUpdate = true;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcGeEllipseArc2d.prototype, "majorAxisRadius", {
        /**
         * Major axis radius of the ellipse
         */
        get: function () {
            return this._majorAxisRadius;
        },
        set: function (value) {
            if (value < 0)
                throw AcCmErrors.ILLEGAL_PARAMETERS;
            this._majorAxisRadius = value;
            this._boundingBoxNeedsUpdate = true;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcGeEllipseArc2d.prototype, "minorAxisRadius", {
        /**
         * Minor axis radius of the ellipse
         */
        get: function () {
            return this._minorAxisRadius;
        },
        set: function (value) {
            if (value < 0)
                throw AcCmErrors.ILLEGAL_PARAMETERS;
            this._minorAxisRadius = value;
            this._boundingBoxNeedsUpdate = true;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcGeEllipseArc2d.prototype, "startAngle", {
        /**
         * Start angle of the ellipse arc in radians in the range -pi to pi.
         */
        get: function () {
            return this._startAngle;
        },
        set: function (value) {
            this._startAngle = AcGeMathUtil.normalizeAngle(value);
            this._boundingBoxNeedsUpdate = true;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcGeEllipseArc2d.prototype, "endAngle", {
        /**
         * End angle of the ellipse arc in radians in the range -pi to pi.
         */
        get: function () {
            return this._endAngle;
        },
        set: function (value) {
            this._endAngle =
                this.startAngle == 0 && value == TAU
                    ? value
                    : AcGeMathUtil.normalizeAngle(value);
            this._boundingBoxNeedsUpdate = true;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcGeEllipseArc2d.prototype, "clockwise", {
        /**
         * The flag Whether the ellipse arc is drawn clockwise. Default is false.
         */
        get: function () {
            return this._clockwise;
        },
        set: function (value) {
            this._clockwise = value;
            this._boundingBoxNeedsUpdate = true;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcGeEllipseArc2d.prototype, "rotation", {
        /**
         * The rotation angle of the ellipse in radians, counterclockwise from the positive X axis (optional).
         * Default is 0.
         */
        get: function () {
            return this._rotation;
        },
        set: function (value) {
            this._rotation = value;
            this._boundingBoxNeedsUpdate = true;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcGeEllipseArc2d.prototype, "deltaAngle", {
        /**
         * Return angle between endAngle and startAngle in range 0 to 2*PI
         */
        get: function () {
            return AcGeMathUtil.normalizeAngle(this.endAngle - this.startAngle);
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcGeEllipseArc2d.prototype, "isLargeArc", {
        /**
         * Return true if the arc is a large arc whose delta angle value is greater than PI.
         */
        get: function () {
            return Math.abs(this.deltaAngle) > Math.PI ? 1 : 0;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Compute the bounding box of the 3D ellipse arc.
     * The bounding box is defined by its minimum and maximum x, y, and z coordinates.
     * @returns Return bounding box containing the min and max coordinates
     */
    AcGeEllipseArc2d.prototype.calculateBoundingBox = function () {
        var numPoints = 100;
        var minX = Infinity, minY = Infinity;
        var maxX = -Infinity, maxY = -Infinity;
        for (var i = 0; i <= numPoints; i++) {
            var point = this.getPoint(i / numPoints);
            minX = Math.min(minX, point.x);
            minY = Math.min(minY, point.y);
            maxX = Math.max(maxX, point.x);
            maxY = Math.max(maxY, point.y);
        }
        // Return the bounding box defined by min and max coordinates
        return new AcGeBox2d({ x: minX, y: minY }, { x: maxX, y: maxY });
    };
    Object.defineProperty(AcGeEllipseArc2d.prototype, "closed", {
        /**
         * Return true if its start point is identical to its end point. Otherwise, return false.
         */
        get: function () {
            return this.deltaAngle == 0;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Return the point for a given position on the curve according to the arc length.
     * @param t Input a position on the curve according to the arc length. Must be in the range [ 0, 1 ].
     * @returns Return the point for a given position on the curve according to the arc length.
     */
    AcGeEllipseArc2d.prototype.getPoint = function (t) {
        var twoPi = Math.PI * 2;
        var deltaAngle = this.endAngle - this.startAngle;
        var samePoints = Math.abs(deltaAngle) < Number.EPSILON;
        // ensures that deltaAngle is 0 .. 2 PI
        while (deltaAngle < 0)
            deltaAngle += twoPi;
        while (deltaAngle > twoPi)
            deltaAngle -= twoPi;
        if (deltaAngle < Number.EPSILON) {
            if (samePoints) {
                deltaAngle = 0;
            }
            else {
                deltaAngle = twoPi;
            }
        }
        if (this.clockwise === true && !samePoints) {
            if (deltaAngle === twoPi) {
                deltaAngle = -twoPi;
            }
            else {
                deltaAngle = deltaAngle - twoPi;
            }
        }
        var angle = this.startAngle + t * deltaAngle;
        var x = this.center.x + this.majorAxisRadius * Math.cos(angle);
        var y = this.center.y + this.minorAxisRadius * Math.sin(angle);
        if (this.rotation !== 0) {
            var cos = Math.cos(this.rotation);
            var sin = Math.sin(this.rotation);
            var tx = x - this.center.x;
            var ty = y - this.center.y;
            // Rotate the point about the center of the ellipse.
            x = tx * cos - ty * sin + this.center.x;
            y = tx * sin + ty * cos + this.center.y;
        }
        return new AcGePoint2d(x, y);
    };
    /**
     * @inheritdoc
     */
    AcGeEllipseArc2d.prototype.transform = function (_matrix) {
        var matrix = _matrix;
        var transformedCenter2d = new AcGePoint2d(this.center).applyMatrix2d(matrix);
        var transformedCenter = new AcGePoint3d(transformedCenter2d.x, transformedCenter2d.y, this.center.z);
        var transformedMajorPoint = this.getPointAtAngle(0)
            .clone()
            .applyMatrix2d(matrix);
        var transformedMinorPoint = this.getPointAtAngle(Math.PI / 2)
            .clone()
            .applyMatrix2d(matrix);
        var majorVector = new AcGePoint2d(transformedMajorPoint).sub(transformedCenter2d);
        var minorVector = new AcGePoint2d(transformedMinorPoint).sub(transformedCenter2d);
        var majorAxisRadius = majorVector.length();
        var minorAxisRadius = minorVector.length();
        var rotation = Math.atan2(majorVector.y, majorVector.x);
        var majorDirection = majorVector.clone().normalize();
        var minorDirection = minorVector.clone().normalize();
        var getAngle = function (point) {
            var relative = new AcGePoint2d(point).sub(transformedCenter2d);
            var x = relative.dot(majorDirection);
            var y = relative.dot(minorDirection);
            return AcGeMathUtil.normalizeAngle(Math.atan2(y / minorAxisRadius, x / majorAxisRadius));
        };
        var clockwise = matrix.determinant() < 0 ? !this.clockwise : this.clockwise;
        var transformedEllipse = this.closed
            ? new AcGeEllipseArc2d(transformedCenter, majorAxisRadius, minorAxisRadius, 0, TAU, clockwise, rotation)
            : new AcGeEllipseArc2d(transformedCenter, majorAxisRadius, minorAxisRadius, getAngle(this.startPoint.clone().applyMatrix2d(matrix)), getAngle(this.endPoint.clone().applyMatrix2d(matrix)), clockwise, rotation);
        this.center = transformedEllipse.center;
        this.majorAxisRadius = transformedEllipse.majorAxisRadius;
        this.minorAxisRadius = transformedEllipse.minorAxisRadius;
        this._startAngle = transformedEllipse._startAngle;
        this._endAngle = transformedEllipse._endAngle;
        this._clockwise = transformedEllipse._clockwise;
        this.rotation = transformedEllipse.rotation;
        this._boundingBoxNeedsUpdate = true;
        return this;
    };
    AcGeEllipseArc2d.prototype.getPointAtAngle = function (angle) {
        return this.getPoint(this.closed
            ? angle / TAU
            : AcGeMathUtil.normalizeAngle(angle - this.startAngle) / this.deltaAngle);
    };
    /**
     * @inheritdoc
     */
    AcGeEllipseArc2d.prototype.copy = function (value) {
        this.center = value.center;
        this.majorAxisRadius = value.majorAxisRadius;
        this.minorAxisRadius = value.minorAxisRadius;
        this.startAngle = value.startAngle;
        this.endAngle = value.endAngle;
        this.clockwise = value.clockwise;
        this.rotation = value.rotation;
        return this;
    };
    /**
     * @inheritdoc
     */
    AcGeEllipseArc2d.prototype.clone = function () {
        return new AcGeEllipseArc2d(this.center, this.majorAxisRadius, this.minorAxisRadius, this.startAngle, this.endAngle, this.clockwise, this.rotation);
    };
    /**
     * Returns the two focal points of this ellipse in WCS (XY).
     *
     * Returns an empty array when the ellipse is circular (major equals minor).
     */
    AcGeEllipseArc2d.prototype.getFocusPoints = function () {
        var a = Math.max(this.majorAxisRadius, this.minorAxisRadius);
        var b = Math.min(this.majorAxisRadius, this.minorAxisRadius);
        if (a - b < 1e-9) {
            return [];
        }
        var c = Math.sqrt(a * a - b * b);
        var cos = Math.cos(this.rotation);
        var sin = Math.sin(this.rotation);
        var isMajorPrimary = this.majorAxisRadius >= this.minorAxisRadius;
        var ux = isMajorPrimary ? cos : -sin;
        var uy = isMajorPrimary ? sin : cos;
        return [
            new AcGePoint2d(this.center.x - c * ux, this.center.y - c * uy),
            new AcGePoint2d(this.center.x + c * ux, this.center.y + c * uy)
        ];
    };
    /**
     * Returns quadrant snap points for a closed ellipse (axis crossings).
     */
    AcGeEllipseArc2d.prototype.getQuadrantPoints = function () {
        if (!this.closed) {
            return [];
        }
        return [
            this.getPoint(0),
            this.getPoint(0.25),
            this.getPoint(0.5),
            this.getPoint(0.75)
        ];
    };
    /**
     * Returns the nearest point on this ellipse arc to the given point.
     *
     * Uses uniform sampling in curve parameter space; sufficient for object snap.
     *
     * @param point - Query point in WCS.
     */
    AcGeEllipseArc2d.prototype.nearestPoint = function (point) {
        var p = new AcGePoint2d(point.x, point.y);
        var best = this.getPoint(0);
        var bestDistSq = p.distanceToSquared(best);
        var samples = 72;
        for (var i = 1; i <= samples; i++) {
            var candidate = this.getPoint(i / samples);
            var d = p.distanceToSquared(candidate);
            if (d < bestDistSq) {
                bestDistSq = d;
                best = candidate;
            }
        }
        return best;
    };
    /**
     * Returns tangent snap point(s) from the given point to this ellipse arc.
     */
    AcGeEllipseArc2d.prototype.tangentPoints = function (point) {
        var _this = this;
        var localP = this._projectPointToLocal(point);
        var angles = this._findSnapAngles(localP, function (qx, qy, cos, sin) {
            var tx = -_this.majorAxisRadius * sin;
            var ty = _this.minorAxisRadius * cos;
            return (qx - localP.x) * ty - (qy - localP.y) * tx;
        });
        return angles.map(function (angle) { return _this.getPointAtAngle(angle); });
    };
    /**
     * Returns perpendicular snap point(s) on this ellipse arc from the given point.
     */
    AcGeEllipseArc2d.prototype.perpendicularPoints = function (point) {
        var _this = this;
        var localP = this._projectPointToLocal(point);
        var angles = this._findSnapAngles(localP, function (qx, qy, cos, sin) {
            var nx = _this.minorAxisRadius * cos;
            var ny = _this.majorAxisRadius * sin;
            return (qx - localP.x) * ny - (qy - localP.y) * nx;
        });
        return angles.map(function (angle) { return _this.getPointAtAngle(angle); });
    };
    AcGeEllipseArc2d.prototype._projectPointToLocal = function (point) {
        var dx = point.x - this.center.x;
        var dy = point.y - this.center.y;
        var cos = Math.cos(-this.rotation);
        var sin = Math.sin(-this.rotation);
        return {
            x: dx * cos - dy * sin,
            y: dx * sin + dy * cos
        };
    };
    AcGeEllipseArc2d.prototype._findSnapAngles = function (_localP, errorAt, samples) {
        if (samples === void 0) { samples = 144; }
        var a = this.majorAxisRadius;
        var b = this.minorAxisRadius;
        var start = this.startAngle;
        var delta = this.deltaAngle;
        var angles = [];
        var evalError = function (theta) {
            var cos = Math.cos(theta);
            var sin = Math.sin(theta);
            return errorAt(a * cos, b * sin, cos, sin);
        };
        var prevT = start;
        var prevE = evalError(prevT);
        for (var i = 1; i <= samples; i++) {
            var t = start + (delta * i) / samples;
            var e = evalError(t);
            if (Math.abs(prevE) < 1e-10)
                angles.push(prevT);
            if (prevE * e < 0) {
                var lo = prevT;
                var hi = t;
                for (var j = 0; j < 32; j++) {
                    var mid = (lo + hi) / 2;
                    if (evalError(lo) * evalError(mid) <= 0)
                        hi = mid;
                    else
                        lo = mid;
                }
                angles.push((lo + hi) / 2);
            }
            prevT = t;
            prevE = e;
        }
        return angles;
    };
    return AcGeEllipseArc2d;
}(AcGeCurve2d));
export { AcGeEllipseArc2d };
//# sourceMappingURL=AcGeEllipseArc2d.js.map