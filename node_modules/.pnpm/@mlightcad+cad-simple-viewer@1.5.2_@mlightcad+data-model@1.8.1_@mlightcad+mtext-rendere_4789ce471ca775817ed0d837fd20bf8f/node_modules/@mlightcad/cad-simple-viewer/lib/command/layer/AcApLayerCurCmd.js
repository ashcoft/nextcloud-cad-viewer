var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import { AcApDocManager } from '../../app';
import { AcEdCommand, AcEdOpenMode, AcEdPromptSelectionOptions, AcEdPromptStatus } from '../../editor';
import { AcApI18n } from '../../i18n';
/**
 * AutoCAD-like `LAYCUR` command.
 *
 * The command changes the layer property of selected objects to the current
 * database layer (`CLAYER`). It supports both preselection and an interactive
 * selection prompt, matching AutoCAD's object-first workflow.
 */
var AcApLayerCurCmd = /** @class */ (function (_super) {
    __extends(AcApLayerCurCmd, _super);
    /**
     * Creates a write-enabled `LAYCUR` command instance.
     */
    function AcApLayerCurCmd() {
        var _this = _super.call(this) || this;
        _this.mode = AcEdOpenMode.Write;
        return _this;
    }
    /**
     * Runs the change-selected-objects-to-current-layer workflow.
     *
     * @param context - Active application context used to read and update the current drawing.
     * @returns Resolves when command execution completes or is canceled.
     */
    AcApLayerCurCmd.prototype.execute = function (context) {
        return __awaiter(this, void 0, void 0, function () {
            var selectionSet, objectIds, _a;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        selectionSet = context.view.selectionSet;
                        if (!(selectionSet.count > 0)) return [3 /*break*/, 1];
                        _a = selectionSet.ids;
                        return [3 /*break*/, 3];
                    case 1: return [4 /*yield*/, this.promptSelection()];
                    case 2:
                        _a = _b.sent();
                        _b.label = 3;
                    case 3:
                        objectIds = _a;
                        if (!objectIds || objectIds.length === 0)
                            return [2 /*return*/];
                        this.moveObjectsToCurrentLayer(context, objectIds);
                        return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Prompts for objects whose layer should be changed.
     *
     * @returns Selected object identifiers, or `undefined` when the prompt is canceled.
     */
    AcApLayerCurCmd.prototype.promptSelection = function () {
        return __awaiter(this, void 0, void 0, function () {
            var prompt, result;
            var _a, _b;
            return __generator(this, function (_c) {
                switch (_c.label) {
                    case 0:
                        prompt = new AcEdPromptSelectionOptions(AcApI18n.t('jig.laycur.prompt'));
                        return [4 /*yield*/, AcApDocManager.instance.editor.getSelection(prompt)];
                    case 1:
                        result = _c.sent();
                        if (result.status !== AcEdPromptStatus.OK)
                            return [2 /*return*/, undefined];
                        return [2 /*return*/, (_b = (_a = result.value) === null || _a === void 0 ? void 0 : _a.ids) !== null && _b !== void 0 ? _b : []];
                }
            });
        });
    };
    /**
     * Applies the current layer to selected entities.
     *
     * @param context - Active application context containing database and view.
     * @param objectIds - Selected entity identifiers.
     */
    AcApLayerCurCmd.prototype.moveObjectsToCurrentLayer = function (context, objectIds) {
        var _a;
        var db = context.doc.database;
        var currentLayerName = (_a = db.clayer) === null || _a === void 0 ? void 0 : _a.trim();
        var currentLayer = currentLayerName
            ? db.tables.layerTable.getAt(currentLayerName)
            : undefined;
        if (!currentLayer) {
            this.notify(AcApI18n.t('jig.laycur.currentLayerNotFound'), 'warning');
            return;
        }
        var changedEntities = [];
        var alreadyCurrent = 0;
        var missing = 0;
        new Set(objectIds).forEach(function (objectId) {
            var entity = db.tables.blockTable.getEntityById(objectId);
            if (!entity) {
                missing++;
                return;
            }
            if (entity.layer === currentLayer.name) {
                alreadyCurrent++;
                return;
            }
            entity.layer = currentLayer.name;
            changedEntities.push(entity);
        });
        if (changedEntities.length === 0) {
            this.notify(alreadyCurrent > 0
                ? AcApI18n.t('jig.laycur.alreadyCurrent')
                : AcApI18n.t('jig.laycur.noObjects'), alreadyCurrent > 0 && missing === 0 ? 'info' : 'warning');
            return;
        }
        changedEntities.forEach(function (entity) { return entity.triggerModifiedEvent(); });
        context.view.selectionSet.clear();
        // Layer changes move entities between render buckets, so rebuild the view.
        AcApDocManager.instance.regen();
        this.notify("".concat(AcApI18n.t('jig.laycur.changed'), ": ").concat(changedEntities.length, " (").concat(currentLayer.name, ")"), 'success');
    };
    /**
     * Sends a localized status message through the command-line output.
     *
     * @param message - Text to display to the user.
     * @param type - Visual severity mapped to command-line message styles.
     */
    AcApLayerCurCmd.prototype.notify = function (message, type) {
        if (type === void 0) { type = 'info'; }
        AcApDocManager.instance.editor.showMessage(message, type);
    };
    return AcApLayerCurCmd;
}(AcEdCommand));
export { AcApLayerCurCmd };
//# sourceMappingURL=AcApLayerCurCmd.js.map