var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcApSettingManager, resolveExportDownloadName } from '@mlightcad/cad-simple-viewer';
import { AcSvgRenderer } from '@mlightcad/cad-svg-plugin';
import { jsPDF } from 'jspdf';
import { svg2pdf } from 'svg2pdf.js';
/**
 * Utility class for converting CAD drawings to PDF format.
 *
 * Reuses the SVG renderer pipeline and converts the resulting SVG to a
 * vector PDF using jsPDF + svg2pdf.js.
 */
var AcApPdfConvertor = /** @class */ (function () {
    function AcApPdfConvertor() {
    }
    /**
     * Renders the current drawing to PDF and triggers a browser download.
     */
    AcApPdfConvertor.prototype.convert = function (context) {
        return __awaiter(this, void 0, void 0, function () {
            var svgString, downloadName;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0: return [4 /*yield*/, this.buildSvg(context)];
                    case 1:
                        svgString = _a.sent();
                        downloadName = resolveExportDownloadName(context.doc.fileName || context.doc.docTitle, 'pdf');
                        return [4 /*yield*/, this.downloadAsPdf(svgString, downloadName)];
                    case 2:
                        _a.sent();
                        return [2 /*return*/];
                }
            });
        });
    };
    AcApPdfConvertor.prototype.buildSvg = function (context) {
        return __awaiter(this, void 0, void 0, function () {
            var entities, renderer, entities_1, entities_1_1, entity;
            var e_1, _a;
            return __generator(this, function (_b) {
                AcSvgRenderer.prepareExport();
                entities = context.doc.database.tables.blockTable.modelSpace.newIterator();
                renderer = new AcSvgRenderer();
                this.configureRenderer(renderer, context);
                try {
                    for (entities_1 = __values(entities), entities_1_1 = entities_1.next(); !entities_1_1.done; entities_1_1 = entities_1.next()) {
                        entity = entities_1_1.value;
                        entity.worldDraw(renderer);
                    }
                }
                catch (e_1_1) { e_1 = { error: e_1_1 }; }
                finally {
                    try {
                        if (entities_1_1 && !entities_1_1.done && (_a = entities_1.return)) _a.call(entities_1);
                    }
                    finally { if (e_1) throw e_1.error; }
                }
                return [2 /*return*/, renderer.exportAsync()];
            });
        });
    };
    AcApPdfConvertor.prototype.configureRenderer = function (renderer, context) {
        var _a;
        var db = context.doc.database;
        renderer.ltscale = db.ltscale;
        renderer.celtscale = db.celtscale;
        renderer.showLineWeight = !!db.lwdisplay;
        renderer.setFontMapping(AcApSettingManager.instance.fontMapping);
        var view = context.view;
        var bg = (_a = view === null || view === void 0 ? void 0 : view.backgroundColor) !== null && _a !== void 0 ? _a : 0xffffff;
        renderer.currentBackgroundColor = bg;
        renderer.changeForeground(bg === 0 ? 0xffffff : 0x000000);
    };
    AcApPdfConvertor.prototype.downloadAsPdf = function (svgString, downloadName) {
        return __awaiter(this, void 0, void 0, function () {
            var parser, svgDoc, svgEl, vb, vbWidth, vbHeight, orientation, pdf;
            var _a;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        parser = new DOMParser();
                        svgDoc = parser.parseFromString(svgString, 'image/svg+xml');
                        svgEl = svgDoc.documentElement;
                        vb = (_a = svgEl.getAttribute('viewBox')) === null || _a === void 0 ? void 0 : _a.split(' ').map(Number);
                        vbWidth = vb && vb.length === 4 ? Math.abs(vb[2]) : 297;
                        vbHeight = vb && vb.length === 4 ? Math.abs(vb[3]) : 210;
                        orientation = vbWidth >= vbHeight ? 'landscape' : 'portrait';
                        pdf = new jsPDF({
                            orientation: orientation,
                            unit: 'mm',
                            format: [vbWidth, vbHeight]
                        });
                        return [4 /*yield*/, svg2pdf(svgEl, pdf, {
                                x: 0,
                                y: 0,
                                width: vbWidth,
                                height: vbHeight
                            })];
                    case 1:
                        _b.sent();
                        pdf.save(downloadName);
                        return [2 /*return*/];
                }
            });
        });
    };
    return AcApPdfConvertor;
}());
export { AcApPdfConvertor };
//# sourceMappingURL=AcApPdfConvertor.js.map