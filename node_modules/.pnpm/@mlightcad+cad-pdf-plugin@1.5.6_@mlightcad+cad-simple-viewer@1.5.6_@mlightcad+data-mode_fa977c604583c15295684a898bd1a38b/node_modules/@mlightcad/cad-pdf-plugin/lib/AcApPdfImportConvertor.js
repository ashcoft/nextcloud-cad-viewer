var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
import { AcDbLine, AcDbPolyline, AcGePoint2d, AcGePoint3d, log } from '@mlightcad/data-model';
import * as pdfjsLib from 'pdfjs-dist';
pdfjsLib.GlobalWorkerOptions.workerSrc = '';
/** 1 PDF point in mm (1 pt = 1/72 inch = 25.4/72 mm) */
var PT_TO_MM = 25.4 / 72;
/** Bezier approximation resolution (line segments per curve) */
var BEZIER_STEPS = 8;
/**
 * Converts a PDF file into CAD entities appended to the current document's
 * model space.
 */
var AcApPdfImportConvertor = /** @class */ (function () {
    function AcApPdfImportConvertor() {
    }
    /**
     * Prompts the user to pick a PDF file and imports vector geometry.
     *
     * @param context - Application context for the target document
     */
    AcApPdfImportConvertor.prototype.importFromFilePicker = function (context) {
        var _this = this;
        var input = document.createElement('input');
        input.type = 'file';
        input.accept = '.pdf';
        input.style.display = 'none';
        document.body.appendChild(input);
        input.addEventListener('change', function () { return __awaiter(_this, void 0, void 0, function () {
            var file, buffer;
            var _a;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        file = (_a = input.files) === null || _a === void 0 ? void 0 : _a[0];
                        document.body.removeChild(input);
                        if (!file)
                            return [2 /*return*/];
                        return [4 /*yield*/, file.arrayBuffer()];
                    case 1:
                        buffer = _b.sent();
                        return [4 /*yield*/, this.convert(context, buffer)];
                    case 2:
                        _b.sent();
                        return [2 /*return*/];
                }
            });
        }); });
        input.click();
    };
    /**
     * Converts the first page of a PDF ArrayBuffer into CAD entities.
     * @param context - Application context for the target document
     * @param data - Raw PDF bytes
     * @param pageNumber - 1-based page number (default: 1)
     */
    AcApPdfImportConvertor.prototype.convert = function (context_1, data_1) {
        return __awaiter(this, arguments, void 0, function (context, data, pageNumber) {
            var pdf, page, viewport, pageHeight, operatorList, entities, modelSpace, entities_1, entities_1_1, entity, err_1;
            var e_1, _a;
            if (pageNumber === void 0) { pageNumber = 1; }
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        _b.trys.push([0, 4, , 5]);
                        return [4 /*yield*/, pdfjsLib.getDocument({ data: data }).promise];
                    case 1:
                        pdf = _b.sent();
                        return [4 /*yield*/, pdf.getPage(pageNumber)];
                    case 2:
                        page = _b.sent();
                        viewport = page.getViewport({ scale: 1 });
                        pageHeight = viewport.height;
                        return [4 /*yield*/, page.getOperatorList()];
                    case 3:
                        operatorList = _b.sent();
                        entities = this.extractEntities(operatorList, pageHeight);
                        if (entities.length === 0) {
                            log.warn('[PdfImport] No vector paths found in PDF page.');
                            return [2 /*return*/];
                        }
                        modelSpace = context.doc.database.tables.blockTable.modelSpace;
                        try {
                            for (entities_1 = __values(entities), entities_1_1 = entities_1.next(); !entities_1_1.done; entities_1_1 = entities_1.next()) {
                                entity = entities_1_1.value;
                                modelSpace.appendEntity(entity);
                            }
                        }
                        catch (e_1_1) { e_1 = { error: e_1_1 }; }
                        finally {
                            try {
                                if (entities_1_1 && !entities_1_1.done && (_a = entities_1.return)) _a.call(entities_1);
                            }
                            finally { if (e_1) throw e_1.error; }
                        }
                        log.info("[PdfImport] Imported ".concat(entities.length, " entities from PDF."));
                        return [3 /*break*/, 5];
                    case 4:
                        err_1 = _b.sent();
                        log.error('[PdfImport] Failed to import PDF:', err_1);
                        return [3 /*break*/, 5];
                    case 5: return [2 /*return*/];
                }
            });
        });
    };
    AcApPdfImportConvertor.prototype.extractEntities = function (opList, pageHeight) {
        var e_2, _a, e_3, _b, e_4, _c;
        var _this = this;
        var OPS = pdfjsLib.OPS;
        var fnArray = opList.fnArray, argsArray = opList.argsArray;
        var result = [];
        var subpaths = [];
        var current = [];
        var curX = 0;
        var curY = 0;
        var flush = function () {
            if (current.length > 1)
                subpaths.push(current);
            current = [];
        };
        var commit = function () {
            var e_5, _a;
            flush();
            try {
                for (var subpaths_1 = __values(subpaths), subpaths_1_1 = subpaths_1.next(); !subpaths_1_1.done; subpaths_1_1 = subpaths_1.next()) {
                    var sp = subpaths_1_1.value;
                    var entity = _this.subpathToEntity(sp);
                    if (entity)
                        result.push(entity);
                }
            }
            catch (e_5_1) { e_5 = { error: e_5_1 }; }
            finally {
                try {
                    if (subpaths_1_1 && !subpaths_1_1.done && (_a = subpaths_1.return)) _a.call(subpaths_1);
                }
                finally { if (e_5) throw e_5.error; }
            }
            subpaths = [];
        };
        var tx = function (x, _y) { return x * PT_TO_MM; };
        var ty = function (_x, y) { return (pageHeight - y) * PT_TO_MM; };
        for (var i = 0; i < fnArray.length; i++) {
            var fn = fnArray[i];
            var args = argsArray[i];
            switch (fn) {
                case OPS.moveTo: {
                    flush();
                    curX = args[0];
                    curY = args[1];
                    current = [{ x: tx(curX, curY), y: ty(curX, curY) }];
                    break;
                }
                case OPS.lineTo: {
                    curX = args[0];
                    curY = args[1];
                    current.push({ x: tx(curX, curY), y: ty(curX, curY) });
                    break;
                }
                case OPS.curveTo: {
                    var _d = __read(args, 6), x1 = _d[0], y1 = _d[1], x2 = _d[2], y2 = _d[3], x3 = _d[4], y3 = _d[5];
                    var pts = cubicBezier({ x: curX, y: curY }, { x: x1, y: y1 }, { x: x2, y: y2 }, { x: x3, y: y3 }, BEZIER_STEPS);
                    try {
                        for (var pts_1 = (e_2 = void 0, __values(pts)), pts_1_1 = pts_1.next(); !pts_1_1.done; pts_1_1 = pts_1.next()) {
                            var p = pts_1_1.value;
                            current.push({ x: tx(p.x, p.y), y: ty(p.x, p.y) });
                        }
                    }
                    catch (e_2_1) { e_2 = { error: e_2_1 }; }
                    finally {
                        try {
                            if (pts_1_1 && !pts_1_1.done && (_a = pts_1.return)) _a.call(pts_1);
                        }
                        finally { if (e_2) throw e_2.error; }
                    }
                    curX = x3;
                    curY = y3;
                    break;
                }
                case OPS.curveTo2: {
                    var _e = __read(args, 4), x2 = _e[0], y2 = _e[1], x3 = _e[2], y3 = _e[3];
                    var pts = cubicBezier({ x: curX, y: curY }, { x: curX, y: curY }, { x: x2, y: y2 }, { x: x3, y: y3 }, BEZIER_STEPS);
                    try {
                        for (var pts_2 = (e_3 = void 0, __values(pts)), pts_2_1 = pts_2.next(); !pts_2_1.done; pts_2_1 = pts_2.next()) {
                            var p = pts_2_1.value;
                            current.push({ x: tx(p.x, p.y), y: ty(p.x, p.y) });
                        }
                    }
                    catch (e_3_1) { e_3 = { error: e_3_1 }; }
                    finally {
                        try {
                            if (pts_2_1 && !pts_2_1.done && (_b = pts_2.return)) _b.call(pts_2);
                        }
                        finally { if (e_3) throw e_3.error; }
                    }
                    curX = x3;
                    curY = y3;
                    break;
                }
                case OPS.curveTo3: {
                    var _f = __read(args, 4), x1 = _f[0], y1 = _f[1], x3 = _f[2], y3 = _f[3];
                    var pts = cubicBezier({ x: curX, y: curY }, { x: x1, y: y1 }, { x: x3, y: y3 }, { x: x3, y: y3 }, BEZIER_STEPS);
                    try {
                        for (var pts_3 = (e_4 = void 0, __values(pts)), pts_3_1 = pts_3.next(); !pts_3_1.done; pts_3_1 = pts_3.next()) {
                            var p = pts_3_1.value;
                            current.push({ x: tx(p.x, p.y), y: ty(p.x, p.y) });
                        }
                    }
                    catch (e_4_1) { e_4 = { error: e_4_1 }; }
                    finally {
                        try {
                            if (pts_3_1 && !pts_3_1.done && (_c = pts_3.return)) _c.call(pts_3);
                        }
                        finally { if (e_4) throw e_4.error; }
                    }
                    curX = x3;
                    curY = y3;
                    break;
                }
                case OPS.closePath: {
                    if (current.length > 0 && subpaths.length === 0) {
                        current.push(__assign({}, current[0]));
                    }
                    flush();
                    break;
                }
                case OPS.stroke:
                case OPS.fill:
                case OPS.eoFill:
                case OPS.fillStroke:
                case OPS.eoFillStroke:
                case OPS.endPath: {
                    commit();
                    break;
                }
            }
        }
        commit();
        return result;
    };
    AcApPdfImportConvertor.prototype.subpathToEntity = function (pts) {
        if (pts.length < 2)
            return null;
        if (pts.length === 2) {
            return new AcDbLine(new AcGePoint3d(pts[0].x, pts[0].y, 0), new AcGePoint3d(pts[1].x, pts[1].y, 0));
        }
        var poly = new AcDbPolyline();
        for (var i = 0; i < pts.length; i++) {
            poly.addVertexAt(i, new AcGePoint2d(pts[i].x, pts[i].y));
        }
        var first = pts[0];
        var last = pts[pts.length - 1];
        var dx = first.x - last.x;
        var dy = first.y - last.y;
        if (Math.sqrt(dx * dx + dy * dy) < 1e-6) {
            poly.closed = true;
        }
        return poly;
    };
    return AcApPdfImportConvertor;
}());
export { AcApPdfImportConvertor };
/**
 * Approximates a cubic Bézier curve as a polyline.
 *
 * @param p0 - Start point
 * @param p1 - First control point
 * @param p2 - Second control point
 * @param p3 - End point
 * @param steps - Number of line segments to generate
 * @returns Sampled points along the curve (excluding `p0`)
 */
function cubicBezier(p0, p1, p2, p3, steps) {
    var pts = [];
    for (var i = 1; i <= steps; i++) {
        var t = i / steps;
        var mt = 1 - t;
        var x = mt * mt * mt * p0.x +
            3 * mt * mt * t * p1.x +
            3 * mt * t * t * p2.x +
            t * t * t * p3.x;
        var y = mt * mt * mt * p0.y +
            3 * mt * mt * t * p1.y +
            3 * mt * t * t * p2.y +
            t * t * t * p3.y;
        pts.push({ x: x, y: y });
    }
    return pts;
}
//# sourceMappingURL=AcApPdfImportConvertor.js.map