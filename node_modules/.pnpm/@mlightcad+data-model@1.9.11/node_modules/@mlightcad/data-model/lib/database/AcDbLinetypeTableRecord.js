var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
import { AcDbSymbolTableRecord } from './AcDbSymbolTableRecord';
/**
 * Represents a record in the line type table within the AutoCAD drawing database.
 *
 * Each line type table record contains the information necessary to define a specific line type,
 * including its pattern, description, and rendering characteristics. Line types define how lines
 * are drawn, including patterns of dashes, dots, and spaces.
 *
 * Within the line type table record, the dashes (line segments that make up characteristics of the
 * linetype) are stored in a list with an index that is zero based. If the linetype is complex, then
 * embedded shapes or text strings are stored in the list at the same index as the dash that preceded
 * them in the linetype definition. So there will always be a dashLength for any valid index in the
 * list, even if there is a shape or text string sharing the same index. When the linetype is elaborated,
 * a shape's insertion point will coincide with the end of the dash that it shares an index with.
 */
var AcDbLinetypeTableRecord = /** @class */ (function (_super) {
    __extends(AcDbLinetypeTableRecord, _super);
    /**
     * Creates a new line type table record.
     *
     * @param linetype - The line type style object that defines the visual characteristics
     *                   and pattern of this line type
     */
    function AcDbLinetypeTableRecord(linetype) {
        var _this = _super.call(this) || this;
        _this.name = linetype.name;
        _this._linetype = linetype;
        return _this;
    }
    Object.defineProperty(AcDbLinetypeTableRecord.prototype, "numDashes", {
        /**
         * Gets the number of dash elements in the line type pattern.
         *
         * This value represents the total count of dashes, spaces, dots, and other pattern elements
         * that make up the line type. It corresponds to DXF group code 73 in the AutoCAD file format.
         *
         * @returns The number of pattern elements in the line type
         */
        get: function () {
            return this._linetype.pattern ? this._linetype.pattern.length : 0;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbLinetypeTableRecord.prototype, "patternLength", {
        /**
         * Gets the total pattern length in AutoCAD drawing units.
         *
         * The pattern length represents the total length of all dashes and spaces when the line type
         * scale is 1.0. This value is used to calculate how the pattern repeats along a line.
         *
         * Note: Embedded shapes or text strings do not add to the pattern length because they are
         * overlaid and do not interrupt the actual dash pattern.
         *
         * @returns The total length of the line type pattern in drawing units
         */
        get: function () {
            return this._linetype.totalPatternLength;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbLinetypeTableRecord.prototype, "comments", {
        /**
         * Gets the description or comments associated with this line type.
         *
         * This property provides additional information about the line type, such as its intended
         * use or any special characteristics.
         *
         * @returns The description text for the line type
         */
        get: function () {
            return this._linetype.description;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbLinetypeTableRecord.prototype, "linetype", {
        /**
         * Gets the line type style object used by the renderer.
         *
         * This property provides access to the underlying line type definition that contains
         * all the visual characteristics and rendering information.
         *
         * @returns The line type style object
         */
        get: function () {
            return this._linetype;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Gets the length of a specific dash element in the line type pattern.
     *
     * Each dash element in the pattern has a specific length that determines how it appears
     * when the line type is rendered. Positive values represent visible dashes, while negative
     * values represent spaces (pen up).
     *
     * @param index - Zero-based index of the dash element. Must be greater than or equal to zero,
     *                but less than the value of property 'numDashes'
     * @returns The length of the specified dash element in drawing units
     * @throws {Error} When the index is out of range
     */
    AcDbLinetypeTableRecord.prototype.dashLengthAt = function (index) {
        if (index < 0 || index >= this.numDashes) {
            throw new Error('Index must be greater than or equal to zero, but less than the value of property "numDashes".');
        }
        return this._linetype.pattern[index].elementLength;
    };
    /**
     * Converts this linetype preview to an SVG string.
     *
     * The preview is rendered as a horizontal line centered in the SVG viewport.
     * Positive pattern elements are drawn as visible strokes, negative elements
     * are treated as gaps, and zero-length elements are rendered as dots.
     *
     * @param options - Preview rendering options
     * @returns SVG string for linetype preview
     */
    AcDbLinetypeTableRecord.prototype.toPreviewSvgString = function (options) {
        var e_1, _a;
        var _b, _c, _d, _e, _f, _g, _h;
        var width = Math.max((_b = options === null || options === void 0 ? void 0 : options.width) !== null && _b !== void 0 ? _b : 220, 1);
        var height = Math.max((_c = options === null || options === void 0 ? void 0 : options.height) !== null && _c !== void 0 ? _c : 36, 1);
        var padding = Math.min(Math.max((_d = options === null || options === void 0 ? void 0 : options.padding) !== null && _d !== void 0 ? _d : 8, 0), Math.floor(width / 2));
        var strokeWidth = Math.max((_e = options === null || options === void 0 ? void 0 : options.strokeWidth) !== null && _e !== void 0 ? _e : 2, 0.5);
        var stroke = this.escapeSvgAttribute((_f = options === null || options === void 0 ? void 0 : options.stroke) !== null && _f !== void 0 ? _f : 'currentColor');
        var repeats = Math.max((_g = options === null || options === void 0 ? void 0 : options.repeats) !== null && _g !== void 0 ? _g : 4, 1);
        var y = height / 2;
        var startX = padding;
        var endX = Math.max(width - padding, startX + 1);
        var previewWidth = endX - startX;
        var pattern = (_h = this._linetype.pattern) !== null && _h !== void 0 ? _h : [];
        if (pattern.length === 0 ||
            this.patternLength <= 0 ||
            !pattern.some(function (item) { return item.elementLength !== 0; })) {
            return this.buildSvgString({
                width: width,
                height: height,
                stroke: stroke,
                strokeWidth: strokeWidth,
                lineSegments: [[startX, endX]],
                dots: [],
                y: y
            });
        }
        var lineSegments = [];
        var dots = [];
        var unitToPx = previewWidth / (this.patternLength * repeats);
        var minSegmentPx = 0.5;
        var dotStep = Math.max(strokeWidth * 2, 2);
        var currentX = startX;
        while (currentX < endX) {
            try {
                for (var pattern_1 = (e_1 = void 0, __values(pattern)), pattern_1_1 = pattern_1.next(); !pattern_1_1.done; pattern_1_1 = pattern_1.next()) {
                    var item = pattern_1_1.value;
                    if (currentX >= endX)
                        break;
                    var length_1 = item.elementLength;
                    if (length_1 === 0) {
                        dots.push(currentX);
                        currentX = Math.min(currentX + dotStep, endX);
                        continue;
                    }
                    var segmentLengthPx = Math.max(Math.abs(length_1) * unitToPx, minSegmentPx);
                    var nextX = Math.min(currentX + segmentLengthPx, endX);
                    if (length_1 > 0 && nextX > currentX) {
                        lineSegments.push([currentX, nextX]);
                    }
                    currentX = nextX;
                }
            }
            catch (e_1_1) { e_1 = { error: e_1_1 }; }
            finally {
                try {
                    if (pattern_1_1 && !pattern_1_1.done && (_a = pattern_1.return)) _a.call(pattern_1);
                }
                finally { if (e_1) throw e_1.error; }
            }
        }
        return this.buildSvgString({
            width: width,
            height: height,
            stroke: stroke,
            strokeWidth: strokeWidth,
            lineSegments: lineSegments,
            dots: dots,
            y: y
        });
    };
    AcDbLinetypeTableRecord.prototype.buildSvgString = function (input) {
        var width = input.width, height = input.height, stroke = input.stroke, strokeWidth = input.strokeWidth, lineSegments = input.lineSegments, dots = input.dots, y = input.y;
        var lines = lineSegments
            .map(function (_a) {
            var _b = __read(_a, 2), x1 = _b[0], x2 = _b[1];
            return "<line x1=\"".concat(x1.toFixed(2), "\" y1=\"").concat(y.toFixed(2), "\" x2=\"").concat(x2.toFixed(2), "\" y2=\"").concat(y.toFixed(2), "\" stroke=\"").concat(stroke, "\" stroke-width=\"").concat(strokeWidth.toFixed(2), "\" stroke-linecap=\"round\" />");
        })
            .join('');
        var circles = dots
            .map(function (x) {
            return "<circle cx=\"".concat(x.toFixed(2), "\" cy=\"").concat(y.toFixed(2), "\" r=\"").concat((strokeWidth / 2).toFixed(2), "\" fill=\"").concat(stroke, "\" />");
        })
            .join('');
        return "<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"".concat(width, "\" height=\"").concat(height, "\" viewBox=\"0 0 ").concat(width, " ").concat(height, "\" role=\"img\" aria-label=\"").concat(this.escapeSvgAttribute(this.name), " linetype preview\">").concat(lines).concat(circles, "</svg>");
    };
    AcDbLinetypeTableRecord.prototype.escapeSvgAttribute = function (value) {
        return value
            .replace(/&/g, '&amp;')
            .replace(/"/g, '&quot;')
            .replace(/</g, '&lt;')
            .replace(/>/g, '&gt;')
            .replace(/'/g, '&#39;');
    };
    /**
     * Writes DXF fields for this object.
     *
     * @param filer - DXF output writer.
     * @returns The instance (for chaining).
     */
    AcDbLinetypeTableRecord.prototype.dxfOutFields = function (filer) {
        var e_2, _a;
        var _b;
        _super.prototype.dxfOutFields.call(this, filer);
        filer.writeSubclassMarker('AcDbLinetypeTableRecord');
        filer.writeString(2, this.name);
        filer.writeInt16(70, this.linetype.standardFlag);
        filer.writeString(3, this.comments);
        filer.writeInt16(72, 65);
        filer.writeInt16(73, this.numDashes);
        filer.writeDouble(40, this.patternLength);
        try {
            for (var _c = __values((_b = this.linetype.pattern) !== null && _b !== void 0 ? _b : []), _d = _c.next(); !_d.done; _d = _c.next()) {
                var item = _d.value;
                filer.writeDouble(49, item.elementLength);
                filer.writeInt16(74, item.elementTypeFlag);
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_2) throw e_2.error; }
        }
        return this;
    };
    return AcDbLinetypeTableRecord;
}(AcDbSymbolTableRecord));
export { AcDbLinetypeTableRecord };
//# sourceMappingURL=AcDbLinetypeTableRecord.js.map