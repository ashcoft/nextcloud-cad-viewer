var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
import { AcCmColor, AcGeBox2d, AcGiMTextAttachmentPoint, AcGiMTextFlowDirection } from '@mlightcad/data-model';
import { MTextContext, MTextParagraphAlignment, MTextParser, TokenType } from '@mlightcad/mtext-parser';
import { normalizeCadFontName, resolveSvgFontFamily, resolveSvgFontSizeScale } from './AcSvgFontMap';
import { AcSvgStyleUtil } from './AcSvgStyleUtil';
var DEFAULT_LINE_SPACE_FACTOR = 0.25;
var LINE_SPACING_SCALE_FACTOR = 1.666666;
var STACK_VERTICAL_SHIFT_FACTOR = 0.3;
var LATIN_CHAR_WIDTH_FACTOR = 0.6;
var WIDE_CHAR_WIDTH_FACTOR = 1;
var DEFAULT_TAB_WIDTH_FACTOR = 4;
var STACK_SCRIPT_SCALE = 0.7;
function isWideCharacter(char) {
    var _a;
    if (!char || char.length !== 1) {
        return false;
    }
    var code = (_a = char.codePointAt(0)) !== null && _a !== void 0 ? _a : 0;
    return ((code >= 0x1100 && code <= 0x11ff) ||
        (code >= 0x2e80 && code <= 0x9fff) ||
        (code >= 0xf900 && code <= 0xfaff) ||
        (code >= 0xfe10 && code <= 0xfe6f) ||
        (code >= 0xff00 && code <= 0xff60) ||
        (code >= 0x20000 && code <= 0x2ffff));
}
var FlowMode;
(function (FlowMode) {
    FlowMode[FlowMode["HorizontalLtr"] = 0] = "HorizontalLtr";
    FlowMode[FlowMode["HorizontalRtl"] = 1] = "HorizontalRtl";
    FlowMode[FlowMode["VerticalTtb"] = 2] = "VerticalTtb";
    FlowMode[FlowMode["VerticalBtt"] = 3] = "VerticalBtt";
})(FlowMode || (FlowMode = {}));
function resolveLineAdvance(baseHeight, mtext) {
    var factor = typeof mtext.lineSpaceFactor === 'number'
        ? mtext.lineSpaceFactor
        : DEFAULT_LINE_SPACE_FACTOR;
    return baseHeight * (1 + factor * LINE_SPACING_SCALE_FACTOR);
}
function resolveWrapWidth(mtext) {
    if (!Number.isFinite(mtext.width) || mtext.width <= 0) {
        return null;
    }
    return mtext.width;
}
function resolveFlowMode(drawingDirection) {
    switch (drawingDirection) {
        case AcGiMTextFlowDirection.RIGHT_TO_LEFT:
            return FlowMode.HorizontalRtl;
        case AcGiMTextFlowDirection.TOP_TO_BOTTOM:
            return FlowMode.VerticalTtb;
        case AcGiMTextFlowDirection.BOTTOM_TO_TOP:
            return FlowMode.VerticalBtt;
        case AcGiMTextFlowDirection.LEFT_TO_RIGHT:
        case AcGiMTextFlowDirection.BY_STYLE:
        default:
            return FlowMode.HorizontalLtr;
    }
}
function resolveRotation(mtext) {
    var _a;
    var dir = mtext.directionVector;
    if (dir && (dir.x !== 0 || dir.y !== 0)) {
        return Math.atan2(dir.y, dir.x);
    }
    return (_a = mtext.rotation) !== null && _a !== void 0 ? _a : 0;
}
function resolveParagraphAlignment(mtext) {
    var width = mtext.width;
    if (!Number.isFinite(width) || width <= 0 || mtext.attachmentPoint == null) {
        return MTextParagraphAlignment.LEFT;
    }
    switch (mtext.attachmentPoint) {
        case AcGiMTextAttachmentPoint.TopCenter:
        case AcGiMTextAttachmentPoint.MiddleCenter:
        case AcGiMTextAttachmentPoint.BottomCenter:
        case AcGiMTextAttachmentPoint.BaselineCenter:
            return MTextParagraphAlignment.CENTER;
        case AcGiMTextAttachmentPoint.TopRight:
        case AcGiMTextAttachmentPoint.MiddleRight:
        case AcGiMTextAttachmentPoint.BottomRight:
        case AcGiMTextAttachmentPoint.BaselineRight:
            return MTextParagraphAlignment.RIGHT;
        default:
            return MTextParagraphAlignment.LEFT;
    }
}
function layoutBoundsToAnchorMetrics(bounds) {
    return {
        minX: bounds.minX,
        maxX: bounds.maxX,
        minY: -bounds.maxY,
        maxY: -bounds.minY,
        baselineY: -bounds.firstBaselineY
    };
}
function computeAttachmentOffset(attachmentPoint, bounds) {
    var m = layoutBoundsToAnchorMetrics(bounds);
    var centerX = (m.minX + m.maxX) / 2;
    var centerY = (m.minY + m.maxY) / 2;
    switch (attachmentPoint) {
        case AcGiMTextAttachmentPoint.TopCenter:
            return { x: -centerX, y: -m.maxY };
        case AcGiMTextAttachmentPoint.TopRight:
            return { x: -m.maxX, y: -m.maxY };
        case AcGiMTextAttachmentPoint.MiddleLeft:
            return { x: -m.minX, y: -centerY };
        case AcGiMTextAttachmentPoint.MiddleCenter:
            return { x: -centerX, y: -centerY };
        case AcGiMTextAttachmentPoint.MiddleRight:
            return { x: -m.maxX, y: -centerY };
        case AcGiMTextAttachmentPoint.BottomLeft:
            return { x: -m.minX, y: -m.minY };
        case AcGiMTextAttachmentPoint.BottomCenter:
            return { x: -centerX, y: -m.minY };
        case AcGiMTextAttachmentPoint.BottomRight:
            return { x: -m.maxX, y: -m.minY };
        case AcGiMTextAttachmentPoint.BaselineCenter:
            return { x: -centerX, y: -m.baselineY };
        case AcGiMTextAttachmentPoint.BaselineRight:
            return { x: -m.maxX, y: -m.baselineY };
        case AcGiMTextAttachmentPoint.BaselineLeft:
            return { x: -m.minX, y: -m.baselineY };
        case AcGiMTextAttachmentPoint.TopLeft:
        default:
            return { x: -m.minX, y: -m.maxY };
    }
}
/**
 * Builds SVG {@code <text>/<tspan>} output from raw MTEXT using {@link MTextParser}.
 */
export function buildSvgMText(mtext, style, traits, ctx) {
    var e_1, _a;
    var box = new AcGeBox2d();
    var text = mtext.text, height = mtext.height, position = mtext.position;
    if (!text || height <= 0) {
        return { localSvg: '', box: box };
    }
    var baseHeight = height;
    var lineAdvance = resolveLineAdvance(baseHeight, mtext);
    var wrapWidth = resolveWrapWidth(mtext);
    var flowMode = resolveFlowMode(mtext.drawingDirection);
    var defaultFont = resolveSvgFontFamily(style.extendedFont || style.font, 'sans-serif');
    var entityWidthFactor = typeof mtext.widthFactor === 'number' && mtext.widthFactor > 0
        ? mtext.widthFactor
        : typeof style.widthFactor === 'number' && style.widthFactor > 0
            ? style.widthFactor
            : 1;
    var initialCtx = new MTextContext();
    initialCtx.capHeight = { value: baseHeight, isRelative: false };
    initialCtx.widthFactor = { value: entityWidthFactor, isRelative: false };
    initialCtx.paragraph.align = resolveParagraphAlignment(mtext);
    if (typeof style.obliqueAngle === 'number') {
        initialCtx.oblique = style.obliqueAngle;
    }
    initialCtx.fontFace.family = normalizeCadFontName(style.extendedFont || style.font || defaultFont);
    var parser = new MTextParser(text, initialCtx, {
        resetParagraphParameters: true,
        yieldPropertyCommands: true
    });
    var layout = new MTextSvgLayout(baseHeight, lineAdvance, wrapWidth, flowMode, defaultFont, style, traits, ctx);
    try {
        for (var _b = __values(parser.parse()), _c = _b.next(); !_c.done; _c = _b.next()) {
            var token = _c.value;
            layout.consume(token);
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_1) throw e_1.error; }
    }
    layout.finish();
    var bounds = layout.bounds;
    var attachment = computeAttachmentOffset(mtext.attachmentPoint, bounds);
    var x = position.x, y = position.y;
    var rotDeg = (resolveRotation(mtext) * 180) / Math.PI;
    var transformParts = ["translate(".concat(x, ",").concat(y, ")")];
    if (rotDeg !== 0) {
        transformParts.push("rotate(".concat(-rotDeg, ")"));
    }
    transformParts.push("translate(".concat(attachment.x, ",").concat(attachment.y, ")"));
    transformParts.push('scale(1,-1)');
    var textAttrs = __assign({ 'font-size': String(baseHeight), 'font-family': defaultFont }, AcSvgStyleUtil.textAttributes(traits, ctx));
    var innerMarkup = [
        AcSvgStyleUtil.tag('text', textAttrs, layout.tspanMarkup),
        layout.decorationMarkup
    ].join('');
    var localSvg = AcSvgStyleUtil.tag('g', { transform: transformParts.join(' ') }, innerMarkup);
    expandBox(box, bounds, attachment, x, y);
    return { localSvg: localSvg, box: box };
}
function expandBox(box, bounds, attachment, originX, originY) {
    var e_2, _a;
    if (bounds.maxX <= bounds.minX && bounds.maxY <= bounds.minY) {
        return;
    }
    var corners = [
        { x: bounds.minX + attachment.x, y: attachment.y - bounds.minY },
        { x: bounds.maxX + attachment.x, y: attachment.y - bounds.minY },
        { x: bounds.minX + attachment.x, y: attachment.y - bounds.maxY },
        { x: bounds.maxX + attachment.x, y: attachment.y - bounds.maxY }
    ];
    try {
        for (var corners_1 = __values(corners), corners_1_1 = corners_1.next(); !corners_1_1.done; corners_1_1 = corners_1.next()) {
            var corner = corners_1_1.value;
            box.expandByPoint({ x: originX + corner.x, y: originY + corner.y });
        }
    }
    catch (e_2_1) { e_2 = { error: e_2_1 }; }
    finally {
        try {
            if (corners_1_1 && !corners_1_1.done && (_a = corners_1.return)) _a.call(corners_1);
        }
        finally { if (e_2) throw e_2.error; }
    }
}
var MTextSvgLayout = /** @class */ (function () {
    function MTextSvgLayout(baseHeight, lineAdvance, wrapWidth, flowMode, defaultFont, style, traits, ctx) {
        this.baseHeight = baseHeight;
        this.lineAdvance = lineAdvance;
        this.wrapWidth = wrapWidth;
        this.flowMode = flowMode;
        this.defaultFont = defaultFont;
        this.style = style;
        this.traits = traits;
        this.ctx = ctx;
        this.x = 0;
        this.y = 0;
        this.lineStartX = 0;
        this.columnStartY = 0;
        this.columnAdvance = 0;
        this.isLineStart = true;
        this.firstLineOfParagraph = true;
        this.currentLine = null;
        this.lines = [];
        this.decorationLines = [];
        this.activeLineIndex = 0;
        this.minX = 0;
        this.maxX = 0;
        this.minY = 0;
        this.maxY = 0;
        this.firstBaselineY = 0;
        this.hasContent = false;
        this.resetParagraphPosition(new MTextContext());
    }
    Object.defineProperty(MTextSvgLayout.prototype, "tspanMarkup", {
        get: function () {
            var e_3, _a, e_4, _b;
            var markup = '';
            var isFirstLine = true;
            try {
                for (var _c = __values(this.lines), _d = _c.next(); !_d.done; _d = _c.next()) {
                    var line = _d.value;
                    var lineStartX = void 0;
                    var expectedX = void 0;
                    try {
                        for (var _e = (e_4 = void 0, __values(line.spans)), _f = _e.next(); !_f.done; _f = _e.next()) {
                            var span = _f.value;
                            var attrs = this.tspanAttributes(span.tokenCtx, span.fontSize);
                            var isLineStart = lineStartX === undefined;
                            if (this.isVertical()) {
                                attrs.x = String(span.x);
                                attrs.y = String(span.y);
                            }
                            else if (isLineStart) {
                                lineStartX = span.x;
                                attrs.x = String(span.x);
                                attrs.dy = isFirstLine ? '0' : String(this.lineAdvance);
                                isFirstLine = false;
                                expectedX =
                                    span.x + this.measureText(span.text, span.fontSize, span.tokenCtx);
                            }
                            else if (expectedX != null && Math.abs(span.x - expectedX) > 1e-6) {
                                attrs.x = String(span.x);
                                expectedX =
                                    span.x + this.measureText(span.text, span.fontSize, span.tokenCtx);
                            }
                            else {
                                expectedX =
                                    (expectedX !== null && expectedX !== void 0 ? expectedX : span.x) +
                                        this.measureText(span.text, span.fontSize, span.tokenCtx);
                            }
                            markup += AcSvgStyleUtil.tag('tspan', attrs, escapeXml(span.text));
                        }
                    }
                    catch (e_4_1) { e_4 = { error: e_4_1 }; }
                    finally {
                        try {
                            if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
                        }
                        finally { if (e_4) throw e_4.error; }
                    }
                }
            }
            catch (e_3_1) { e_3 = { error: e_3_1 }; }
            finally {
                try {
                    if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
                }
                finally { if (e_3) throw e_3.error; }
            }
            return markup;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(MTextSvgLayout.prototype, "decorationMarkup", {
        get: function () {
            var _this = this;
            return this.decorationLines
                .map(function (line) {
                return AcSvgStyleUtil.tag('line', {
                    x1: String(line.x1),
                    y1: String(line.y1),
                    x2: String(line.x2),
                    y2: String(line.y2),
                    stroke: line.stroke,
                    'stroke-width': String(Math.max(_this.baseHeight * 0.05, 0.05)),
                    'stroke-linecap': 'butt'
                });
            })
                .join('');
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(MTextSvgLayout.prototype, "bounds", {
        get: function () {
            return {
                minX: this.minX,
                maxX: this.maxX,
                minY: this.minY,
                maxY: this.maxY,
                firstBaselineY: this.firstBaselineY
            };
        },
        enumerable: false,
        configurable: true
    });
    MTextSvgLayout.prototype.consume = function (token) {
        var _a;
        switch (token.type) {
            case TokenType.WORD:
                this.emitText(String((_a = token.data) !== null && _a !== void 0 ? _a : ''), token.ctx);
                break;
            case TokenType.SPACE:
                this.emitSpace(token.ctx);
                break;
            case TokenType.NBSP:
                this.emitText('\u00A0', token.ctx);
                break;
            case TokenType.TABULATOR:
                this.advanceToTab(token.ctx);
                break;
            case TokenType.NEW_PARAGRAPH:
                this.newParagraph(token.ctx);
                break;
            case TokenType.NEW_COLUMN:
                this.newColumn(token.ctx);
                break;
            case TokenType.WRAP_AT_DIMLINE:
                this.wrapLine(token.ctx);
                break;
            case TokenType.PROPERTIES_CHANGED:
                this.applyPropertyChange(token.data, token.ctx);
                break;
            case TokenType.STACK:
                if (Array.isArray(token.data) && token.data.length === 3) {
                    this.emitStack(token.data, token.ctx);
                }
                break;
            default:
                break;
        }
    };
    MTextSvgLayout.prototype.finish = function () {
        this.finalizeCurrentLine();
        this.applyLineAlignment();
        this.recomputeBoundsFromLines();
    };
    MTextSvgLayout.prototype.applyPropertyChange = function (item, tokenCtx) {
        var _a, _b;
        if (((_a = item.changes.paragraph) === null || _a === void 0 ? void 0 : _a.align) != null && this.currentLine) {
            this.currentLine.paragraphAlign = item.changes.paragraph.align;
        }
        if (item.command === 'f' || item.command === 'F') {
            this.applyFontFaceChange(item.changes.fontFace, tokenCtx);
        }
        else if (item.command === 'p') {
            if (((_b = item.changes.paragraph) === null || _b === void 0 ? void 0 : _b.indent) != null && this.firstLineOfParagraph) {
                var indent = item.changes.paragraph.indent * this.baseHeight;
                if (this.isHorizontalRtl()) {
                    this.x -= indent;
                }
                else if (!this.isVertical()) {
                    this.x += indent;
                }
            }
        }
    };
    MTextSvgLayout.prototype.applyFontFaceChange = function (fontFace, tokenCtx) {
        var _a;
        if (!(fontFace === null || fontFace === void 0 ? void 0 : fontFace.family)) {
            return;
        }
        var family = normalizeCadFontName(fontFace.family);
        tokenCtx.fontFace = __assign(__assign({}, fontFace), { family: family });
        if (fontFace.style === 'Italic') {
            tokenCtx.oblique = ((_a = this.style.obliqueAngle) !== null && _a !== void 0 ? _a : 0) || 15;
        }
    };
    MTextSvgLayout.prototype.applyLineAlignment = function () {
        var e_5, _a, e_6, _b;
        var _c, _d;
        if (this.wrapWidth == null) {
            return;
        }
        for (var lineIndex = 0; lineIndex < this.lines.length; lineIndex++) {
            var line = this.lines[lineIndex];
            var lineWidth = line.maxX - line.minX;
            if (lineWidth <= 0) {
                continue;
            }
            var tokenCtx = (_d = (_c = line.spans[0]) === null || _c === void 0 ? void 0 : _c.tokenCtx) !== null && _d !== void 0 ? _d : new MTextContext();
            var contentWidth = this.wrapWidth -
                this.paragraphLeft(tokenCtx) -
                this.rightMargin(tokenCtx);
            var shift = 0;
            switch (line.paragraphAlign) {
                case MTextParagraphAlignment.CENTER:
                    shift =
                        line.columnLeft + contentWidth / 2 - (line.minX + line.maxX) / 2;
                    break;
                case MTextParagraphAlignment.RIGHT:
                    shift = line.columnLeft + contentWidth - line.maxX;
                    break;
                case MTextParagraphAlignment.JUSTIFIED:
                case MTextParagraphAlignment.DISTRIBUTED:
                case MTextParagraphAlignment.LEFT:
                case MTextParagraphAlignment.DEFAULT:
                default:
                    shift = 0;
                    break;
            }
            if (shift === 0) {
                continue;
            }
            try {
                for (var _e = (e_5 = void 0, __values(line.spans)), _f = _e.next(); !_f.done; _f = _e.next()) {
                    var span = _f.value;
                    span.x += shift;
                }
            }
            catch (e_5_1) { e_5 = { error: e_5_1 }; }
            finally {
                try {
                    if (_f && !_f.done && (_a = _e.return)) _a.call(_e);
                }
                finally { if (e_5) throw e_5.error; }
            }
            line.minX += shift;
            line.maxX += shift;
            try {
                for (var _g = (e_6 = void 0, __values(this.decorationLines)), _h = _g.next(); !_h.done; _h = _g.next()) {
                    var decoration = _h.value;
                    if (decoration.lineIndex === lineIndex) {
                        decoration.x1 += shift;
                        decoration.x2 += shift;
                    }
                }
            }
            catch (e_6_1) { e_6 = { error: e_6_1 }; }
            finally {
                try {
                    if (_h && !_h.done && (_b = _g.return)) _b.call(_g);
                }
                finally { if (e_6) throw e_6.error; }
            }
        }
    };
    MTextSvgLayout.prototype.recomputeBoundsFromLines = function () {
        var e_7, _a, e_8, _b;
        this.minX = 0;
        this.maxX = 0;
        this.minY = 0;
        this.maxY = 0;
        this.hasContent = false;
        try {
            for (var _c = __values(this.lines), _d = _c.next(); !_d.done; _d = _c.next()) {
                var line = _d.value;
                try {
                    for (var _e = (e_8 = void 0, __values(line.spans)), _f = _e.next(); !_f.done; _f = _e.next()) {
                        var span = _f.value;
                        this.includeSpanBounds(span);
                    }
                }
                catch (e_8_1) { e_8 = { error: e_8_1 }; }
                finally {
                    try {
                        if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
                    }
                    finally { if (e_8) throw e_8.error; }
                }
            }
        }
        catch (e_7_1) { e_7 = { error: e_7_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_7) throw e_7.error; }
        }
    };
    MTextSvgLayout.prototype.emitSpace = function (tokenCtx) {
        var width = this.spaceWidth(tokenCtx);
        if (this.isLineStart) {
            return;
        }
        if (this.isHorizontal()) {
            this.ensureFits(width, tokenCtx);
            if (this.isLineStart) {
                return;
            }
        }
        this.advancePosition(width, this.resolveCapHeight(tokenCtx));
    };
    MTextSvgLayout.prototype.emitText = function (value, tokenCtx) {
        var e_9, _a, e_10, _b;
        if (!value) {
            return;
        }
        if (this.isVertical()) {
            try {
                for (var value_1 = __values(value), value_1_1 = value_1.next(); !value_1_1.done; value_1_1 = value_1.next()) {
                    var char = value_1_1.value;
                    this.emitTextRun(char, tokenCtx);
                }
            }
            catch (e_9_1) { e_9 = { error: e_9_1 }; }
            finally {
                try {
                    if (value_1_1 && !value_1_1.done && (_a = value_1.return)) _a.call(value_1);
                }
                finally { if (e_9) throw e_9.error; }
            }
            return;
        }
        if (this.wrapWidth == null) {
            this.emitTextRun(value, tokenCtx);
            return;
        }
        var fontSize = this.resolveCapHeight(tokenCtx);
        var wordWidth = this.measureText(value, fontSize, tokenCtx);
        if (!this.isLineStart &&
            wordWidth > 0 &&
            this.penOffset(tokenCtx) + wordWidth > this.maxLineWidth(tokenCtx)) {
            this.wrapLine(tokenCtx);
        }
        try {
            for (var value_2 = __values(value), value_2_1 = value_2.next(); !value_2_1.done; value_2_1 = value_2.next()) {
                var char = value_2_1.value;
                this.emitTextRun(char, tokenCtx);
            }
        }
        catch (e_10_1) { e_10 = { error: e_10_1 }; }
        finally {
            try {
                if (value_2_1 && !value_2_1.done && (_b = value_2.return)) _b.call(value_2);
            }
            finally { if (e_10) throw e_10.error; }
        }
    };
    MTextSvgLayout.prototype.emitTextRun = function (value, tokenCtx) {
        var _a;
        if (!value) {
            return;
        }
        var fontSize = this.resolveCapHeight(tokenCtx);
        var width = this.measureText(value, fontSize, tokenCtx);
        var advance = this.isVertical() ? fontSize : width;
        this.ensureFits(advance, tokenCtx);
        var pos = this.currentPosition();
        var span = {
            text: value,
            x: pos.x,
            y: pos.y,
            fontSize: fontSize,
            tokenCtx: tokenCtx.copy()
        };
        (_a = this.currentLine) === null || _a === void 0 ? void 0 : _a.spans.push(span);
        this.includeSpanBounds(span);
        this.advancePosition(width, fontSize);
        this.firstLineOfParagraph = false;
    };
    MTextSvgLayout.prototype.emitStack = function (data, tokenCtx) {
        var _a = __read(data, 3), numerator = _a[0], denominator = _a[1], divider = _a[2];
        var start = this.currentPosition();
        var wasLineStart = this.isLineStart;
        var baseFontSize = this.resolveCapHeight(tokenCtx);
        if (divider === '^') {
            var scriptCtx = tokenCtx.copy();
            scriptCtx.capHeight = {
                value: STACK_SCRIPT_SCALE,
                isRelative: true
            };
            if (numerator && !denominator) {
                this.setPosition(start.x, start.y - baseFontSize * 0.35);
                this.isLineStart = true;
                this.emitText(numerator, scriptCtx);
                var width = this.measureHorizontalSpan(start, this.currentPosition());
                this.setPosition(start.x + width, start.y);
            }
            else if (!numerator && denominator) {
                this.setPosition(start.x, start.y + baseFontSize * 0.2);
                this.isLineStart = true;
                this.emitText(denominator, scriptCtx);
                var width = this.measureHorizontalSpan(start, this.currentPosition());
                this.setPosition(start.x + width, start.y);
            }
            else if (numerator && denominator) {
                this.setPosition(start.x, start.y - baseFontSize * 0.2);
                this.isLineStart = true;
                this.emitText(numerator, scriptCtx);
                var width = this.measureHorizontalSpan(start, this.currentPosition());
                this.setPosition(start.x + width, start.y);
            }
            this.isLineStart = wasLineStart;
            return;
        }
        if (divider === '/') {
            this.emitText("".concat(numerator, "/").concat(denominator), tokenCtx);
            return;
        }
        var numWidth = this.measureText(numerator, baseFontSize, tokenCtx);
        var denWidth = this.measureText(denominator, baseFontSize, tokenCtx);
        var fractionWidth = Math.max(numWidth, denWidth);
        var numOffset = (fractionWidth - numWidth) / 2;
        var denOffset = (fractionWidth - denWidth) / 2;
        var stroke = resolveMTextFill(tokenCtx.color, this.traits, this.ctx);
        this.setPosition(start.x + numOffset, start.y - baseFontSize * 0.35);
        this.isLineStart = true;
        this.emitText(numerator, tokenCtx);
        this.setPosition(start.x + denOffset, start.y + baseFontSize * 0.2);
        this.isLineStart = true;
        this.emitText(denominator, tokenCtx);
        if (divider === '/' || divider === '#') {
            var lineY = start.y -
                baseFontSize * 0.05 +
                this.baseHeight * STACK_VERTICAL_SHIFT_FACTOR;
            this.decorationLines.push({
                x1: start.x,
                y1: lineY,
                x2: start.x + fractionWidth,
                y2: lineY,
                stroke: stroke,
                lineIndex: this.activeLineIndex
            });
        }
        var nextX = this.isHorizontalRtl()
            ? start.x - fractionWidth
            : start.x + fractionWidth;
        this.setPosition(nextX, start.y);
        this.isLineStart = wasLineStart;
    };
    MTextSvgLayout.prototype.newParagraph = function (tokenCtx) {
        this.finalizeCurrentLine();
        if (this.isVertical()) {
            this.newColumn(tokenCtx);
            return;
        }
        this.y += this.lineAdvance;
        this.columnStartY = this.y;
        this.resetParagraphPosition(tokenCtx);
    };
    MTextSvgLayout.prototype.wrapLine = function (tokenCtx) {
        this.finalizeCurrentLine();
        if (this.isVertical()) {
            this.newColumn(tokenCtx);
            return;
        }
        this.y += this.lineAdvance;
        this.columnStartY = this.y;
        this.resetLinePosition(tokenCtx);
    };
    MTextSvgLayout.prototype.newColumn = function (tokenCtx) {
        var _a;
        this.finalizeCurrentLine();
        if (this.isVertical()) {
            this.x += this.columnAdvance > 0 ? this.columnAdvance : this.baseHeight;
            this.columnAdvance = 0;
            if (this.isVerticalTtb()) {
                this.columnStartY = 0;
            }
            this.resetParagraphPosition(tokenCtx);
            return;
        }
        this.x += (_a = this.wrapWidth) !== null && _a !== void 0 ? _a : this.baseHeight * 4;
        this.resetParagraphPosition(tokenCtx);
    };
    MTextSvgLayout.prototype.finalizeCurrentLine = function () {
        if (this.currentLine && this.currentLine.spans.length > 0) {
            this.lines.push(this.currentLine);
            this.activeLineIndex = this.lines.length;
        }
        this.currentLine = null;
    };
    MTextSvgLayout.prototype.resetParagraphPosition = function (tokenCtx) {
        this.firstLineOfParagraph = true;
        this.resetLinePosition(tokenCtx);
    };
    MTextSvgLayout.prototype.resetLinePosition = function (tokenCtx) {
        var left = this.paragraphLeft(tokenCtx);
        var indent = this.firstLineOfParagraph
            ? this.firstLineIndent(tokenCtx)
            : 0;
        var limits = this.lineLimits(tokenCtx);
        if (this.isHorizontalRtl()) {
            this.x = limits.right - indent;
            this.y = this.columnStartY;
        }
        else if (this.isVerticalTtb()) {
            this.x = left;
            this.y = this.columnStartY + indent;
        }
        else if (this.isVerticalBtt()) {
            this.x = left;
            this.y =
                (this.wrapWidth != null ? this.wrapWidth : this.baseHeight * 10) -
                    indent;
            this.columnStartY = this.y;
        }
        else {
            this.x = left + indent;
            this.y = this.columnStartY;
        }
        this.lineStartX = left;
        this.isLineStart = true;
        this.currentLine = {
            spans: [],
            minX: this.x,
            maxX: this.x,
            minY: this.y,
            maxY: this.y,
            baselineY: this.y,
            columnLeft: left,
            paragraphAlign: tokenCtx.paragraph.align
        };
    };
    MTextSvgLayout.prototype.wouldOverflow = function (width, tokenCtx) {
        if (this.wrapWidth == null || width <= 0 || this.isLineStart) {
            return false;
        }
        if (this.isHorizontal()) {
            return this.penOffset(tokenCtx) + width > this.maxLineWidth(tokenCtx);
        }
        var limits = this.verticalLimits(tokenCtx);
        return this.isVerticalTtb()
            ? this.y + width > limits.bottom
            : this.y - width < limits.top;
    };
    MTextSvgLayout.prototype.penOffset = function (tokenCtx) {
        if (this.isHorizontalRtl()) {
            return this.lineLimits(tokenCtx).right - this.x;
        }
        return this.x - this.paragraphLeft(tokenCtx);
    };
    MTextSvgLayout.prototype.maxLineWidth = function (tokenCtx) {
        if (this.wrapWidth == null) {
            return Number.POSITIVE_INFINITY;
        }
        return (this.wrapWidth - this.paragraphLeft(tokenCtx) - this.rightMargin(tokenCtx));
    };
    MTextSvgLayout.prototype.ensureFits = function (width, tokenCtx) {
        if (this.wrapWidth != null &&
            this.isHorizontal() &&
            !this.isLineStart &&
            this.penOffset(tokenCtx) > this.maxLineWidth(tokenCtx)) {
            this.wrapLine(tokenCtx);
        }
        if (!this.wouldOverflow(width, tokenCtx)) {
            return;
        }
        this.wrapLine(tokenCtx);
    };
    MTextSvgLayout.prototype.advancePosition = function (width, fontSize) {
        if (this.isHorizontalRtl()) {
            this.x -= width;
        }
        else if (this.isVerticalTtb()) {
            this.y += fontSize;
        }
        else if (this.isVerticalBtt()) {
            this.y -= fontSize;
        }
        else {
            this.x += width;
        }
        this.isLineStart = false;
        this.columnAdvance = Math.max(this.columnAdvance, width);
    };
    MTextSvgLayout.prototype.advanceToTab = function (tokenCtx) {
        var e_11, _a;
        var tabWidth = this.baseHeight * DEFAULT_TAB_WIDTH_FACTOR;
        var tabs = tokenCtx.paragraph.tabs;
        if (tabs.length === 0) {
            var target = this.isHorizontalRtl()
                ? Math.floor(this.x / tabWidth) * tabWidth
                : Math.ceil((this.x + 1) / tabWidth) * tabWidth;
            var delta = this.isHorizontalRtl() ? this.x - target : target - this.x;
            this.advancePosition(Math.max(0, delta), this.resolveCapHeight(tokenCtx));
            return;
        }
        try {
            for (var tabs_1 = __values(tabs), tabs_1_1 = tabs_1.next(); !tabs_1_1.done; tabs_1_1 = tabs_1.next()) {
                var stop_1 = tabs_1_1.value;
                var pos = this.lineStartX +
                    (typeof stop_1 === 'number'
                        ? stop_1 * this.baseHeight
                        : parseFloat(String(stop_1)) * this.baseHeight);
                if (this.isHorizontalRtl()) {
                    if (!Number.isNaN(pos) && pos < this.x) {
                        this.advancePosition(this.x - pos, this.resolveCapHeight(tokenCtx));
                        return;
                    }
                }
                else if (!Number.isNaN(pos) && pos > this.x) {
                    this.advancePosition(pos - this.x, this.resolveCapHeight(tokenCtx));
                    return;
                }
            }
        }
        catch (e_11_1) { e_11 = { error: e_11_1 }; }
        finally {
            try {
                if (tabs_1_1 && !tabs_1_1.done && (_a = tabs_1.return)) _a.call(tabs_1);
            }
            finally { if (e_11) throw e_11.error; }
        }
        this.advancePosition(tabWidth, this.resolveCapHeight(tokenCtx));
    };
    MTextSvgLayout.prototype.lineLimits = function (tokenCtx) {
        var left = this.paragraphLeft(tokenCtx);
        var rightMargin = tokenCtx.paragraph.right * this.baseHeight;
        var right = this.wrapWidth != null
            ? left + this.wrapWidth - rightMargin
            : Number.POSITIVE_INFINITY;
        return { left: left, right: right };
    };
    MTextSvgLayout.prototype.verticalLimits = function (tokenCtx) {
        if (this.isVerticalBtt()) {
            var bottom_1 = this.columnStartY - tokenCtx.paragraph.right * this.baseHeight;
            var top_1 = this.wrapWidth != null
                ? this.columnStartY - this.wrapWidth + this.firstLineIndent(tokenCtx)
                : Number.NEGATIVE_INFINITY;
            return { top: top_1, bottom: bottom_1 };
        }
        var top = this.columnStartY + this.firstLineIndent(tokenCtx);
        var bottom = this.wrapWidth != null
            ? this.columnStartY +
                this.wrapWidth -
                tokenCtx.paragraph.right * this.baseHeight
            : Number.POSITIVE_INFINITY;
        return { top: top, bottom: bottom };
    };
    MTextSvgLayout.prototype.currentPosition = function () {
        return { x: this.x, y: this.y };
    };
    MTextSvgLayout.prototype.setPosition = function (x, y) {
        this.x = x;
        this.y = y;
    };
    MTextSvgLayout.prototype.includeSpanBounds = function (span) {
        var width = this.measureText(span.text, span.fontSize, span.tokenCtx);
        var spanMinX = this.isHorizontalRtl() ? span.x - width : span.x;
        var spanMaxX = this.isHorizontalRtl() ? span.x : span.x + width;
        var spanMinY = span.y - span.fontSize;
        var spanMaxY = span.y;
        if (!this.hasContent) {
            this.minX = spanMinX;
            this.maxX = spanMaxX;
            this.minY = spanMinY;
            this.maxY = spanMaxY;
            this.firstBaselineY = span.y;
            this.hasContent = true;
        }
        else {
            this.minX = Math.min(this.minX, spanMinX);
            this.maxX = Math.max(this.maxX, spanMaxX);
            this.minY = Math.min(this.minY, spanMinY);
            this.maxY = Math.max(this.maxY, spanMaxY);
        }
        if (this.currentLine) {
            this.currentLine.minX = Math.min(this.currentLine.minX, spanMinX);
            this.currentLine.maxX = Math.max(this.currentLine.maxX, spanMaxX);
            this.currentLine.minY = Math.min(this.currentLine.minY, spanMinY);
            this.currentLine.maxY = Math.max(this.currentLine.maxY, spanMaxY);
        }
    };
    MTextSvgLayout.prototype.measureHorizontalSpan = function (start, end) {
        return Math.abs(end.x - start.x);
    };
    MTextSvgLayout.prototype.spaceWidth = function (tokenCtx) {
        var fontSize = this.resolveCapHeight(tokenCtx);
        return this.measureText(' ', fontSize, tokenCtx);
    };
    MTextSvgLayout.prototype.measureCharWidth = function (char, fontSize, tokenCtx) {
        var widthFactor = this.resolveWidthFactor(tokenCtx);
        var tracking = this.resolveTracking(tokenCtx);
        var baseFactor = isWideCharacter(char)
            ? WIDE_CHAR_WIDTH_FACTOR
            : LATIN_CHAR_WIDTH_FACTOR;
        return fontSize * baseFactor * widthFactor * tracking;
    };
    MTextSvgLayout.prototype.measureText = function (value, fontSize, tokenCtx) {
        var e_12, _a;
        var width = 0;
        try {
            for (var value_3 = __values(value), value_3_1 = value_3.next(); !value_3_1.done; value_3_1 = value_3.next()) {
                var char = value_3_1.value;
                width += this.measureCharWidth(char, fontSize, tokenCtx);
            }
        }
        catch (e_12_1) { e_12 = { error: e_12_1 }; }
        finally {
            try {
                if (value_3_1 && !value_3_1.done && (_a = value_3.return)) _a.call(value_3);
            }
            finally { if (e_12) throw e_12.error; }
        }
        return width;
    };
    MTextSvgLayout.prototype.paragraphLeft = function (tokenCtx) {
        return tokenCtx.paragraph.left * this.baseHeight;
    };
    MTextSvgLayout.prototype.firstLineIndent = function (tokenCtx) {
        return tokenCtx.paragraph.indent * this.baseHeight;
    };
    MTextSvgLayout.prototype.rightMargin = function (tokenCtx) {
        var _a;
        return ((_a = tokenCtx === null || tokenCtx === void 0 ? void 0 : tokenCtx.paragraph.right) !== null && _a !== void 0 ? _a : 0) * this.baseHeight;
    };
    MTextSvgLayout.prototype.resolveCapHeight = function (tokenCtx) {
        var cap = tokenCtx.capHeight;
        var size = cap.isRelative ? this.baseHeight * cap.value : cap.value;
        var cadFont = tokenCtx.fontFace.family || this.defaultFont;
        return size * resolveSvgFontSizeScale(cadFont);
    };
    MTextSvgLayout.prototype.resolveWidthFactor = function (tokenCtx) {
        var _a;
        var wf = tokenCtx.widthFactor;
        if (wf.isRelative) {
            var ref = (_a = this.wrapWidth) !== null && _a !== void 0 ? _a : this.baseHeight * 4;
            return wf.value * ref;
        }
        return wf.value * 0.85;
    };
    MTextSvgLayout.prototype.resolveTracking = function (tokenCtx) {
        var tf = tokenCtx.charTrackingFactor;
        if (tf.isRelative) {
            return tf.value + 1;
        }
        return tf.value;
    };
    MTextSvgLayout.prototype.isHorizontal = function () {
        return (this.flowMode === FlowMode.HorizontalLtr ||
            this.flowMode === FlowMode.HorizontalRtl);
    };
    MTextSvgLayout.prototype.isHorizontalRtl = function () {
        return this.flowMode === FlowMode.HorizontalRtl;
    };
    MTextSvgLayout.prototype.isVertical = function () {
        return (this.flowMode === FlowMode.VerticalTtb ||
            this.flowMode === FlowMode.VerticalBtt);
    };
    MTextSvgLayout.prototype.isVerticalTtb = function () {
        return this.flowMode === FlowMode.VerticalTtb;
    };
    MTextSvgLayout.prototype.isVerticalBtt = function () {
        return this.flowMode === FlowMode.VerticalBtt;
    };
    MTextSvgLayout.prototype.tspanAttributes = function (tokenCtx, fontSize) {
        var attrs = {};
        if (Math.abs(fontSize - this.baseHeight) > 1e-9) {
            attrs['font-size'] = String(fontSize);
        }
        var cadFont = tokenCtx.fontFace.family;
        attrs['font-family'] = resolveSvgFontFamily(cadFont, this.defaultFont);
        if (tokenCtx.bold) {
            attrs['font-weight'] = '700';
        }
        if (tokenCtx.italic) {
            attrs['font-style'] = 'italic';
        }
        var widthFactor = this.resolveWidthFactor(tokenCtx);
        var tracking = this.resolveTracking(tokenCtx);
        if (tracking !== 1) {
            attrs['letter-spacing'] =
                "".concat(((tracking - 1) * fontSize * 0.25).toFixed(3));
        }
        var oblique = tokenCtx.oblique;
        if (oblique !== 0) {
            var transforms = ["skewX(".concat(-oblique, ")")];
            if (widthFactor !== 1) {
                transforms.unshift("scale(".concat(widthFactor, ",1)"));
            }
            attrs.transform = transforms.join(' ');
        }
        else if (widthFactor !== 1) {
            attrs.transform = "scale(".concat(widthFactor, ",1)");
        }
        attrs.fill = resolveMTextFill(tokenCtx.color, this.traits, this.ctx);
        var decorations = [];
        if (tokenCtx.underline) {
            decorations.push('underline');
        }
        if (tokenCtx.overline) {
            decorations.push('overline');
        }
        if (tokenCtx.strikeThrough) {
            decorations.push('line-through');
        }
        if (decorations.length > 0) {
            attrs['text-decoration'] = decorations.join(' ');
        }
        return attrs;
    };
    return MTextSvgLayout;
}());
function resolveMTextFill(color, traits, ctx) {
    if (color.isRgb && color.rgbValue != null) {
        return AcSvgStyleUtil.rgbToHex(color.rgbValue);
    }
    if (color.aci === 256 || color.aci == null) {
        return AcSvgStyleUtil.rgbToHex(AcSvgStyleUtil.resolveRgb(traits, ctx, 'text'));
    }
    if (color.aci === 0) {
        return AcSvgStyleUtil.rgbToHex(AcSvgStyleUtil.resolveRgb(traits, ctx, 'text'));
    }
    var aciColor = new AcCmColor();
    aciColor.colorIndex = color.aci;
    var rgb = aciColor.RGB;
    if (typeof rgb === 'number') {
        return AcSvgStyleUtil.rgbToHex(rgb);
    }
    return AcSvgStyleUtil.rgbToHex(AcSvgStyleUtil.resolveRgb(traits, ctx, 'text'));
}
function escapeXml(s) {
    return s
        .replace(/&/g, '&amp;')
        .replace(/</g, '&lt;')
        .replace(/>/g, '&gt;')
        .replace(/"/g, '&quot;');
}
export { normalizeCadFontName, resolveSvgFontFamily, resolveSvgFontSizeScale, setSvgFontMapping, setSvgFontSizeScales } from './AcSvgFontMap';
//# sourceMappingURL=AcSvgMTextUtil.js.map