import { AcGePoint2d } from '@mlightcad/data-model';
import * as THREE from 'three';
var AcTrCamera = /** @class */ (function () {
    function AcTrCamera(camera) {
        this._camera = camera;
    }
    Object.defineProperty(AcTrCamera.prototype, "position", {
        get: function () {
            return this._camera.position;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrCamera.prototype, "left", {
        get: function () {
            return this._camera.left;
        },
        set: function (value) {
            this._camera.left = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrCamera.prototype, "right", {
        get: function () {
            return this._camera.right;
        },
        set: function (value) {
            this._camera.right = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrCamera.prototype, "top", {
        get: function () {
            return this._camera.top;
        },
        set: function (value) {
            this._camera.top = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrCamera.prototype, "bottom", {
        get: function () {
            return this._camera.bottom;
        },
        set: function (value) {
            this._camera.bottom = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrCamera.prototype, "zoom", {
        get: function () {
            return this._camera.zoom;
        },
        set: function (value) {
            this._camera.zoom = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrCamera.prototype, "internalCamera", {
        /**
         * The internal THREE.js camera.
         */
        get: function () {
            return this._camera;
        },
        enumerable: false,
        configurable: true
    });
    AcTrCamera.prototype.lookAt = function (vector) {
        this._camera.lookAt(vector);
    };
    AcTrCamera.prototype.setRotationFromEuler = function (euler) {
        this._camera.setRotationFromEuler(euler);
    };
    AcTrCamera.prototype.updateProjectionMatrix = function () {
        this._camera.updateProjectionMatrix();
    };
    /**
     * Convert point cooridinate from the screen coordinate system to the world coordinate system.
     * The origin of the screen coordinate system is the left-top corner of the browser.
     * @param point Input point to convert
     * @param width Input width of the browser window
     * @param height Input height of the browser window
     * @returns Return point coordinate in the world coordinate system
     */
    AcTrCamera.prototype.screenToWorld = function (point, width, height) {
        var cwcsPos = new THREE.Vector3(point.x, point.y, 0);
        cwcsPos.x = (point.x / width) * 2 - 1;
        cwcsPos.y = -(point.y / height) * 2 + 1;
        var wcsPos = cwcsPos.unproject(this._camera);
        return new AcGePoint2d(wcsPos.x, wcsPos.y);
    };
    /**
     * Convert point cooridinate from the world coordinate system to the screen coordinate system.
     * The origin of the screen coordinate system is the left-top corner of the browser.
     * @param point Input point to convert
     * @param width Input width of the browser window
     * @param height Input height of the browser window
     * @returns Return point coordinate in the screen coordinate system
     */
    AcTrCamera.prototype.worldToScreen = function (point, width, height) {
        var wcsPos = new THREE.Vector3(point.x, point.y, 0);
        var cwcsPos = wcsPos.project(this._camera);
        return new AcGePoint2d(((cwcsPos.x + 1) / 2) * width, ((-cwcsPos.y + 1) / 2) * height);
    };
    /**
     * Convert point cooridinate from the world coordinate system to the normalized device coordinate.
     * Bottom-left cooridinate in NDC (Normalized screen coordinate) is (-1, -1) and top-right is (1, 1).
     */
    AcTrCamera.prototype.wcs2Ndc = function (point, width, height) {
        var cwcsCoord = this.worldToScreen(point, width, height);
        return this.cwcs2Ndc(cwcsCoord, width, height);
    };
    /**
     * Convert point cooridinate from the client window coordinate system to the normalized device coordinate.
     * Bottom-left cooridinate in NDC (Normalized screen coordinate) is (-1, -1) and top-right is (1, 1).
     */
    AcTrCamera.prototype.cwcs2Ndc = function (point, width, height) {
        return new AcGePoint2d((point.x / width) * 2 - 1, -(point.y / height) * 2 + 1);
    };
    return AcTrCamera;
}());
export { AcTrCamera };
//# sourceMappingURL=AcTrCamera.js.map