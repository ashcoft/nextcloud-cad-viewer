var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { log } from '@mlightcad/data-model';
import * as THREE from 'three';
import { AcTrMTextRenderer } from '../renderer';
import { AcTrMTextColorUtil, AcTrSubEntityTraitsUtil, resolveShapeGlyphKey, resolveShapeTextStyle } from '../util';
import { AcTrBufferGeometryUtil } from '../util/AcTrBufferGeometryUtil';
import { getSceneDrawableUserData, resolveMTextRenderRoot } from '../util/AcTrObjectUserData';
import { AcTrEntity } from './AcTrEntity';
var _raycastBox = /*@__PURE__*/ new THREE.Box3();
var _raycastPoint = /*@__PURE__*/ new THREE.Vector3();
var AcTrShape = /** @class */ (function (_super) {
    __extends(AcTrShape, _super);
    function AcTrShape(shape, traits, style, context, delay) {
        if (delay === void 0) { delay = false; }
        var _this = _super.call(this, context) || this;
        _this._shape = shape;
        _this._style = resolveShapeTextStyle(shape, style, context);
        _this._entityTraits = AcTrMTextColorUtil.snapshotEntityTraits(traits);
        _this._colorSettings = AcTrMTextColorUtil.buildColorSettingsFromTraits(traits, context.styleManager.currentBackgroundColor);
        if (!delay) {
            _this.syncDraw();
        }
        return _this;
    }
    /** Reapplies CAD text materials from the entity traits snapshot. */
    AcTrShape.prototype.refreshTextMaterials = function () {
        this._colorSettings = AcTrMTextColorUtil.buildColorSettingsFromTraits(__assign(__assign({}, AcTrSubEntityTraitsUtil.createDefaultTraits()), { color: this._entityTraits.color, layer: this._entityTraits.layer }), this.renderContext.styleManager.currentBackgroundColor);
        AcTrMTextColorUtil.rematerializeTextHierarchy(this, this._entityTraits, this.renderContext.styleManager);
    };
    /**
     * Builds renderer input with only the glyph key that should be resolved.
     *
     * mtext-renderer falls back from shape name to shape number when both are
     * present; SHAPE entities should use the name exclusively when it is set.
     */
    AcTrShape.prototype.toRenderableShapeData = function () {
        var source = this._shape;
        var _a = resolveShapeGlyphKey(this._shape), byName = _a.byName, byCode = _a.byCode;
        return __assign(__assign({}, source), { name: byName, shapeNumber: byCode });
    };
    AcTrShape.prototype.syncDraw = function () {
        var mtextRenderer = AcTrMTextRenderer.getInstance();
        if (!mtextRenderer)
            return;
        try {
            this._rendered = mtextRenderer.syncRenderShape(this.toRenderableShapeData(), this._style, this._colorSettings);
            this.attachRendered(this._rendered);
        }
        catch (error) {
            log.info("Failed to render shape '".concat(this.describeShape(), "' with the following error:\n"), error);
        }
    };
    AcTrShape.prototype.draw = function () {
        return __awaiter(this, void 0, void 0, function () {
            var mtextRenderer, _a, error_1;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        mtextRenderer = AcTrMTextRenderer.getInstance();
                        if (!mtextRenderer)
                            return [2 /*return*/];
                        _b.label = 1;
                    case 1:
                        _b.trys.push([1, 3, , 4]);
                        _a = this;
                        return [4 /*yield*/, mtextRenderer.asyncRenderShape(this.toRenderableShapeData(), this._style, this._colorSettings)];
                    case 2:
                        _a._rendered = _b.sent();
                        this.attachRendered(this._rendered);
                        return [3 /*break*/, 4];
                    case 3:
                        error_1 = _b.sent();
                        log.info("Failed to render shape '".concat(this.describeShape(), "' with the following error:\n"), error_1);
                        return [3 /*break*/, 4];
                    case 4: return [2 /*return*/];
                }
            });
        });
    };
    AcTrShape.prototype.raycast = function (raycaster, intersects) {
        var _a;
        var previousLength = intersects.length;
        (_a = this._rendered) === null || _a === void 0 ? void 0 : _a.raycast(raycaster, intersects);
        if (intersects.length > previousLength || this.wcsBbox.isEmpty())
            return;
        _raycastBox.copy(this.wcsBbox);
        if (raycaster.ray.intersectBox(_raycastBox, _raycastPoint)) {
            intersects.push({
                distance: raycaster.ray.origin.distanceTo(_raycastPoint),
                point: _raycastPoint.clone(),
                object: this,
                face: null,
                faceIndex: undefined,
                uv: undefined
            });
        }
    };
    AcTrShape.prototype.describeShape = function () {
        var _a, _b;
        return ((_a = this._shape.name) === null || _a === void 0 ? void 0 : _a.trim()) || String((_b = this._shape.shapeNumber) !== null && _b !== void 0 ? _b : '');
    };
    AcTrShape.prototype.resolveDrawMode = function () {
        return this.batchDrawPolicy.resolveDrawMode({
            position: this._shape.position
        });
    };
    AcTrShape.prototype.attachRendered = function (rendered) {
        this.add(rendered);
        var renderRoot = resolveMTextRenderRoot(rendered);
        if (this.resolveDrawMode() === 'unbatch') {
            this.markDrawableUnbatched(renderRoot);
        }
        else {
            this.flatten();
        }
        this.removeInvalidGeometryLeaves();
        this.traverse(function (object) {
            getSceneDrawableUserData(object).bboxIntersectionCheck = true;
        });
        this.updateSelectionBox(rendered);
    };
    AcTrShape.prototype.updateSelectionBox = function (rendered) {
        var geometryBox = this.computeGeometryBox();
        if (geometryBox.isEmpty()) {
            this.wcsBbox = rendered.box;
            return;
        }
        if (!rendered.box.isEmpty() && rendered.box.intersectsBox(geometryBox)) {
            this.wcsBbox = geometryBox.clone().union(rendered.box);
            return;
        }
        this.wcsBbox = geometryBox;
    };
    AcTrShape.prototype.computeGeometryBox = function () {
        var _this = this;
        var box = new THREE.Box3();
        var childBox = new THREE.Box3();
        this.updateMatrixWorld(true);
        this.traverse(function (object) {
            if (!_this.hasGeometry(object))
                return;
            var geometry = object.geometry;
            var boundingBox = AcTrBufferGeometryUtil.safeComputeBoundingBox(geometry);
            if (boundingBox == null)
                return;
            object.updateMatrixWorld(true);
            childBox.copy(boundingBox).applyMatrix4(object.matrixWorld);
            box.union(childBox);
        });
        return box;
    };
    AcTrShape.prototype.removeInvalidGeometryLeaves = function () {
        var e_1, _a;
        var _this = this;
        var _b;
        var invalidObjects = [];
        this.traverse(function (object) {
            if (!_this.hasGeometry(object))
                return;
            if (AcTrBufferGeometryUtil.hasFinitePositions(object.geometry))
                return;
            invalidObjects.push(object);
        });
        try {
            for (var invalidObjects_1 = __values(invalidObjects), invalidObjects_1_1 = invalidObjects_1.next(); !invalidObjects_1_1.done; invalidObjects_1_1 = invalidObjects_1.next()) {
                var object = invalidObjects_1_1.value;
                (_b = object.parent) === null || _b === void 0 ? void 0 : _b.remove(object);
                if (this.hasGeometry(object)) {
                    object.geometry.dispose();
                }
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (invalidObjects_1_1 && !invalidObjects_1_1.done && (_a = invalidObjects_1.return)) _a.call(invalidObjects_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
    };
    AcTrShape.prototype.hasGeometry = function (object) {
        return ('geometry' in object && object.geometry instanceof THREE.BufferGeometry);
    };
    return AcTrShape;
}(AcTrEntity));
export { AcTrShape };
//# sourceMappingURL=AcTrShape.js.map