/**
 * @internal
 */
var AcTrCommonUtil = /** @class */ (function () {
    function AcTrCommonUtil() {
    }
    AcTrCommonUtil.getExtension = function (url) {
        return url.substring(url.lastIndexOf('.') + 1);
    };
    /**
     * Estimates the memory size of an object in bytes.
     * @param {any} obj - The object to estimate.
     * @returns {number} Estimated size in bytes.
     */
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    AcTrCommonUtil.estimateObjectSize = function (obj) {
        var seen = new WeakSet();
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        function sizeOf(value) {
            if (value === null || value === undefined)
                return 0;
            var type = typeof value;
            if (type === 'boolean')
                return 4;
            if (type === 'number')
                return 8;
            if (type === 'string')
                return value.length * 2;
            if (type === 'symbol')
                return 0;
            if (type === 'function')
                return 0;
            if (seen.has(value))
                return 0;
            seen.add(value);
            if (Array.isArray(value)) {
                return value.map(sizeOf).reduce(function (acc, curr) { return acc + curr; }, 0);
            }
            if (type === 'object') {
                var bytes = 0;
                for (var key in value) {
                    if (Object.prototype.hasOwnProperty.call(value, key)) {
                        bytes += key.length * 2; // key is a string
                        bytes += sizeOf(value[key]);
                    }
                }
                return bytes;
            }
            return 0;
        }
        return sizeOf(obj);
    };
    AcTrCommonUtil.getFileName = function (url) {
        return url.split('/').pop();
    };
    AcTrCommonUtil.getFileNameWithoutExtension = function (url) {
        var fileName = AcTrCommonUtil.getFileName(url);
        if (fileName) {
            // Find the last dot to separate the extension, if any
            var dotIndex = fileName.lastIndexOf('.');
            // If no dot is found, return the file name as is
            if (dotIndex === -1) {
                return fileName;
            }
            // Otherwise, return the part before the last dot (file name without extension)
            return fileName.substring(0, dotIndex);
        }
        return url;
    };
    return AcTrCommonUtil;
}());
export { AcTrCommonUtil };
//# sourceMappingURL=AcTrCommonUtil.js.map