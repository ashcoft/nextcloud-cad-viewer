var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { FontManager, ShxParserFont } from '@mlightcad/mtext-renderer';
var SHX_SUFFIX = /\.(shx|ttf|otf|woff2?)$/i;
var EMPTY_TEXT_STYLE = {
    name: '',
    standardFlag: 0,
    fixedTextHeight: 0,
    widthFactor: 1,
    obliqueAngle: 0,
    textGenerationFlag: 0,
    lastHeight: 0,
    font: '',
    bigFont: '',
    extendedFont: ''
};
function normalizeCadFontName(fontName) {
    var trimmed = fontName.trim();
    var base = trimmed.replace(SHX_SUFFIX, '');
    return base.toLowerCase();
}
/**
 * Resolves which SHX glyph key a SHAPE entity should use.
 *
 * Named shapes are looked up by name only; numeric codes are used only when
 * the entity has no shape name.
 */
export function resolveShapeGlyphKey(shape) {
    var _a;
    var name = (_a = shape.name) === null || _a === void 0 ? void 0 : _a.trim();
    if (name) {
        return { byName: name };
    }
    var code = shape.shapeNumber;
    if (code != null && code !== 0) {
        return { byCode: code };
    }
    return {};
}
function hasDrawableShapeInFont(shape, fontName) {
    var normalized = normalizeCadFontName(fontName);
    var size = shape.size;
    var _a = resolveShapeGlyphKey(shape), byName = _a.byName, byCode = _a.byCode;
    var fontManager = FontManager.instance;
    var fromManager = byName
        ? fontManager.getShapeByName(byName, normalized, size)
        : byCode != null
            ? fontManager.getShapeByCode(byCode, normalized, size)
            : undefined;
    if (fromManager) {
        return true;
    }
    var font = fontManager.getFontByName(normalized, false);
    if ((font === null || font === void 0 ? void 0 : font.type) !== 'shx') {
        return false;
    }
    var parser = new ShxParserFont(font.data);
    try {
        var glyph = byName
            ? parser.getShapeByName(byName, size)
            : byCode != null
                ? parser.getCharShape(byCode, size)
                : undefined;
        var polylines = glyph === null || glyph === void 0 ? void 0 : glyph.polylines;
        return !!(polylines === null || polylines === void 0 ? void 0 : polylines.some(function (line) { return line.length >= 2; }));
    }
    finally {
        parser.release();
    }
}
/**
 * Resolves the text style used to render a SHAPE entity.
 *
 * When {@link style} is omitted, searches every shape definition file in the
 * drawing's text style table for a glyph matching {@link shape}.
 */
export function resolveShapeTextStyle(shape, style, context) {
    var e_1, _a;
    var _b;
    if (style != null) {
        var fontName = (_b = style.font) === null || _b === void 0 ? void 0 : _b.trim();
        if (fontName && hasDrawableShapeInFont(shape, fontName)) {
            return __assign({}, style);
        }
    }
    var database = context.database;
    if (!database) {
        return __assign({}, EMPTY_TEXT_STYLE);
    }
    try {
        for (var _c = __values(database.tables.textStyleTable.shapeFiles), _d = _c.next(); !_d.done; _d = _c.next()) {
            var record = _d.value;
            var fontName = record.fileName || record.textStyle.font;
            if (!fontName) {
                continue;
            }
            if (hasDrawableShapeInFont(shape, fontName)) {
                return __assign({}, record.textStyle);
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return __assign({}, EMPTY_TEXT_STYLE);
}
//# sourceMappingURL=AcTrShapeStyleUtil.js.map