/**
 * Util class for Threejs Object
 * @internal
 */
var AcTrObjectUtil = /** @class */ (function () {
    function AcTrObjectUtil() {
    }
    AcTrObjectUtil.isEmptyObject = function (object) {
        var length = object.children.length;
        if (length === 0) {
            var geom = object.geometry; // eslint-disable-line
            if (!geom ||
                !geom.hasAttribute('position') ||
                geom.getAttribute('position').count === 0) {
                return true;
            }
        }
        return false;
    };
    AcTrObjectUtil.removeEmptyObjects = function (object) {
        for (var i = 0; i < object.children.length;) {
            if (!this.removeEmptyObjects(object.children[i])) {
                ++i;
            }
        }
        if (this.isEmptyObject(object)) {
            object.removeFromParent();
            return true;
        }
        return false;
    };
    return AcTrObjectUtil;
}());
export { AcTrObjectUtil };
//# sourceMappingURL=AcTrObjectUtil.js.map