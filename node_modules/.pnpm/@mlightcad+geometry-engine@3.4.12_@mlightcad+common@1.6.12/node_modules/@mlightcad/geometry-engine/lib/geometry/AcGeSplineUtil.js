export function isNonZeroDirection(vector) {
    var _a, _b, _c;
    if (!vector)
        return false;
    var x = (_a = vector.x) !== null && _a !== void 0 ? _a : 0;
    var y = (_b = vector.y) !== null && _b !== void 0 ? _b : 0;
    var z = (_c = vector.z) !== null && _c !== void 0 ? _c : 0;
    return x * x + y * y + z * z > 1e-20;
}
export function resolveControlPointSplineDegree(declaredDegree, controlPointCount, knotCount) {
    var degree = declaredDegree !== null && declaredDegree !== void 0 ? declaredDegree : 0;
    if (degree < 1 && knotCount > 0 && controlPointCount > 0) {
        degree = knotCount - controlPointCount - 1;
    }
    if (degree < 1) {
        degree = 3;
    }
    degree = Math.min(degree, controlPointCount - 1);
    return Math.max(1, degree);
}
export function resolveFitPointSplineDegree(declaredDegree, fitPointCount, tangentCount) {
    var degree = declaredDegree !== null && declaredDegree !== void 0 ? declaredDegree : 0;
    if (degree < 1) {
        degree = 3;
    }
    var maxDegree = fitPointCount + tangentCount - 1;
    degree = Math.min(degree, maxDegree);
    return Math.max(1, degree);
}
export function normalizeSplineWeights(weights, controlPointCount) {
    if (!weights || weights.length !== controlPointCount) {
        return undefined;
    }
    return weights;
}
//# sourceMappingURL=AcGeSplineUtil.js.map