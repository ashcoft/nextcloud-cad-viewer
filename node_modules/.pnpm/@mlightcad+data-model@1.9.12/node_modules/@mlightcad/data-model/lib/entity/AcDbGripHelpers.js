var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
/**
 * Invokes a grip move handler for each valid grip index.
 */
export function acdbForEachGripIndex(indices, moveAtIndex) {
    var e_1, _a;
    try {
        for (var indices_1 = __values(indices), indices_1_1 = indices_1.next(); !indices_1_1.done; indices_1_1 = indices_1.next()) {
            var index = indices_1_1.value;
            if (Number.isInteger(index) && index >= 0) {
                moveAtIndex(index);
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (indices_1_1 && !indices_1_1.done && (_a = indices_1.return)) _a.call(indices_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
}
/**
 * Moves the primary grip point at index 0.
 */
export function acdbMovePrimaryGripPointAt(indices, offset, point) {
    acdbForEachGripIndex(indices, function (index) {
        if (index === 0) {
            point.add(offset);
        }
    });
}
/**
 * Moves one vertex on a 2D polyline geometry object.
 */
export function acdbMovePolyline2dVertexAt(vertices, index, offset) {
    var vertex = vertices[index];
    if (!vertex)
        return;
    vertex.x += offset.x;
    vertex.y += offset.y;
}
/**
 * Moves indexed points in a mutable point array.
 */
export function acdbMovePointArrayGripAt(indices, offset, points) {
    acdbForEachGripIndex(indices, function (index) {
        var point = points[index];
        if (point) {
            point.add(offset);
        }
    });
}
//# sourceMappingURL=AcDbGripHelpers.js.map