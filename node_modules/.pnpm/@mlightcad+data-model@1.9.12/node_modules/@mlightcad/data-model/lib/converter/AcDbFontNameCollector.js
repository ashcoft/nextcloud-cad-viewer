var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
import { DEFAULT_TEXT_STYLE } from '../misc/AcDbConstants';
var MTEXT_INLINE_FONT_PATTERN = /\\f(.*?)\|/g;
/**
 * Collects normalized font names from parsed drawing data before conversion.
 *
 * Host converters supply STYLE table entries plus an adapter that describes
 * each entity's font usage and nested block structure.
 */
var AcDbFontNameCollector = /** @class */ (function () {
    function AcDbFontNameCollector(options) {
        var _a;
        this.styles = options.styles;
        this.styleMap = AcDbFontNameCollector.buildStyleFontMap(this.styles);
        this.textStyleVar = (_a = options.textStyleVar) !== null && _a !== void 0 ? _a : DEFAULT_TEXT_STYLE;
    }
    /**
     * Walks entities and returns all normalized font names referenced by the drawing.
     */
    AcDbFontNameCollector.prototype.collect = function (entities, adapter) {
        var e_1, _a;
        var fonts = new Set();
        try {
            for (var _b = __values(AcDbFontNameCollector.collectShapeDefinitionFonts(this.styles)), _c = _b.next(); !_c.done; _c = _b.next()) {
                var fontName = _c.value;
                fonts.add(fontName);
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_1) throw e_1.error; }
        }
        this.collectFromEntities(entities, adapter, fonts);
        return Array.from(fonts);
    };
    /**
     * Normalizes a font file name for font loading: strips the extension and lowercases.
     */
    AcDbFontNameCollector.normalizeFontFileName = function (fontFileName) {
        if (!fontFileName) {
            return undefined;
        }
        var lastDotIndex = fontFileName.lastIndexOf('.');
        if (lastDotIndex >= 0) {
            return fontFileName.substring(0, lastDotIndex).toLowerCase();
        }
        return fontFileName.toLowerCase();
    };
    AcDbFontNameCollector.prototype.collectFromEntities = function (entities, adapter, fonts) {
        var e_2, _a, e_3, _b;
        try {
            for (var entities_1 = __values(entities), entities_1_1 = entities_1.next(); !entities_1_1.done; entities_1_1 = entities_1.next()) {
                var entity = entities_1_1.value;
                var info = adapter.getEntityFontInfo(entity);
                if (!info) {
                    continue;
                }
                if (info.formattedText) {
                    try {
                        for (var _c = (e_3 = void 0, __values(AcDbFontNameCollector.extractInlineMTextFonts(info.formattedText))), _d = _c.next(); !_d.done; _d = _c.next()) {
                            var fontName = _d.value;
                            fonts.add(fontName);
                        }
                    }
                    catch (e_3_1) { e_3 = { error: e_3_1 }; }
                    finally {
                        try {
                            if (_d && !_d.done && (_b = _c.return)) _b.call(_c);
                        }
                        finally { if (e_3) throw e_3.error; }
                    }
                }
                if (info.resolveStyle) {
                    this.addResolvedStyleFonts(info.styleName, fonts);
                }
                if (info.blockName && adapter.getBlockEntities) {
                    var blockEntities = adapter.getBlockEntities(info.blockName);
                    if (blockEntities) {
                        this.collectFromEntities(blockEntities, adapter, fonts);
                    }
                }
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (entities_1_1 && !entities_1_1.done && (_a = entities_1.return)) _a.call(entities_1);
            }
            finally { if (e_2) throw e_2.error; }
        }
    };
    /**
     * Resolves font names for a text entity style using the same fallbacks as
     * {@link AcDbTextStyleTable.resolveAt}.
     */
    AcDbFontNameCollector.prototype.resolveStyleFontNames = function (styleName) {
        var e_4, _a, e_5, _b;
        var candidates = [];
        var addCandidate = function (value) {
            var trimmed = value === null || value === void 0 ? void 0 : value.trim();
            if (trimmed && !candidates.includes(trimmed)) {
                candidates.push(trimmed);
            }
        };
        addCandidate(styleName);
        addCandidate(this.textStyleVar);
        addCandidate(DEFAULT_TEXT_STYLE);
        addCandidate('STANDARD');
        try {
            for (var candidates_1 = __values(candidates), candidates_1_1 = candidates_1.next(); !candidates_1_1.done; candidates_1_1 = candidates_1.next()) {
                var candidate = candidates_1_1.value;
                var exact = this.styleMap.get(candidate);
                if (exact) {
                    return exact;
                }
                var normalizedCandidate = candidate.toUpperCase();
                try {
                    for (var _c = (e_5 = void 0, __values(this.styleMap)), _d = _c.next(); !_d.done; _d = _c.next()) {
                        var _e = __read(_d.value, 2), key = _e[0], fontNames = _e[1];
                        if (key.toUpperCase() === normalizedCandidate) {
                            return fontNames;
                        }
                    }
                }
                catch (e_5_1) { e_5 = { error: e_5_1 }; }
                finally {
                    try {
                        if (_d && !_d.done && (_b = _c.return)) _b.call(_c);
                    }
                    finally { if (e_5) throw e_5.error; }
                }
            }
        }
        catch (e_4_1) { e_4 = { error: e_4_1 }; }
        finally {
            try {
                if (candidates_1_1 && !candidates_1_1.done && (_a = candidates_1.return)) _a.call(candidates_1);
            }
            finally { if (e_4) throw e_4.error; }
        }
        return this.styleMap.values().next().value;
    };
    AcDbFontNameCollector.prototype.addResolvedStyleFonts = function (styleName, fonts) {
        var fontNames = this.resolveStyleFontNames(styleName);
        fontNames === null || fontNames === void 0 ? void 0 : fontNames.forEach(function (name) { return fonts.add(name); });
    };
    AcDbFontNameCollector.buildStyleFontMap = function (styles) {
        var e_6, _a;
        var styleMap = new Map();
        try {
            for (var styles_1 = __values(styles), styles_1_1 = styles_1.next(); !styles_1_1.done; styles_1_1 = styles_1.next()) {
                var style = styles_1_1.value;
                styleMap.set(style.name, AcDbFontNameCollector.collectStyleEntryFontNames(style));
            }
        }
        catch (e_6_1) { e_6 = { error: e_6_1 }; }
        finally {
            try {
                if (styles_1_1 && !styles_1_1.done && (_a = styles_1.return)) _a.call(styles_1);
            }
            finally { if (e_6) throw e_6.error; }
        }
        return styleMap;
    };
    AcDbFontNameCollector.collectStyleEntryFontNames = function (style) {
        var e_7, _a;
        var fontNames = [];
        try {
            for (var _b = __values([style.font, style.bigFont, style.extendedFont]), _c = _b.next(); !_c.done; _c = _b.next()) {
                var fileName = _c.value;
                var normalized = AcDbFontNameCollector.normalizeFontFileName(fileName);
                if (normalized) {
                    fontNames.push(normalized);
                }
            }
        }
        catch (e_7_1) { e_7 = { error: e_7_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_7) throw e_7.error; }
        }
        return fontNames;
    };
    AcDbFontNameCollector.collectShapeDefinitionFonts = function (styles) {
        var e_8, _a;
        var fonts = [];
        try {
            for (var styles_2 = __values(styles), styles_2_1 = styles_2.next(); !styles_2_1.done; styles_2_1 = styles_2.next()) {
                var style = styles_2_1.value;
                if (style.standardFlag && style.standardFlag & 1) {
                    var normalized = AcDbFontNameCollector.normalizeFontFileName(style.font);
                    if (normalized) {
                        fonts.push(normalized);
                    }
                }
            }
        }
        catch (e_8_1) { e_8 = { error: e_8_1 }; }
        finally {
            try {
                if (styles_2_1 && !styles_2_1.done && (_a = styles_2.return)) _a.call(styles_2);
            }
            finally { if (e_8) throw e_8.error; }
        }
        return fonts;
    };
    AcDbFontNameCollector.extractInlineMTextFonts = function (text) {
        var e_9, _a;
        var fonts = [];
        try {
            for (var _b = __values(text.matchAll(MTEXT_INLINE_FONT_PATTERN)), _c = _b.next(); !_c.done; _c = _b.next()) {
                var match = _c.value;
                fonts.push(match[1].toLowerCase());
            }
        }
        catch (e_9_1) { e_9 = { error: e_9_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_9) throw e_9.error; }
        }
        return fonts;
    };
    return AcDbFontNameCollector;
}());
export { AcDbFontNameCollector };
//# sourceMappingURL=AcDbFontNameCollector.js.map