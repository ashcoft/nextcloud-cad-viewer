var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
/**
 * Converts CAD 4×4 matrices to SVG transforms and transforms 2D boxes.
 */
var AcSvgMatrixUtil = /** @class */ (function () {
    function AcSvgMatrixUtil() {
    }
    /**
     * SVG `matrix(a,b,c,d,e,f)` for 2D geometry (CAD Y-up, applied before root Y-flip).
     */
    AcSvgMatrixUtil.toSvgTransform = function (matrix) {
        var e = matrix.elements;
        return "matrix(".concat(e[0], ",").concat(e[1], ",").concat(e[4], ",").concat(e[5], ",").concat(e[12], ",").concat(e[13], ")");
    };
    AcSvgMatrixUtil.transformPoint = function (matrix, point) {
        var _a;
        var e = matrix.elements;
        var x = point.x;
        var y = point.y;
        var z = (_a = point.z) !== null && _a !== void 0 ? _a : 0;
        var w = e[3] * x + e[7] * y + e[11] * z + e[15];
        var invW = w === 0 ? 1 : 1 / w;
        return {
            x: (e[0] * x + e[4] * y + e[8] * z + e[12]) * invW,
            y: (e[1] * x + e[5] * y + e[9] * z + e[13]) * invW
        };
    };
    AcSvgMatrixUtil.transformBox = function (box, matrix) {
        var e_1, _a;
        if (box.isEmpty()) {
            return;
        }
        var min = box.min;
        var max = box.max;
        var corners = [
            { x: min.x, y: min.y, z: 0 },
            { x: max.x, y: min.y, z: 0 },
            { x: max.x, y: max.y, z: 0 },
            { x: min.x, y: max.y, z: 0 }
        ];
        var minX = Infinity;
        var minY = Infinity;
        var maxX = -Infinity;
        var maxY = -Infinity;
        try {
            for (var corners_1 = __values(corners), corners_1_1 = corners_1.next(); !corners_1_1.done; corners_1_1 = corners_1.next()) {
                var corner = corners_1_1.value;
                var p = this.transformPoint(matrix, corner);
                minX = Math.min(minX, p.x);
                minY = Math.min(minY, p.y);
                maxX = Math.max(maxX, p.x);
                maxY = Math.max(maxY, p.y);
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (corners_1_1 && !corners_1_1.done && (_a = corners_1.return)) _a.call(corners_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
        box.min.set(minX, minY);
        box.max.set(maxX, maxY);
    };
    return AcSvgMatrixUtil;
}());
export { AcSvgMatrixUtil };
//# sourceMappingURL=AcSvgMatrixUtil.js.map