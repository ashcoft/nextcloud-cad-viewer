import { AcGeArea2d, AcGeCircArc3d, AcGeEllipseArc3d, AcGePoint3d, AcGePoint3dLike, AcGiContext, AcGiFontMapping, AcGiImageStyle, AcGiMTextData, AcGiPointStyle, AcGiRenderer, AcGiShapeData, AcGiSubEntityTraits, AcGiTextStyle } from '@mlightcad/data-model';
import { AcSvgEntity } from './AcSvgEntity';
export declare class AcSvgRenderer implements AcGiRenderer<AcSvgEntity> {
    /**
     * Clears the shared block rendering cache before SVG/PDF export.
     *
     * The cache stores drawable objects from the last renderer that populated it
     * (typically Three.js). Reusing those entries during export causes failures
     * such as `renderSvg is not a function` when dimensions or block references
     * are resolved from cache.
     */
    static prepareExport(): void;
    private _entities;
    private _bbox;
    private _subEntityTraits;
    private _fontMapping;
    private _ltscale;
    private _celtscale;
    private _currentBackgroundColor;
    private _foregroundColor;
    private _showLineWeight;
    private _pendingImages;
    constructor();
    /**
     * @inheritdoc
     */
    get subEntityTraits(): AcGiSubEntityTraits;
    /**
     * @inheritdoc
     */
    get context(): AcGiContext;
    /**
     * @inheritdoc
     */
    setFontMapping(mapping: AcGiFontMapping): void;
    /**
     * Sets global ltscale for linetype dash scaling.
     */
    set ltscale(scale: number);
    /**
     * Sets global celtscale for linetype dash scaling.
     */
    set celtscale(scale: number);
    /**
     * Canvas background colour tracked for ACI 7 resolution and SVG export.
     *
     * Mirrors {@link AcTrRenderer.currentBackgroundColor}.
     */
    get currentBackgroundColor(): number;
    set currentBackgroundColor(value: number);
    /**
     * Foreground colour used when resolving ACI 7 linework and patterned hatches.
     * Mirrors {@link AcTrRenderer.changeForeground}.
     */
    changeForeground(color: number): void;
    /**
     * Whether lineweights are rendered. Mirrors the LWDISPLAY system variable.
     */
    get showLineWeight(): boolean;
    set showLineWeight(value: boolean);
    private get styleContext();
    private pushEntity;
    private removeEntities;
    /**
     * @inheritdoc
     */
    group(entities: AcSvgEntity[]): AcSvgEntity;
    /**
     * @inheritdoc
     */
    point(point: AcGePoint3d, style: AcGiPointStyle): AcSvgEntity;
    /**
     * @inheritdoc
     */
    circularArc(arc: AcGeCircArc3d): AcSvgEntity;
    /**
     * @inheritdoc
     */
    ellipticalArc(ellipseArc: AcGeEllipseArc3d): AcSvgEntity;
    /**
     * @inheritdoc
     */
    lines(points: AcGePoint3dLike[]): AcSvgEntity;
    /**
     * @inheritdoc
     */
    lineSegments(array: Float32Array, itemSize: number, indices: Uint16Array): AcSvgEntity;
    /**
     * @inheritdoc
     */
    area(area: AcGeArea2d): AcSvgEntity;
    /**
     * @inheritdoc
     */
    mtext(mtext: AcGiMTextData, style: AcGiTextStyle, _delay?: boolean): AcSvgEntity;
    /**
     * @inheritdoc
     */
    shape(shape: AcGiShapeData, style: AcGiTextStyle, _delay?: boolean): AcSvgEntity;
    /**
     * @inheritdoc
     */
    image(blob: Blob, style: AcGiImageStyle): AcSvgEntity;
    /**
     * Exports accumulated SVG markup. Awaits any pending raster images first.
     */
    exportAsync(): Promise<string>;
    /**
     * Synchronous export. Raster images added via {@link image} may be missing
     * unless {@link exportAsync} is used.
     */
    export(): string;
    private buildBackgroundRect;
}
//# sourceMappingURL=AcSvgRenderer.d.ts.map