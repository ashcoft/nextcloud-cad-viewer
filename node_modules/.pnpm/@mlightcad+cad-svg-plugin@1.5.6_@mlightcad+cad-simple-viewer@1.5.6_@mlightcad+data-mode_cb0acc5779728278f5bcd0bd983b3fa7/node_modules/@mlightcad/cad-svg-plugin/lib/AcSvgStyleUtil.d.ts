import { AcGiSubEntityTraits } from '@mlightcad/data-model';
/** Runtime style options passed from {@link AcSvgRenderer}. */
export interface AcSvgStyleContext {
    ltscale: number;
    celtscale: number;
    /** Canvas / export background (24-bit RGB). Used for ACI 7 solid hatches. */
    backgroundColor: number;
    /** Resolved foreground colour for ACI 7 linework (24-bit RGB). */
    foregroundColor: number;
    /** Mirrors LWDISPLAY: when false, lineweights are not rendered. */
    showLineWeight: boolean;
}
export type AcSvgPrimitiveKind = 'line' | 'fill' | 'text' | 'point';
/**
 * Converts entity traits and export context into SVG presentation attributes.
 */
export declare class AcSvgStyleUtil {
    static rgbToHex(rgb: number): string;
    static resolveRgb(traits: AcGiSubEntityTraits, ctx: AcSvgStyleContext, kind: AcSvgPrimitiveKind): number;
    static strokeAttributes(traits: AcGiSubEntityTraits, ctx: AcSvgStyleContext): Record<string, string>;
    static fillAttributes(traits: AcGiSubEntityTraits, ctx: AcSvgStyleContext): Record<string, string>;
    static textAttributes(traits: AcGiSubEntityTraits, ctx: AcSvgStyleContext): Record<string, string>;
    static pointAttributes(traits: AcGiSubEntityTraits, ctx: AcSvgStyleContext): Record<string, string>;
    static formatAttributes(attrs: Record<string, string>): string;
    static tag(name: string, attrs: Record<string, string>, inner?: string): string;
    private static isSolidBackgroundHatch;
    private static resolveStrokeWidth;
    private static resolveOpacity;
    private static strokeDasharray;
    private static patternToDashSegments;
}
//# sourceMappingURL=AcSvgStyleUtil.d.ts.map