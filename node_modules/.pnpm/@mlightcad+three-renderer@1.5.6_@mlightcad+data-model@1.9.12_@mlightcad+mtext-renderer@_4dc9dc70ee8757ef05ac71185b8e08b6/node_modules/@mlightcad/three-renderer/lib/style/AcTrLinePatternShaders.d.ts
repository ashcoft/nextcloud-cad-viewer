import { AcGiLineTypePatternElement } from '@mlightcad/data-model';
import * as THREE from 'three';
export declare class AcTrLinePatternShaders {
    /**
     * Creates line shader by given pattern.
     * Note: remember to call line.computeLineDistances() in order to make it work!
     */
    static createLineShaderMaterial(pattern: AcGiLineTypePatternElement[], color: number, scale: number, viewportScaleUniform: number, cameraZoomUniform: {
        value: number;
    }): THREE.Material;
    /**
     * Creates a linetype shader from pre-scaled pattern values.
     * Used by HTML export playback where pattern uniforms are serialized directly.
     */
    static createLineShaderMaterialFromScaledPattern(pattern: number[], patternLength: number, color: number, viewportScaleUniform: number, cameraZoomUniform: {
        value: number;
    }): THREE.Material;
}
//# sourceMappingURL=AcTrLinePatternShaders.d.ts.map