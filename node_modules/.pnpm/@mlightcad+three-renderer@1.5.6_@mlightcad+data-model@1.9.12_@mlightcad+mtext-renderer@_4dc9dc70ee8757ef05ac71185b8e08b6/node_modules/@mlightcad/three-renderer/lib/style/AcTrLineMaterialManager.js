var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { AcGiLineWeight } from '@mlightcad/data-model';
import * as THREE from 'three';
import { LineMaterial } from 'three/examples/jsm/lines/LineMaterial.js';
import { AcTrLinePatternShaders } from './AcTrLinePatternShaders';
import { AcTrMaterialManager } from './AcTrMaterialManager';
/**
 * Material manager responsible for creating and caching all line materials.
 *
 * This class supports:
 * - Simple non-patterned lines (THREE.LineBasicMaterial)
 * - Patterned/dashed lines (custom shader materials)
 *
 * Materials are cached by computed keys. Each material also remembers:
 * - The CAD layer it belongs to
 * - Whether its certain property value (for example color or linetype) is ByLayer
 *
 * Layer updates then locate materials using these userData flags instead of
 * maintaining a separate layer → key mapping.
 */
var AcTrLineMaterialManager = /** @class */ (function (_super) {
    __extends(AcTrLineMaterialManager, _super);
    function AcTrLineMaterialManager() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    /**
     * Builds a stable material key from traits.
     * Key differs for shader vs basic, ByLayer vs ByEntity.
     */
    AcTrLineMaterialManager.prototype.buildKey = function (traits, options) {
        var hasByLayerKeyTraits = this.hasByLayerKeyTraits(traits);
        var lineWidth = this.resolveLineWidth(traits.lineWeight);
        var mode = this.getMaterialMode(traits, options);
        var drawOrderSuffix = this.buildDrawOrderSuffix(traits);
        var rgb = this.resolveTraitsRgb(traits);
        if (mode === 'shader') {
            return hasByLayerKeyTraits
                ? "layer_".concat(mode, "_").concat(traits.layer, "_").concat(traits.lineType.name, "_").concat(rgb, "_").concat(traits.lineTypeScale, "_").concat(lineWidth).concat(drawOrderSuffix)
                : "entity_".concat(mode, "_").concat(traits.lineType.name, "_").concat(rgb, "_").concat(traits.lineTypeScale, "_").concat(lineWidth).concat(drawOrderSuffix);
        }
        return hasByLayerKeyTraits
            ? "layer_".concat(mode, "_").concat(traits.layer, "_").concat(rgb, "_").concat(lineWidth).concat(drawOrderSuffix)
            : "entity_".concat(mode, "_").concat(rgb, "_").concat(lineWidth).concat(drawOrderSuffix);
    };
    /** Returns true if a shader material is required. */
    AcTrLineMaterialManager.prototype.isShaderMaterial = function (traits, options) {
        return !!(!options.basicMaterialOnly &&
            traits.lineType.pattern &&
            traits.lineType.pattern.length > 0);
    };
    AcTrLineMaterialManager.prototype.getMaterialMode = function (traits, options) {
        if (this.isShaderMaterial(traits, options))
            return 'shader';
        return options.basicMaterialOnly ? 'basic' : 'fat';
    };
    AcTrLineMaterialManager.prototype.createMaterialImpl = function (traits, options) {
        if (options === void 0) { options = {}; }
        var material;
        var rgb = this.resolveTraitsRgb(traits);
        var scales = this.getLineTypeScales();
        var scale = scales.ltscale * scales.celtscale * traits.lineTypeScale;
        if (this.isShaderMaterial(traits, options)) {
            material = AcTrLinePatternShaders.createLineShaderMaterial(traits.lineType.pattern, rgb, scale, this.options.viewportScaleUniform, AcTrMaterialManager.CameraZoomUniform);
        }
        else if (options.basicMaterialOnly || traits.lineWeight < 0) {
            material = new THREE.LineBasicMaterial({
                color: rgb
            });
        }
        else {
            var fatLineMaterial = new LineMaterial({
                color: rgb,
                linewidth: this.resolveLineWidth(traits.lineWeight)
            });
            fatLineMaterial.resolution.copy(this.options.resolution);
            material = fatLineMaterial;
        }
        return material;
    };
    AcTrLineMaterialManager.prototype.hasByLayerKeyTraits = function (traits) {
        return (_super.prototype.hasByLayerKeyTraits.call(this, traits) ||
            traits.lineWeight === AcGiLineWeight.ByLayer);
    };
    AcTrLineMaterialManager.prototype.resolveLineWidth = function (lineWeight) {
        if (lineWeight < 0)
            return 1;
        return Math.max(1, lineWeight / 40);
    };
    AcTrLineMaterialManager.prototype.updateResolution = function () {
        var resolution = this.options.resolution;
        Object.values(this.cache).forEach(function (material) {
            if (material instanceof LineMaterial) {
                material.resolution.copy(resolution);
            }
        });
    };
    //References https://knowledge.autodesk.com/support/autocad-lt/learn-explore/caas/CloudHelp/cloudhelp/2020/ENU/AutoCAD-LT/files/GUID-4323BBAD-2757-4E92-B2E4-E0E550BB37CB-htm.html
    AcTrLineMaterialManager.prototype.getLineTypeScales = function () {
        return {
            ltscale: this.options.ltscale || 1.0,
            celtscale: this.options.celtscale || 1.0
        };
    };
    return AcTrLineMaterialManager;
}(AcTrMaterialManager));
export { AcTrLineMaterialManager };
//# sourceMappingURL=AcTrLineMaterialManager.js.map