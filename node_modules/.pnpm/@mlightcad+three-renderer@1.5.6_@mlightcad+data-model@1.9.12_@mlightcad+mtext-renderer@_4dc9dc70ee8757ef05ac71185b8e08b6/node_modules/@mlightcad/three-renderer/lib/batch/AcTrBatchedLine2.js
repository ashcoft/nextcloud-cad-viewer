var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import * as THREE from 'three';
import { LineSegments2 } from 'three/examples/jsm/lines/LineSegments2.js';
import { LineSegmentsGeometry } from 'three/examples/jsm/lines/LineSegmentsGeometry.js';
import { AcTrBufferGeometryUtil } from '../util/AcTrBufferGeometryUtil';
import { copyArrayContents, copyAttributeData, isBatchGeometryActive, isBatchGeometryVisible } from './AcTrBatchedGeometryInfo';
import { assertReservedCapacity, createAcTrBatchedMixin, createGeometryState, growCapacityIfNeeded, reserveGeometryId, resolveReservedCount } from './AcTrBatchedMixin';
import { syncBatchDrawVisibilityAfterOptimize } from './drawVisibility';
var _box = /*@__PURE__*/ new THREE.Box3();
var _vector = /*@__PURE__*/ new THREE.Vector3();
var _vector2 = /*@__PURE__*/ new THREE.Vector3();
var _batchIntersects = [];
var _raycastObject = /*@__PURE__*/ new LineSegments2(new LineSegmentsGeometry());
var AcTrBatchedLine2Base = createAcTrBatchedMixin(LineSegments2, {
    typeName: 'AcTrBatchedLine2',
    createObject: function () { return new LineSegments2(new LineSegmentsGeometry()); },
    getDrawRange: function (_instance, geometryInfo) { return ({
        start: geometryInfo.vertexStart,
        count: geometryInfo.vertexCount
    }); }
});
/**
 * Batched renderer for `LineSegments2` wide-line geometries.
 *
 * This class packs many `LineSegmentsGeometry` segment ranges into one shared
 * GPU buffer and renders them via one `LineSegments2` object per material.
 */
var AcTrBatchedLine2 = /** @class */ (function (_super) {
    __extends(AcTrBatchedLine2, _super);
    function AcTrBatchedLine2(maxSegmentCount, material) {
        if (maxSegmentCount === void 0) { maxSegmentCount = 1000; }
        var _this = _super.call(this, new LineSegmentsGeometry(), material) || this;
        /** Next free segment offset for appended geometries. */
        _this._nextSegmentStart = 0;
        /** Whether packed segment buffers have been allocated. */
        _this._geometryInitialized = false;
        _this.frustumCulled = false;
        _this._maxSegmentCount = maxSegmentCount;
        return _this;
    }
    Object.defineProperty(AcTrBatchedLine2.prototype, "geometryCount", {
        get: function () {
            return this._geometryCount;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrBatchedLine2.prototype, "unusedSegmentCount", {
        get: function () {
            return this._maxSegmentCount - this._nextSegmentStart;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrBatchedLine2.prototype, "origin", {
        /** World-space origin used when rebasing packed segment data, if established. */
        get: function () {
            return this._origin;
        },
        enumerable: false,
        configurable: true
    });
    AcTrBatchedLine2.prototype._initializeGeometry = function (reference) {
        if (this._geometryInitialized) {
            return;
        }
        ;
        this.geometry.setPositions(new Float32Array(this._maxSegmentCount * 6));
        this._copyStaticAttributes(reference);
        this._geometryInitialized = true;
    };
    AcTrBatchedLine2.prototype._copyStaticAttributes = function (reference, target) {
        if (target === void 0) { target = this.geometry; }
        var dstGeometry = target;
        var srcIndex = reference.getIndex();
        if (srcIndex) {
            dstGeometry.setIndex(srcIndex.clone());
        }
        for (var key in reference.attributes) {
            if (key === 'instanceStart' || key === 'instanceEnd')
                continue;
            dstGeometry.setAttribute(key, reference.getAttribute(key).clone());
        }
    };
    AcTrBatchedLine2.prototype._validateGeometry = function (geometry) {
        var start = geometry.getAttribute('instanceStart');
        var end = geometry.getAttribute('instanceEnd');
        if (!start || !end) {
            throw new Error('AcTrBatchedLine2: Geometry must have "instanceStart" and "instanceEnd" attributes.');
        }
        if (start.itemSize !== 3 ||
            end.itemSize !== 3 ||
            start.count !== end.count) {
            throw new Error('AcTrBatchedLine2: Invalid line segment attributes. Expected matching vec3 instanceStart/instanceEnd.');
        }
    };
    AcTrBatchedLine2.prototype._resizeSpaceIfNeeded = function (geometry) {
        var segmentCount = geometry.getAttribute('instanceStart').count;
        var newMaxSegmentCount = growCapacityIfNeeded({
            currentMaxCount: this._maxSegmentCount,
            nextStart: this._nextSegmentStart,
            requiredCount: segmentCount,
            growthFactor: AcTrBatchedLine2.GROWTH_FACTOR
        });
        if (newMaxSegmentCount > this._maxSegmentCount) {
            this.setGeometrySize(newMaxSegmentCount);
        }
    };
    /**
     * Resets batched state and releases current geometry buffers.
     */
    AcTrBatchedLine2.prototype.reset = function () {
        this.boundingBox = null;
        this.boundingSphere = null;
        this._geometryInfo = [];
        this._availableGeometryIds = [];
        this._nextSegmentStart = 0;
        this._geometryCount = 0;
        this._origin = undefined;
        this.position.set(0, 0, 0);
        this._geometryInitialized = false;
        this.geometry.dispose();
    };
    /**
     * Appends one source line-segment geometry into the packed segment buffer.
     */
    AcTrBatchedLine2.prototype.addGeometry = function (geometry, reservedSegmentCount, worldOffset) {
        if (reservedSegmentCount === void 0) { reservedSegmentCount = -1; }
        if (worldOffset === void 0) { worldOffset = new THREE.Vector3(); }
        this.rebaseGeometryInPlace(geometry, worldOffset);
        this._initializeGeometry(geometry);
        this._validateGeometry(geometry);
        this._resizeSpaceIfNeeded(geometry);
        var segmentCount = geometry.getAttribute('instanceStart').count;
        var geometryInfo = __assign({ vertexStart: this._nextSegmentStart, vertexCount: -1, reservedVertexCount: resolveReservedCount(reservedSegmentCount, segmentCount) }, createGeometryState());
        assertReservedCapacity({
            typeName: 'AcTrBatchedLine2',
            maxVertexCount: this._maxSegmentCount,
            vertexStart: geometryInfo.vertexStart,
            reservedVertexCount: geometryInfo.reservedVertexCount
        });
        var _a = reserveGeometryId(this._availableGeometryIds, this._geometryInfo, this._geometryCount, geometryInfo), geometryId = _a.geometryId, geometryCount = _a.geometryCount;
        this._geometryCount = geometryCount;
        this.setGeometryAt(geometryId, geometry);
        this._nextSegmentStart =
            geometryInfo.vertexStart + geometryInfo.reservedVertexCount;
        this._syncDrawRange();
        return geometryId;
    };
    AcTrBatchedLine2.prototype.rebaseGeometryInPlace = function (geometry, worldOffset) {
        var start = geometry.getAttribute('instanceStart');
        var end = geometry.getAttribute('instanceEnd');
        if (!start || !end) {
            return;
        }
        if (!this._origin) {
            AcTrBufferGeometryUtil.safeComputeBoundingBox(geometry);
            var center = geometry.boundingBox
                ? geometry.boundingBox.getCenter(new THREE.Vector3())
                : new THREE.Vector3();
            this._origin = center.add(worldOffset.clone());
            this.position.copy(this._origin);
        }
        var origin = this._origin;
        if (!origin) {
            return;
        }
        for (var i = 0; i < start.count; i++) {
            start.setXYZ(i, start.getX(i) + worldOffset.x - origin.x, start.getY(i) + worldOffset.y - origin.y, start.getZ(i) + worldOffset.z - origin.z);
            end.setXYZ(i, end.getX(i) + worldOffset.x - origin.x, end.getY(i) + worldOffset.y - origin.y, end.getZ(i) + worldOffset.z - origin.z);
        }
        start.needsUpdate = true;
        end.needsUpdate = true;
    };
    /**
     * Assigns entity metadata for a packed geometry id.
     */
    AcTrBatchedLine2.prototype.setGeometryInfo = function (geometryId, userData) {
        if (geometryId >= this._geometryCount) {
            throw new Error('AcTrBatchedLine2: Maximum geometry count reached.');
        }
        var geometryInfo = this._geometryInfo[geometryId];
        geometryInfo.objectId = userData.objectId;
        geometryInfo.bboxIntersectionCheck = userData.bboxIntersectionCheck;
    };
    /**
     * Rewrites segment data for an existing packed geometry id.
     */
    AcTrBatchedLine2.prototype.setGeometryAt = function (geometryId, geometry) {
        if (geometryId >= this._geometryCount) {
            throw new Error('AcTrBatchedLine2: Maximum geometry count reached.');
        }
        this._validateGeometry(geometry);
        var geometryInfo = this._geometryInfo[geometryId];
        var segmentCount = geometry.getAttribute('instanceStart').count;
        if (segmentCount > geometryInfo.reservedVertexCount) {
            throw new Error('AcTrBatchedLine2: Reserved space not large enough for provided geometry.');
        }
        var dstStart = this.geometry.getAttribute('instanceStart');
        var dstEnd = this.geometry.getAttribute('instanceEnd');
        var srcStart = geometry.getAttribute('instanceStart');
        var srcEnd = geometry.getAttribute('instanceEnd');
        var segmentStart = geometryInfo.vertexStart;
        copyAttributeData(srcStart, dstStart, segmentStart);
        copyAttributeData(srcEnd, dstEnd, segmentStart);
        for (var i = segmentCount, l = geometryInfo.reservedVertexCount; i < l; i++) {
            var index = segmentStart + i;
            for (var c = 0; c < 3; c++) {
                dstStart.setComponent(index, c, 0);
                dstEnd.setComponent(index, c, 0);
            }
        }
        dstStart.needsUpdate = true;
        dstEnd.needsUpdate = true;
        geometryInfo.vertexCount = segmentCount;
        geometryInfo.boundingBox = null;
        return geometryId;
    };
    /**
     * Compacts active segment ranges to remove holes left by deletions.
     */
    AcTrBatchedLine2.prototype.optimize = function () {
        var e_1, _a;
        var packed = this._getPackedSegmentArray();
        var nextSegmentStart = 0;
        var entries = this._geometryInfo
            .map(function (info, id) { return ({ info: info, id: id }); })
            .filter(function (e) { return isBatchGeometryActive(e.info.flags); })
            .sort(function (a, b) { return a.info.vertexStart - b.info.vertexStart; });
        try {
            for (var entries_1 = __values(entries), entries_1_1 = entries_1.next(); !entries_1_1.done; entries_1_1 = entries_1.next()) {
                var info = entries_1_1.value.info;
                var oldStart = info.vertexStart;
                var count = info.reservedVertexCount;
                if (oldStart !== nextSegmentStart) {
                    packed.copyWithin(nextSegmentStart * 6, oldStart * 6, (oldStart + count) * 6);
                }
                info.vertexStart = nextSegmentStart;
                nextSegmentStart += count;
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (entries_1_1 && !entries_1_1.done && (_a = entries_1.return)) _a.call(entries_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
        for (var i = nextSegmentStart * 6, l = packed.length; i < l; i++) {
            packed[i] = 0;
        }
        this._nextSegmentStart = nextSegmentStart;
        this._syncDrawRange();
        this._availableGeometryIds.length = 0;
        var instanceStart = this.geometry.getAttribute('instanceStart');
        var instanceEnd = this.geometry.getAttribute('instanceEnd');
        instanceStart.needsUpdate = true;
        instanceEnd.needsUpdate = true;
        syncBatchDrawVisibilityAfterOptimize(this.geometry, this._geometryInfo);
        return this;
    };
    /**
     * Returns cached per-geometry bounds, computing lazily when missing.
     */
    AcTrBatchedLine2.prototype.getBoundingBoxAt = function (geometryId, target) {
        if (geometryId >= this._geometryCount) {
            return null;
        }
        var geometryInfo = this._geometryInfo[geometryId];
        if (geometryInfo.boundingBox === null) {
            var box = new THREE.Box3();
            var start = this.geometry.getAttribute('instanceStart');
            var end = this.geometry.getAttribute('instanceEnd');
            for (var i = geometryInfo.vertexStart, l = geometryInfo.vertexStart + geometryInfo.vertexCount; i < l; i++) {
                box.expandByPoint(_vector.fromBufferAttribute(start, i));
                box.expandByPoint(_vector2.fromBufferAttribute(end, i));
            }
            geometryInfo.boundingBox = box;
        }
        target.copy(geometryInfo.boundingBox);
        return target;
    };
    /**
     * Returns per-geometry bounding sphere derived from cached bounding box.
     */
    AcTrBatchedLine2.prototype.getBoundingSphereAt = function (geometryId, target) {
        if (geometryId >= this._geometryCount) {
            return null;
        }
        this.getBoundingBoxAt(geometryId, _box);
        _box.getBoundingSphere(target);
        return target;
    };
    AcTrBatchedLine2.prototype.getGeometryAt = function (geometryId) {
        this.validateGeometryId(geometryId);
        return this._geometryInfo[geometryId];
    };
    /**
     * Resizes packed segment capacity while preserving existing segment data.
     */
    AcTrBatchedLine2.prototype.setGeometrySize = function (maxSegmentCount) {
        var oldGeometry = this.geometry;
        var oldPacked = this._getPackedSegmentArray();
        this._maxSegmentCount = maxSegmentCount;
        this.geometry = new LineSegmentsGeometry();
        this.geometry.setPositions(new Float32Array(maxSegmentCount * 6));
        var newPacked = this._getPackedSegmentArray();
        copyArrayContents(oldPacked, newPacked);
        this._copyStaticAttributes(oldGeometry);
        this._geometryInitialized = true;
        this._syncDrawRange();
        oldGeometry.dispose();
    };
    /**
     * Builds a standalone `LineSegments2` object for one packed geometry id.
     */
    AcTrBatchedLine2.prototype.getObjectAt = function (batchId) {
        var _a, _b;
        var info = this._geometryInfo[batchId];
        var geometry = this._createSubGeometry(info.vertexStart, info.vertexCount, true);
        var object = new LineSegments2(geometry, this.material);
        object.position.copy(this.position);
        object.updateMatrix();
        object.updateMatrixWorld(true);
        this.getBoundingBoxAt(batchId, (_a = object.geometry.boundingBox) !== null && _a !== void 0 ? _a : new THREE.Box3());
        this.getBoundingSphereAt(batchId, (_b = object.geometry.boundingSphere) !== null && _b !== void 0 ? _b : new THREE.Sphere());
        return object;
    };
    /**
     * Raycasts one packed geometry id.
     */
    AcTrBatchedLine2.prototype.intersectWith = function (geometryId, raycaster, intersects) {
        this._intersectWith(geometryId, raycaster, intersects);
    };
    /**
     * Raycasts all packed geometry ids.
     */
    AcTrBatchedLine2.prototype.raycast = function (raycaster, intersects) {
        for (var i = 0, l = this._geometryInfo.length; i < l; i++) {
            this._intersectWith(i, raycaster, intersects);
        }
    };
    /**
     * Deep-copies packed geometry state and bounds.
     */
    AcTrBatchedLine2.prototype.copy = function (source) {
        var _a;
        _super.prototype.copy.call(this, source);
        this.geometry = source.geometry.clone();
        this.boundingBox = source.boundingBox ? source.boundingBox.clone() : null;
        this.boundingSphere = source.boundingSphere
            ? source.boundingSphere.clone()
            : null;
        this._geometryInfo = source._geometryInfo.map(function (info) { return (__assign(__assign({}, info), { boundingBox: info.boundingBox ? info.boundingBox.clone() : null })); });
        this._maxSegmentCount = source._maxSegmentCount;
        this._geometryCount = source._geometryCount;
        this._nextSegmentStart = source._nextSegmentStart;
        this._geometryInitialized = source._geometryInitialized;
        this._origin = (_a = source._origin) === null || _a === void 0 ? void 0 : _a.clone();
        return this;
    };
    /**
     * Disposes packed geometry resources.
     */
    AcTrBatchedLine2.prototype.dispose = function () {
        this.geometry.dispose();
        return this;
    };
    /**
     * Internal raycast implementation for one packed geometry id.
     */
    AcTrBatchedLine2.prototype._intersectWith = function (geometryId, raycaster, intersects) {
        var geometryInfo = this._geometryInfo[geometryId];
        if (!isBatchGeometryVisible(geometryInfo.flags)) {
            return;
        }
        if (geometryInfo.bboxIntersectionCheck) {
            this.getBoundingBoxAt(geometryId, _box);
            _box.applyMatrix4(this.matrixWorld);
            if (raycaster.ray.intersectBox(_box, _vector)) {
                var distance = raycaster.ray.origin.distanceTo(_vector);
                intersects.push({
                    distance: distance,
                    point: _vector.clone(),
                    object: this,
                    face: null,
                    faceIndex: undefined,
                    uv: undefined,
                    batchId: geometryId,
                    objectId: geometryInfo.objectId
                });
            }
            return;
        }
        var geometry = this._createSubGeometry(geometryInfo.vertexStart, geometryInfo.vertexCount, false);
        _raycastObject.geometry = geometry;
        _raycastObject.material = this.material;
        _raycastObject.position.copy(this.position);
        _raycastObject.quaternion.copy(this.quaternion);
        _raycastObject.scale.copy(this.scale);
        _raycastObject.updateMatrix();
        _raycastObject.updateMatrixWorld(true);
        _raycastObject.raycast(raycaster, _batchIntersects);
        // LineSegments2.raycast() ignores raycaster.params.Line.threshold and
        // only uses the pixel-based LineMaterial.linewidth for hit detection.
        // When linewidth is small the pick area can be too narrow, so fall back
        // to a bounding-box intersection check when the precise raycast misses.
        if (_batchIntersects.length === 0) {
            this.getBoundingBoxAt(geometryId, _box);
            _box.applyMatrix4(this.matrixWorld);
            var threshold = raycaster.params.Line.threshold;
            if (threshold > 0) {
                _box.expandByScalar(threshold);
            }
            if (raycaster.ray.intersectBox(_box, _vector2)) {
                var distance = raycaster.ray.origin.distanceTo(_vector2);
                intersects.push({
                    distance: distance,
                    point: _vector2.clone(),
                    object: this,
                    face: null,
                    faceIndex: undefined,
                    uv: undefined,
                    batchId: geometryId,
                    objectId: geometryInfo.objectId
                });
            }
            geometry.dispose();
            return;
        }
        for (var i = 0, l = _batchIntersects.length; i < l; i++) {
            var intersect = _batchIntersects[i];
            intersect.batchId = geometryId;
            intersect.objectId = geometryInfo.objectId;
            intersect.object = this;
            intersects.push(intersect);
        }
        _batchIntersects.length = 0;
        geometry.dispose();
    };
    /**
     * Creates a temporary sub-geometry view/copy for raycast/highlight purposes.
     */
    AcTrBatchedLine2.prototype._createSubGeometry = function (start, count, stableCopy) {
        var packed = this._getPackedSegmentArray();
        var segmentArray = stableCopy
            ? packed.slice(start * 6, (start + count) * 6)
            : packed.subarray(start * 6, (start + count) * 6);
        var geometry = new LineSegmentsGeometry();
        geometry.setPositions(segmentArray);
        this._copyStaticAttributes(this.geometry, geometry);
        return geometry;
    };
    /**
     * Returns packed segment array `[sx,sy,sz, ex,ey,ez, ...]`.
     */
    AcTrBatchedLine2.prototype._getPackedSegmentArray = function () {
        var _a;
        var instanceStart = this.geometry.getAttribute('instanceStart');
        if ('data' in instanceStart && ((_a = instanceStart.data) === null || _a === void 0 ? void 0 : _a.array)) {
            return instanceStart.data.array;
        }
        var instanceEnd = this.geometry.getAttribute('instanceEnd');
        var count = instanceStart.count;
        var array = new Float32Array(count * 6);
        for (var i = 0, p = 0; i < count; i++) {
            array[p++] = instanceStart.getComponent(i, 0);
            array[p++] = instanceStart.getComponent(i, 1);
            array[p++] = instanceStart.getComponent(i, 2);
            array[p++] = instanceEnd.getComponent(i, 0);
            array[p++] = instanceEnd.getComponent(i, 1);
            array[p++] = instanceEnd.getComponent(i, 2);
        }
        return array;
    };
    /**
     * Synchronizes rendered instance count with active packed segment range.
     */
    AcTrBatchedLine2.prototype._syncDrawRange = function () {
        ;
        this.geometry.instanceCount =
            this._nextSegmentStart;
    };
    AcTrBatchedLine2.GROWTH_FACTOR = 1.25;
    return AcTrBatchedLine2;
}(AcTrBatchedLine2Base));
export { AcTrBatchedLine2 };
//# sourceMappingURL=AcTrBatchedLine2.js.map