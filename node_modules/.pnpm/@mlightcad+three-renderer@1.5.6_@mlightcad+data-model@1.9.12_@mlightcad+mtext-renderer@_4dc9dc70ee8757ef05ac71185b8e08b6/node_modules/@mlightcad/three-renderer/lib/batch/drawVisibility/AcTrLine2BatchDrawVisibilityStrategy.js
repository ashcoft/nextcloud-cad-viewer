var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { AcTrBatchDrawVisibilityStrategy } from './AcTrBatchDrawVisibilityStrategy';
/**
 * Draw-visibility strategy for wide-line `LineSegments2` batches.
 */
var AcTrLine2BatchDrawVisibilityStrategy = /** @class */ (function (_super) {
    __extends(AcTrLine2BatchDrawVisibilityStrategy, _super);
    function AcTrLine2BatchDrawVisibilityStrategy() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    AcTrLine2BatchDrawVisibilityStrategy.prototype.collapse = function (geometry, info) {
        var startAttr = geometry.getAttribute('instanceStart');
        var endAttr = geometry.getAttribute('instanceEnd');
        if (!startAttr || !endAttr || info.vertexCount <= 0) {
            return false;
        }
        var startArray = startAttr.array;
        var endArray = endAttr.array;
        var segmentStart = info.vertexStart;
        var segmentCount = info.vertexCount;
        if (!info.hiddenDrawSnapshot) {
            info.hiddenDrawSnapshot = {
                instanceStart: startArray.slice(segmentStart * 3, (segmentStart + segmentCount) * 3),
                instanceEnd: endArray.slice(segmentStart * 3, (segmentStart + segmentCount) * 3)
            };
        }
        for (var segment = segmentStart; segment < segmentStart + segmentCount; segment++) {
            var offset = segment * 3;
            endArray[offset] = startArray[offset];
            endArray[offset + 1] = startArray[offset + 1];
            endArray[offset + 2] = startArray[offset + 2];
        }
        endAttr.needsUpdate = true;
        return true;
    };
    AcTrLine2BatchDrawVisibilityStrategy.prototype.restore = function (geometry, info) {
        var snapshot = info.hiddenDrawSnapshot;
        var startAttr = geometry.getAttribute('instanceStart');
        var endAttr = geometry.getAttribute('instanceEnd');
        if (!(snapshot === null || snapshot === void 0 ? void 0 : snapshot.instanceStart) ||
            !snapshot.instanceEnd ||
            !startAttr ||
            !endAttr) {
            return false;
        }
        var segmentStart = info.vertexStart;
        startAttr.array.set(snapshot.instanceStart, segmentStart * 3);
        endAttr.array.set(snapshot.instanceEnd, segmentStart * 3);
        startAttr.needsUpdate = true;
        endAttr.needsUpdate = true;
        delete info.hiddenDrawSnapshot;
        return true;
    };
    return AcTrLine2BatchDrawVisibilityStrategy;
}(AcTrBatchDrawVisibilityStrategy));
export { AcTrLine2BatchDrawVisibilityStrategy };
//# sourceMappingURL=AcTrLine2BatchDrawVisibilityStrategy.js.map