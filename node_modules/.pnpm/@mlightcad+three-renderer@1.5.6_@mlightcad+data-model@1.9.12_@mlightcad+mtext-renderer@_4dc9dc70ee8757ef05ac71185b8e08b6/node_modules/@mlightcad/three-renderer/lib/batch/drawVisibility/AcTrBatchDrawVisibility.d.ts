import * as THREE from 'three';
import type { AcTrBatchDrawVisibilityInfo, AcTrBatchDrawVisibilityMode } from './AcTrBatchDrawVisibilityTypes';
/**
 * Collapses or restores packed GPU geometry for one batch slot so invisible
 * entities are skipped at draw time without removing the slot from the batch.
 */
export declare function applyBatchSlotDrawVisibility(geometry: THREE.BufferGeometry, info: AcTrBatchDrawVisibilityInfo, visible: boolean, mode?: AcTrBatchDrawVisibilityMode): boolean;
/**
 * Clears stale draw snapshots and re-collapses hidden slots after batch
 * compaction moves packed GPU buffer ranges.
 */
export declare function syncBatchDrawVisibilityAfterOptimize(geometry: THREE.BufferGeometry, geometryInfoList: AcTrBatchDrawVisibilityInfo[]): void;
//# sourceMappingURL=AcTrBatchDrawVisibility.d.ts.map