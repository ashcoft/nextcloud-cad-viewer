var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { AcTrBatchDrawVisibilityStrategy } from './AcTrBatchDrawVisibilityStrategy';
/**
 * Draw-visibility strategy for indexed line and mesh batches.
 */
var AcTrIndexedBatchDrawVisibilityStrategy = /** @class */ (function (_super) {
    __extends(AcTrIndexedBatchDrawVisibilityStrategy, _super);
    function AcTrIndexedBatchDrawVisibilityStrategy() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    AcTrIndexedBatchDrawVisibilityStrategy.prototype.collapse = function (geometry, info) {
        var _a;
        var indexAttr = geometry.getIndex();
        if (!indexAttr || info.indexStart == null || info.indexStart < 0) {
            return false;
        }
        var array = indexAttr.array;
        var indexStart = info.indexStart;
        var indexCount = (_a = info.indexCount) !== null && _a !== void 0 ? _a : 0;
        if (indexCount <= 0) {
            return false;
        }
        var vertexStart = info.vertexStart;
        if (!info.hiddenDrawSnapshot) {
            info.hiddenDrawSnapshot = {
                indices: this.copyTypedArraySlice(array, indexStart, indexCount)
            };
        }
        for (var i = indexStart; i < indexStart + indexCount; i++) {
            array[i] = vertexStart;
        }
        indexAttr.needsUpdate = true;
        return true;
    };
    AcTrIndexedBatchDrawVisibilityStrategy.prototype.restore = function (geometry, info) {
        var _a;
        var snapshot = (_a = info.hiddenDrawSnapshot) === null || _a === void 0 ? void 0 : _a.indices;
        var indexAttr = geometry.getIndex();
        if (!snapshot ||
            !indexAttr ||
            info.indexStart == null ||
            info.indexStart < 0) {
            return false;
        }
        indexAttr.array.set(snapshot, info.indexStart);
        indexAttr.needsUpdate = true;
        delete info.hiddenDrawSnapshot;
        return true;
    };
    return AcTrIndexedBatchDrawVisibilityStrategy;
}(AcTrBatchDrawVisibilityStrategy));
export { AcTrIndexedBatchDrawVisibilityStrategy };
//# sourceMappingURL=AcTrIndexedBatchDrawVisibilityStrategy.js.map