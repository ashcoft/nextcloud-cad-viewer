var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { AcTrBatchDrawVisibilityStrategy } from './AcTrBatchDrawVisibilityStrategy';
/**
 * Draw-visibility strategy for non-indexed vertex batches (lines/points).
 */
var AcTrVertexBatchDrawVisibilityStrategy = /** @class */ (function (_super) {
    __extends(AcTrVertexBatchDrawVisibilityStrategy, _super);
    function AcTrVertexBatchDrawVisibilityStrategy() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    AcTrVertexBatchDrawVisibilityStrategy.prototype.collapse = function (geometry, info) {
        var positionAttr = geometry.getAttribute('position');
        if (!positionAttr || info.vertexCount <= 0) {
            return false;
        }
        var array = positionAttr.array;
        var itemSize = positionAttr.itemSize;
        var start = info.vertexStart;
        var count = info.vertexCount;
        var baseOffset = start * itemSize;
        if (!info.hiddenDrawSnapshot) {
            info.hiddenDrawSnapshot = {
                positions: array.slice(baseOffset, baseOffset + count * itemSize)
            };
        }
        if (count === 1) {
            for (var c = 0; c < itemSize; c++) {
                array[baseOffset + c] = Number.NaN;
            }
        }
        else {
            var x = array[baseOffset];
            var y = array[baseOffset + 1];
            var z = array[baseOffset + 2];
            for (var vertex = start; vertex < start + count; vertex++) {
                var offset = vertex * itemSize;
                array[offset] = x;
                array[offset + 1] = y;
                array[offset + 2] = z;
            }
        }
        positionAttr.needsUpdate = true;
        return true;
    };
    AcTrVertexBatchDrawVisibilityStrategy.prototype.restore = function (geometry, info) {
        var _a;
        var snapshot = (_a = info.hiddenDrawSnapshot) === null || _a === void 0 ? void 0 : _a.positions;
        var positionAttr = geometry.getAttribute('position');
        if (!snapshot || !positionAttr || info.vertexCount <= 0) {
            return false;
        }
        var array = positionAttr.array;
        var itemSize = positionAttr.itemSize;
        array.set(snapshot, info.vertexStart * itemSize);
        positionAttr.needsUpdate = true;
        delete info.hiddenDrawSnapshot;
        return true;
    };
    return AcTrVertexBatchDrawVisibilityStrategy;
}(AcTrBatchDrawVisibilityStrategy));
export { AcTrVertexBatchDrawVisibilityStrategy };
//# sourceMappingURL=AcTrVertexBatchDrawVisibilityStrategy.js.map