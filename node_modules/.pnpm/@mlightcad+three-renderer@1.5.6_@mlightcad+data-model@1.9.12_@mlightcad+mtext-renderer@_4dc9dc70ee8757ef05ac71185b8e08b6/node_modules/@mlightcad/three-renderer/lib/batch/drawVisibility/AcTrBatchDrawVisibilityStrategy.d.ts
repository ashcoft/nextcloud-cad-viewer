import * as THREE from 'three';
import type { AcTrBatchDrawVisibilityInfo, AcTrBatchDrawVisibilityMode } from './AcTrBatchDrawVisibilityTypes';
/**
 * Strategy interface for collapsing and restoring one batched geometry slot.
 */
export declare abstract class AcTrBatchDrawVisibilityStrategy {
    /**
     * Rewrites GPU buffers so the slot produces no visible pixels.
     */
    abstract collapse(geometry: THREE.BufferGeometry, info: AcTrBatchDrawVisibilityInfo): boolean;
    /**
     * Restores GPU buffers from the slot snapshot when available.
     */
    abstract restore(geometry: THREE.BufferGeometry, info: AcTrBatchDrawVisibilityInfo): boolean;
    /**
     * Applies one visibility transition to the slot draw buffers.
     */
    apply(geometry: THREE.BufferGeometry, info: AcTrBatchDrawVisibilityInfo, visible: boolean): boolean;
    /**
     * Copies one typed-array slice for snapshot storage.
     */
    protected copyTypedArraySlice(array: THREE.TypedArray, start: number, count: number): THREE.TypedArray;
}
/**
 * Detects which draw-visibility strategy matches a packed batch geometry layout.
 */
export declare function resolveBatchDrawVisibilityMode(geometry: THREE.BufferGeometry): AcTrBatchDrawVisibilityMode;
//# sourceMappingURL=AcTrBatchDrawVisibilityStrategy.d.ts.map