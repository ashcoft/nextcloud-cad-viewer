var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
import * as THREE from 'three';
import { CSS2DObject } from 'three/examples/jsm/renderers/CSS2DRenderer.js';
/** Default layer name when none is specified */
var DEFAULT_LAYER = 'default';
/**
 * Manages transient HTML elements anchored to world coordinates.
 *
 * Similar to {@link AcTrTransientManager} but for HTML overlays instead of
 * Three.js geometry. Uses Three.js CSS2DObject so positioning is handled
 * automatically by CSS2DRenderer — no manual viewChanged listeners needed.
 *
 * Supports grouping entries by **layer** so callers can show/hide or clear
 * specific categories of overlays (e.g. measurements, annotations).
 */
var AcTrHtmlTransientManager = /** @class */ (function () {
    function AcTrHtmlTransientManager(scene) {
        this.scene = scene;
        this.htmlGroup = new THREE.Group();
        this.htmlGroup.name = 'Html_Transient_Group';
        this.scene.add(this.htmlGroup);
        this.entries = new Map();
    }
    /**
     * Add an HTML element anchored at a world-space position.
     *
     * @param id Unique identifier for this entry
     * @param element The HTML element to display
     * @param worldPosition Position in world coordinates
     * @param layer Optional layer name for grouping (default: 'default')
     */
    AcTrHtmlTransientManager.prototype.add = function (id, element, worldPosition, layer) {
        var _a;
        if (layer === void 0) { layer = DEFAULT_LAYER; }
        // Remove existing entry with same id
        this.remove(id);
        var css2dObject = new CSS2DObject(element);
        css2dObject.position.set(worldPosition.x, worldPosition.y, (_a = worldPosition.z) !== null && _a !== void 0 ? _a : 0);
        this.entries.set(id, { object: css2dObject, layer: layer });
        this.htmlGroup.add(css2dObject);
    };
    /**
     * Update the world position of an existing entry.
     *
     * @param id The entry identifier
     * @param worldPosition New position in world coordinates
     */
    AcTrHtmlTransientManager.prototype.updatePosition = function (id, worldPosition) {
        var _a;
        var entry = this.entries.get(id);
        if (!entry)
            return;
        entry.object.position.set(worldPosition.x, worldPosition.y, (_a = worldPosition.z) !== null && _a !== void 0 ? _a : 0);
    };
    /**
     * Remove an entry by ID.
     */
    AcTrHtmlTransientManager.prototype.remove = function (id) {
        var entry = this.entries.get(id);
        if (!entry)
            return;
        this.htmlGroup.remove(entry.object);
        entry.object.element.remove();
        this.entries.delete(id);
    };
    /**
     * Clear all entries, or only entries on a specific layer.
     *
     * @param layer If provided, only entries on this layer are removed.
     *              If omitted, all entries are removed.
     */
    AcTrHtmlTransientManager.prototype.clear = function (layer) {
        var e_1, _a, e_2, _b;
        if (layer == null) {
            try {
                for (var _c = __values(this.entries.values()), _d = _c.next(); !_d.done; _d = _c.next()) {
                    var entry = _d.value;
                    this.htmlGroup.remove(entry.object);
                    entry.object.element.remove();
                }
            }
            catch (e_1_1) { e_1 = { error: e_1_1 }; }
            finally {
                try {
                    if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
                }
                finally { if (e_1) throw e_1.error; }
            }
            this.entries.clear();
        }
        else {
            try {
                for (var _e = __values(this.entries), _f = _e.next(); !_f.done; _f = _e.next()) {
                    var _g = __read(_f.value, 2), id = _g[0], entry = _g[1];
                    if (entry.layer === layer) {
                        this.htmlGroup.remove(entry.object);
                        entry.object.element.remove();
                        this.entries.delete(id);
                    }
                }
            }
            catch (e_2_1) { e_2 = { error: e_2_1 }; }
            finally {
                try {
                    if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
                }
                finally { if (e_2) throw e_2.error; }
            }
        }
    };
    /**
     * Check whether an entry exists.
     */
    AcTrHtmlTransientManager.prototype.has = function (id) {
        return this.entries.has(id);
    };
    /**
     * Retrieve the HTML element for an entry.
     */
    AcTrHtmlTransientManager.prototype.get = function (id) {
        var _a;
        return (_a = this.entries.get(id)) === null || _a === void 0 ? void 0 : _a.object.element;
    };
    /**
     * Show or hide all entries, or only entries on a specific layer.
     *
     * @param visible Whether to show or hide
     * @param layer If provided, only this layer is affected
     */
    AcTrHtmlTransientManager.prototype.setVisible = function (visible, layer) {
        var e_3, _a;
        if (layer == null) {
            this.htmlGroup.visible = visible;
        }
        else {
            try {
                for (var _b = __values(this.entries.values()), _c = _b.next(); !_c.done; _c = _b.next()) {
                    var entry = _c.value;
                    if (entry.layer === layer) {
                        entry.object.visible = visible;
                    }
                }
            }
            catch (e_3_1) { e_3 = { error: e_3_1 }; }
            finally {
                try {
                    if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
                }
                finally { if (e_3) throw e_3.error; }
            }
        }
    };
    /**
     * Destroy the manager and remove all HTML elements.
     */
    AcTrHtmlTransientManager.prototype.dispose = function () {
        this.clear();
        this.scene.remove(this.htmlGroup);
    };
    return AcTrHtmlTransientManager;
}());
export { AcTrHtmlTransientManager };
//# sourceMappingURL=AcTrHtmlTransientManager.js.map