var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import * as THREE from 'three';
import { LineMaterial } from 'three/examples/jsm/lines/LineMaterial.js';
import { LineSegments2 } from 'three/examples/jsm/lines/LineSegments2.js';
import { LineSegmentsGeometry } from 'three/examples/jsm/lines/LineSegmentsGeometry.js';
import { resolveAnchorFromBox } from '../draw/AcTrBatchDrawPolicy';
import { AcTrBufferGeometryUtil, getSceneDrawableUserData } from '../util';
import { AcTrEntity } from './AcTrEntity';
var AcTrLineSegments = /** @class */ (function (_super) {
    __extends(AcTrLineSegments, _super);
    function AcTrLineSegments(array, itemSize, indices, traits, context) {
        var _a, _b, _c, _d;
        var _this = _super.call(this, context) || this;
        var material = _this.styleManager.getLineMaterial(traits);
        var box = new THREE.Box3();
        for (var i = 0; i < array.length; i += itemSize) {
            box.expandByPoint(_point.set(array[i], array[i + 1], (_a = array[i + 2]) !== null && _a !== void 0 ? _a : 0));
        }
        var localOrigin = box.getCenter(new THREE.Vector3());
        if (material instanceof LineMaterial) {
            var segmentCount = Math.floor(indices.length / 2);
            var segmentPositions = new Float32Array(segmentCount * 6);
            for (var i = 0, pos = 0; i < segmentCount; i++) {
                var i1 = indices[i * 2];
                var i2 = indices[i * 2 + 1];
                var base1 = i1 * itemSize;
                var base2 = i2 * itemSize;
                segmentPositions[pos++] = array[base1] - localOrigin.x;
                segmentPositions[pos++] = array[base1 + 1] - localOrigin.y;
                segmentPositions[pos++] = ((_b = array[base1 + 2]) !== null && _b !== void 0 ? _b : 0) - localOrigin.z;
                segmentPositions[pos++] = array[base2] - localOrigin.x;
                segmentPositions[pos++] = array[base2 + 1] - localOrigin.y;
                segmentPositions[pos++] = ((_c = array[base2 + 2]) !== null && _c !== void 0 ? _c : 0) - localOrigin.z;
            }
            var lineGeometry = new LineSegmentsGeometry();
            lineGeometry.setPositions(segmentPositions);
            AcTrBufferGeometryUtil.safeComputeBoundingBox(lineGeometry);
            AcTrBufferGeometryUtil.safeComputeBoundingSphere(lineGeometry);
            _this.setBoundingBox(lineGeometry, localOrigin);
            var line_1 = new LineSegments2(lineGeometry, material);
            line_1.position.set(localOrigin.x, localOrigin.y, localOrigin.z);
            getSceneDrawableUserData(line_1).styleMaterialId = material.id;
            _this.add(line_1);
            _this.finalizeLeafDrawables();
            return _this;
        }
        var rebased = new Float32Array(array.length);
        for (var i = 0; i < array.length; i += itemSize) {
            rebased[i] = array[i] - localOrigin.x;
            rebased[i + 1] = array[i + 1] - localOrigin.y;
            rebased[i + 2] = ((_d = array[i + 2]) !== null && _d !== void 0 ? _d : 0) - localOrigin.z;
        }
        var geometry = new THREE.BufferGeometry();
        geometry.setAttribute('position', new THREE.BufferAttribute(rebased, itemSize));
        geometry.setIndex(new THREE.BufferAttribute(indices, 1));
        _this.setBoundingBox(geometry, localOrigin);
        var line = new THREE.LineSegments(geometry, material);
        line.position.set(localOrigin.x, localOrigin.y, localOrigin.z);
        AcTrBufferGeometryUtil.computeLineDistances(line);
        _this.add(line);
        _this.finalizeLeafDrawables();
        return _this;
    }
    AcTrLineSegments.prototype.resolveDrawMode = function () {
        return this.batchDrawPolicy.resolveDrawMode({
            anchor: resolveAnchorFromBox(this.wcsBbox)
        });
    };
    AcTrLineSegments.prototype.setBoundingBox = function (geometry, localOrigin) {
        var boundingBox = AcTrBufferGeometryUtil.safeComputeBoundingBox(geometry);
        if (!boundingBox) {
            return;
        }
        var worldBox = boundingBox.clone();
        worldBox.translate(localOrigin);
        this.wcsBbox = worldBox;
    };
    return AcTrLineSegments;
}(AcTrEntity));
export { AcTrLineSegments };
var _point = /*@__PURE__*/ new THREE.Vector3();
//# sourceMappingURL=AcTrLineSegments.js.map