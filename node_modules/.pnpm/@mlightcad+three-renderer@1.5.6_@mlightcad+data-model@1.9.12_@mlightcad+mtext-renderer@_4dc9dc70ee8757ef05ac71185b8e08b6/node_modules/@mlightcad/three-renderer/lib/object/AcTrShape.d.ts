import { AcGiShapeData, AcGiSubEntityTraits, AcGiTextStyle } from '@mlightcad/data-model';
import * as THREE from 'three';
import type { AcTrDrawMode } from '../draw/AcTrDrawMode';
import { AcTrRenderContext } from '../renderer/AcTrRenderContext';
import { AcTrEntity } from './AcTrEntity';
export declare class AcTrShape extends AcTrEntity {
    private _rendered?;
    private _shape;
    private _style;
    private _colorSettings;
    private _entityTraits;
    constructor(shape: AcGiShapeData, traits: AcGiSubEntityTraits, style: AcGiTextStyle | null | undefined, context: AcTrRenderContext, delay?: boolean);
    /** Reapplies CAD text materials from the entity traits snapshot. */
    refreshTextMaterials(): void;
    /**
     * Builds renderer input with only the glyph key that should be resolved.
     *
     * mtext-renderer falls back from shape name to shape number when both are
     * present; SHAPE entities should use the name exclusively when it is set.
     */
    private toRenderableShapeData;
    syncDraw(): void;
    draw(): Promise<void>;
    raycast(raycaster: THREE.Raycaster, intersects: THREE.Intersection[]): void;
    private describeShape;
    resolveDrawMode(): AcTrDrawMode;
    private attachRendered;
    private updateSelectionBox;
    private computeGeometryBox;
    private removeInvalidGeometryLeaves;
    private hasGeometry;
}
//# sourceMappingURL=AcTrShape.d.ts.map