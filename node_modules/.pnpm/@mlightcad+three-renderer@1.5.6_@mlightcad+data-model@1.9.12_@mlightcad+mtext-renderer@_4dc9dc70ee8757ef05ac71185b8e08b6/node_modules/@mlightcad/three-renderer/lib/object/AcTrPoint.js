var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import * as THREE from 'three';
import { AcTrPointSymbolCreator } from '../geometry/AcTrPointSymbolCreator';
import { AcTrBufferGeometryUtil } from '../util/AcTrBufferGeometryUtil';
import { getSceneDrawableUserData } from '../util/AcTrObjectUserData';
import { AcTrEntity } from './AcTrEntity';
var _vector3 = /*@__PURE__*/ new THREE.Vector3();
var AcTrPoint = /** @class */ (function (_super) {
    __extends(AcTrPoint, _super);
    function AcTrPoint(point, traits, style, context) {
        var _a, _b, _c;
        var _this = _super.call(this, context) || this;
        _this._point = point;
        var pointSymbol = AcTrPointSymbolCreator.instance.create(style.displayMode);
        _this.isShowPoint = pointSymbol.point != null;
        // Always create one THREE.Points object. If 'isShowPoint' is true, show it. Otherwise, hide it.
        var geometry = (_a = pointSymbol.point) !== null && _a !== void 0 ? _a : new THREE.BufferGeometry().setFromPoints([_vector3.set(0, 0, 0)]);
        _this.unionWorldBoundingBox(geometry, point);
        var material = _this.styleManager.getPointsMaterial(traits);
        var pointObj = new THREE.Points(geometry, material);
        pointObj.position.set(point.x, point.y, (_b = point.z) !== null && _b !== void 0 ? _b : 0);
        // Add the flag to check intersection using bounding box of the mesh
        getSceneDrawableUserData(pointObj).bboxIntersectionCheck = true;
        pointObj.visible = _this.isShowPoint;
        _this.add(pointObj);
        if (pointSymbol.line) {
            var geometry_1 = pointSymbol.line;
            _this.unionWorldBoundingBox(geometry_1, point);
            var material_1 = _this.styleManager.getLineMaterial(traits, true);
            var lineSegmentsObj = new THREE.LineSegments(geometry_1, material_1);
            lineSegmentsObj.position.set(point.x, point.y, (_c = point.z) !== null && _c !== void 0 ? _c : 0);
            var lineDrawable = getSceneDrawableUserData(lineSegmentsObj);
            // Add the flag to check intersection using bounding box of the mesh
            lineDrawable.bboxIntersectionCheck = true;
            // Add this flag so that batched group can handle it with different logic
            lineDrawable.isPoint = true;
            lineDrawable.position = { x: point.x, y: point.y, z: point.z };
            _this.add(lineSegmentsObj);
        }
        _this.finalizeLeafDrawables();
        return _this;
    }
    AcTrPoint.prototype.resolveDrawMode = function () {
        return this.batchDrawPolicy.resolveDrawMode({
            position: this._point
        });
    };
    AcTrPoint.prototype.unionWorldBoundingBox = function (geometry, worldOrigin) {
        var _a;
        var boundingBox = AcTrBufferGeometryUtil.safeComputeBoundingBox(geometry);
        if (!boundingBox) {
            return;
        }
        var worldBox = boundingBox.clone();
        worldBox.translate(_vector3.set(worldOrigin.x, worldOrigin.y, (_a = worldOrigin.z) !== null && _a !== void 0 ? _a : 0));
        this.wcsBbox.union(worldBox);
    };
    return AcTrPoint;
}(AcTrEntity));
export { AcTrPoint };
//# sourceMappingURL=AcTrPoint.js.map