/**
 * Returns false when the object or any ancestor has `visible === false`.
 */
export function isObjectHierarchyVisible(object) {
    var node = object;
    while (node) {
        if (node.visible === false) {
            return false;
        }
        node = node.parent;
    }
    return true;
}
//# sourceMappingURL=AcTrVisibility.js.map