import * as THREE from 'three';
/**
 * Highlight color.
 */
export declare const HIGHLIGHT_COLOR: THREE.Color;
/**
 * @internal
 */
export declare class AcTrMaterialUtil {
    /**
     * Clone given material(s)
     */
    static cloneMaterial(material: THREE.Material | THREE.Material[]): THREE.Material | THREE.Material[];
    static setMaterialColor(material: THREE.Material | THREE.Material[], color?: THREE.Color): void;
    private static hasColorProperty;
    private static hasEmissiveProperty;
    private static hasUniformsProperty;
    private static cloneSingleMaterial;
    /**
     * Cloned highlight materials must not inherit runtime-only shader state from
     * the source instance, otherwise the clone can keep using stale RTE shader
     * uniforms/program keys bound to another render object.
     */
    private static resetRuntimeShaderState;
}
//# sourceMappingURL=AcTrMaterialUtil.d.ts.map