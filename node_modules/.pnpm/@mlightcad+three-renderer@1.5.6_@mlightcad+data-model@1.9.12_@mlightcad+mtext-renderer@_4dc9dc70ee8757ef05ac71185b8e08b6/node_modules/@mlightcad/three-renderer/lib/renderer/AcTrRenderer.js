import { AcCmEventManager } from '@mlightcad/data-model';
import { FontManager } from '@mlightcad/mtext-renderer';
import * as THREE from 'three';
import { AcTrEntity, AcTrGroup, AcTrImage, AcTrLine, AcTrLineSegments, AcTrMText, AcTrObject, AcTrPoint, AcTrPolygon, AcTrShape } from '../object';
import { AcTrMaterialManager } from '../style/AcTrMaterialManager';
import { AcTrSubEntityTraitsUtil } from '../util';
import { AcTrMTextRenderer } from './AcTrMTextRenderer';
import { AcTrRenderContext } from './AcTrRenderContext';
var AcTrRenderer = /** @class */ (function () {
    function AcTrRenderer(renderer) {
        var _this = this;
        this.events = {
            fontNotFound: new AcCmEventManager()
        };
        this._renderer = renderer;
        this._context = new AcTrRenderContext();
        var size = renderer.getSize(new THREE.Vector2());
        this._context.styleManager.updateLineResolution(size.x, size.y);
        AcTrMTextRenderer.getInstance().overrideStyleManager(this._context.styleManager);
        FontManager.instance.events.fontNotFound.addEventListener(function (args) {
            _this.events.fontNotFound.dispatch(args);
        });
        this._subEntityTraits = AcTrSubEntityTraitsUtil.createDefaultTraits();
    }
    Object.defineProperty(AcTrRenderer.prototype, "subEntityTraits", {
        /**
         * @inheritdoc
         */
        get: function () {
            return this._subEntityTraits;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrRenderer.prototype, "context", {
        /**
         * Draw-time context for resolving semantic trait colours (for example ACI 7
         * foreground) into pixel RGB values.
         *
         * Derived from {@link currentBackgroundColor} on each read — no separate
         * sync is required when the canvas background changes.
         */
        get: function () {
            this._context.syncBackgroundColor(this._context.styleManager.currentBackgroundColor);
            return this._context;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrRenderer.prototype, "batchDrawPolicy", {
        /**
         * Strategy that decides whether converted entities should batch or stay unbatched.
         */
        get: function () {
            return this._context.batchDrawPolicy;
        },
        set: function (policy) {
            this._context.batchDrawPolicy = policy;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrRenderer.prototype, "autoClear", {
        get: function () {
            return this._renderer.autoClear;
        },
        set: function (value) {
            this._renderer.autoClear = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrRenderer.prototype, "domElement", {
        get: function () {
            return this._renderer.domElement;
        },
        enumerable: false,
        configurable: true
    });
    AcTrRenderer.prototype.setSize = function (width, height) {
        this._renderer.setSize(width, height);
        this._context.styleManager.updateLineResolution(width, height);
    };
    /**
     * Updates wide-line shader resolution without resizing the canvas.
     */
    AcTrRenderer.prototype.updateLineResolution = function (width, height) {
        this._context.styleManager.updateLineResolution(width, height);
    };
    /**
     * Syncs shader uniforms that depend on the active camera zoom.
     */
    AcTrRenderer.prototype.syncCameraZoom = function (zoom) {
        this.updateCameraZoomUniform(zoom);
    };
    AcTrRenderer.prototype.getViewport = function (target) {
        return this._renderer.getViewport(target);
    };
    AcTrRenderer.prototype.setViewport = function (x, y, width, height) {
        this._renderer.setViewport(x, y, width, height);
    };
    AcTrRenderer.prototype.clear = function () {
        this._renderer.clear();
    };
    AcTrRenderer.prototype.clearDepth = function () {
        this._renderer.clearDepth();
    };
    AcTrRenderer.prototype.render = function (scene, camera) {
        this.updateCameraZoomUniform(camera.zoom);
        this._renderer.render(scene, camera.internalCamera);
        // RTE frame scheduling is added in the large-coordinate feature branch.
        return false;
    };
    /**
     * Repaints materials explicitly registered as background-follow fills.
     *
     * The current fill manager keeps solid hatches on the foreground path, so
     * this is mostly an extension point for future fill styles.
     *
     * @param color - New background color (typically the canvas bg).
     */
    AcTrRenderer.prototype.changeBackground = function (color) {
        this._context.styleManager.changeBackground(color);
    };
    Object.defineProperty(AcTrRenderer.prototype, "currentBackgroundColor", {
        /**
         * The canvas background colour tracked by the style manager.
         *
         * Reading returns the value last written here (or the default
         * `0x000000`).  Writing both stores the colour on the style manager
         * options (so material managers know the current theme) and repaints
         * every background-follow material already in the cache.
         */
        get: function () {
            return this._context.styleManager.currentBackgroundColor;
        },
        set: function (value) {
            this._context.styleManager.currentBackgroundColor = value;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Sets the clear color used when clearing the canvas.
     *
     * @param color - Background color as 24-bit hexadecimal RGB number
     * @param alpha - Optional alpha value (0.0 - 1.0)
     */
    AcTrRenderer.prototype.setClearColor = function (color, alpha) {
        this._renderer.setClearColor(color, alpha);
    };
    /**
     * Gets the current clear color as a 24-bit hexadecimal RGB number.
     */
    AcTrRenderer.prototype.getClearColor = function () {
        var color = new THREE.Color();
        this._renderer.getClearColor(color);
        return color.getHex();
    };
    Object.defineProperty(AcTrRenderer.prototype, "clearAlpha", {
        /**
         * Gets the current clear alpha value.
         */
        get: function () {
            return this._renderer.getClearAlpha();
        },
        /**
         * Sets the clear alpha used when clearing the canvas.
         *
         * @param alpha - Alpha value (0.0 - 1.0)
         */
        set: function (alpha) {
            this._renderer.setClearAlpha(alpha);
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrRenderer.prototype, "internalRenderer", {
        /**
         * The internal THREE.js webgl renderer
         */
        get: function () {
            return this._renderer;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * @inheritdoc
     */
    AcTrRenderer.prototype.setFontMapping = function (mapping) {
        FontManager.instance.setFontMapping(mapping);
    };
    Object.defineProperty(AcTrRenderer.prototype, "ltscale", {
        /**
         * Sets global ltscale
         */
        set: function (scale) {
            this._context.styleManager.options.ltscale = scale;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrRenderer.prototype, "celtscale", {
        /**
         * Sets global celtscale
         */
        set: function (scale) {
            this._context.styleManager.options.celtscale = scale;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrRenderer.prototype, "missedFonts", {
        /**
         * Fonts list which can't be found
         */
        get: function () {
            return FontManager.instance.missedFonts;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrRenderer.prototype, "showLineWeight", {
        /**
         * Gets whether entity lineweights are displayed.
         */
        get: function () {
            return this._context.styleManager.showLineWeight;
        },
        /**
         * Sets whether entity lineweights are displayed.
         *
         * When disabled, line entities are rendered with basic 1px materials.
         */
        set: function (value) {
            this._context.styleManager.showLineWeight = value;
        },
        enumerable: false,
        configurable: true
    });
    AcTrRenderer.prototype.updateLayerMaterial = function (layerName, newTraits) {
        return this._context.styleManager.updateLayerMaterial(layerName, newTraits);
    };
    /**
     * Returns one cached material bound to an effective layer while preserving symbolic traits.
     *
     * This is used for block contents that inherit the layer of the INSERT they belong to.
     */
    AcTrRenderer.prototype.getLayerBoundMaterial = function (material, layerName, layerTraits) {
        return this._context.styleManager.getLayerBoundMaterial(material, layerName, layerTraits);
    };
    /**
     * Create one empty drawable object
     */
    AcTrRenderer.prototype.createObject = function () {
        return new AcTrObject(this._context);
    };
    /**
     * Create one empty entity
     */
    AcTrRenderer.prototype.createEntity = function () {
        return new AcTrEntity(this._context);
    };
    /**
     * @inheritdoc
     */
    AcTrRenderer.prototype.group = function (entities) {
        return new AcTrGroup(entities, this._context);
    };
    /**
     * @inheritdoc
     */
    AcTrRenderer.prototype.point = function (point, style) {
        var geometry = new AcTrPoint(point, this._subEntityTraits, style, this._context);
        return geometry;
    };
    /**
     * @inheritdoc
     */
    AcTrRenderer.prototype.circularArc = function (arc) {
        return this.linePoints(arc.getPoints(100));
    };
    /**
     * @inheritdoc
     */
    AcTrRenderer.prototype.ellipticalArc = function (ellipseArc) {
        return this.linePoints(ellipseArc.getPoints(100));
    };
    /**
     * @inheritdoc
     */
    AcTrRenderer.prototype.lines = function (points) {
        return this.linePoints(points);
    };
    /**
     * @inheritdoc
     */
    AcTrRenderer.prototype.lineSegments = function (array, itemSize, indices) {
        return new AcTrLineSegments(array, itemSize, indices, this._subEntityTraits, this._context);
    };
    /**
     * @inheritdoc
     */
    AcTrRenderer.prototype.area = function (area) {
        return new AcTrPolygon(area, this._subEntityTraits, this._context);
    };
    /**
     * @inheritdoc
     */
    AcTrRenderer.prototype.mtext = function (mtext, style, delay) {
        return new AcTrMText(mtext, this._subEntityTraits, style, this._context, delay);
    };
    /**
     * @inheritdoc
     */
    AcTrRenderer.prototype.shape = function (shape, style, delay) {
        return new AcTrShape(shape, this._subEntityTraits, style, this._context, delay);
    };
    /**
     * @inheritdoc
     */
    AcTrRenderer.prototype.image = function (blob, style) {
        return new AcTrImage(blob, style, this._context);
    };
    /**
     * Clears all cached materials and releases its memory
     */
    AcTrRenderer.prototype.dispose = function () {
        this._context.styleManager.dispose();
        FontManager.instance.missedFonts = {};
    };
    AcTrRenderer.prototype.linePoints = function (points) {
        if (points.length < 2) {
            return this.createEntity();
        }
        return new AcTrLine(points, this._subEntityTraits, this._context, false);
    };
    /**
     * Updates camera zoom value for shader materials
     */
    AcTrRenderer.prototype.updateCameraZoomUniform = function (zoom) {
        // DxfLoader.CameraZoomUniform.value = (zoom * this.container.height) / 50;
        AcTrMaterialManager.CameraZoomUniform.value = zoom;
    };
    return AcTrRenderer;
}());
export { AcTrRenderer };
//# sourceMappingURL=AcTrRenderer.js.map