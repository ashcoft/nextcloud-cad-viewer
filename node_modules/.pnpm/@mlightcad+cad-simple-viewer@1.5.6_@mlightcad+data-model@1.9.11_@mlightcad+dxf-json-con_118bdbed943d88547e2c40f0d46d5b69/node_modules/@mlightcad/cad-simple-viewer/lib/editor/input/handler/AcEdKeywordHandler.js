/**
 * Validates keyword input according to {@link AcEdPromptKeywordOptions}.
 */
var AcEdKeywordHandler = /** @class */ (function () {
    function AcEdKeywordHandler(options) {
        this.options = options;
    }
    AcEdKeywordHandler.prototype.parse = function (value) {
        var input = value.trim();
        // 1. ENTER / empty input
        if (input.length === 0) {
            return this.options.allowNone ? null : null;
            // returning null signals invalid or none;
            // caller can differentiate based on allowNone
        }
        // 2. Try keyword match (case-insensitive)
        var keyword = this.options.keywords.findByName(input);
        if (keyword) {
            // Return canonical keyword name (AutoCAD-style)
            return keyword.globalName;
        }
        // 3. Arbitrary input
        if (this.options.allowArbitraryInput) {
            return input;
        }
        // 4. Invalid input
        return null;
    };
    AcEdKeywordHandler.prototype.parseCommandLine = function (token, _context) {
        return this.parse(token);
    };
    return AcEdKeywordHandler;
}());
export { AcEdKeywordHandler };
//# sourceMappingURL=AcEdKeywordHandler.js.map