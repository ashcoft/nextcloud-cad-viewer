/**
 * Checks if value is the type of object.
 *
 * @example
 * ```ts
 * isObject({}); // true
 * isObject([1, 2, 3]); // true
 * isObject(() => {}); // true
 * isObject(null); // false
 * isObject(123); // false
 * ```
 */
declare const isObject: <T>(input: T) => input is T & object;
export default isObject;
//# sourceMappingURL=isObject.d.ts.map