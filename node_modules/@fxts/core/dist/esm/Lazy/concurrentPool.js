import { isAsyncIterable } from "../_internal/utils.js";
import identity from "../identity.js";
import map from "./map.js";
function concurrentPool(length, iterable) {
    if (iterable === undefined) {
        return (iterable) => {
            return concurrentPool(length, iterable);
        };
    }
    if (!Number.isFinite(length) || length <= 0) {
        throw new RangeError("'length' must be positive integer");
    }
    if (!isAsyncIterable(iterable)) {
        throw new TypeError("'iterable' must be type of AsyncIterable");
    }
    if (length === 1) {
        return map(identity, iterable);
    }
    let fillId = 1;
    let consumeId = 1;
    let idleWorkers = length;
    const consumer = [];
    const itemMap = new Map();
    const iterator = iterable[Symbol.asyncIterator]();
    let finished = false;
    let prev = Promise.resolve();
    async function fetchNextItem() {
        if (idleWorkers <= 0) {
            return;
        }
        idleWorkers--;
        const id = fillId++;
        fillId = fillId % Number.MAX_SAFE_INTEGER;
        try {
            const item = {
                success: await iterator.next(),
            };
            idleWorkers++;
            itemMap.set(id, item);
            chainItem(item);
        }
        catch (error) {
            idleWorkers++;
            finished = true;
            const item = {
                success: { done: false },
                fail: error,
            };
            itemMap.set(id, item);
            chainItem(item);
        }
    }
    function chainItem(item) {
        prev = prev.then(() => item).then(pullItem);
        return item;
    }
    function doneQueue() {
        while (consumer.length > 0) {
            // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
            const [resolve] = consumer.shift();
            resolve({
                value: undefined,
                done: true,
            });
        }
    }
    async function pullItem(item) {
        while (consumer.length > 0) {
            const id = consumer[0][2];
            if (!itemMap.has(id)) {
                break;
            }
            // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
            const [resolve, reject] = consumer.shift();
            // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
            const value = itemMap.get(id);
            itemMap.delete(id);
            if (value.fail) {
                reject(value.fail);
                doneQueue();
            }
            else {
                resolve(value.success);
            }
        }
        /**
         * @TODO ppeeou: If an error occurs, the next iterator should not be executed
         */
        if (!item.success.done && consumer.length > 0) {
            fetchNextItem();
        }
    }
    async function processQueue() {
        if (idleWorkers == 0) {
            return;
        }
        return Promise.all(Array.from({ length: idleWorkers }, () => fetchNextItem()));
    }
    function slide() {
        if (finished && fillId === consumeId) {
            while (consumer.length > 0) {
                // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
                const [resolve] = consumer.shift();
                resolve({ value: undefined, done: true });
            }
        }
        else {
            processQueue();
        }
    }
    return {
        [Symbol.asyncIterator]() {
            return this;
        },
        async next() {
            return new Promise((resolve, reject) => {
                const id = consumeId++;
                consumeId = consumeId % Number.MAX_SAFE_INTEGER;
                const task = [resolve, reject, id];
                consumer.push(task);
                slide();
            });
        },
    };
}
export default concurrentPool;
//# sourceMappingURL=concurrentPool.js.map